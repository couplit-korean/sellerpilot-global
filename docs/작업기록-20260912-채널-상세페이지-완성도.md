# 작업 기록 · 2026-09-12 · 채널/상세페이지 완성도

이 문서는 2026-09-11~12 세션에서 확인·구현·검증한 내용을 기록한다. 완료로 단정할 수 없는 항목은
"미검증"으로 명시한다.

## 1. 이미지 생성 경로 (원인 규명)

| 항목 | 사실 |
|---|---|
| 상세페이지 좋은 이미지의 생성 주체 | **맥 Codex CLI** (`mode: "cli"`, 09-02 성공 작업 `e0ae47da`) |
| Vercel 서버 경로 | `openai/gpt-image-2` 등 이미지 모델이 **이 계정 무료 티어에서 403** (`Free tier requests on this model are rate-limited`). 배포된 앱에서 직접 프로브해 확인 |
| 1차 6장이 크롭으로 나온 이유 | 서버 프리플라이트가 게이트웨이 이미지 생성 실패 시 **조용히 원본 사진 카탈로그로 폴백** |
| 조치 1 | 폴백 사유를 결과 경고로 기록 (`1차 이미지 6장은 AI 생성 대신 원본 사진 기준으로 만들어졌습니다(사유: …)`) — 배포됨 |
| 조치 2 | 맥 Codex로 1차 6장 실제 생성 검증: portrait·wide·detail-overview·detail-use·detail-routine·detail-scale **6장 생성 성공**(카테고리 매칭 → 설정샷 계획 → `buildAssetImagePrompt` → `codex exec --enable image_generation`) |
| 남은 것 | Codex 계정 한도(Plus `You've hit your usage limit`), Pro 백업은 refresh token 폐기 → 재로그인 필요 |

## 2. 상세페이지 작업 생성 경로 (웹에서 동작)

| 항목 | 상태 |
|---|---|
| 서버 enqueue 엔드포인트 | `app/api/admin/product-studio-enqueue/route.ts` 신규. 판매자 값 검증 → 1차 사진을 서비스 키로 새 작업 경로 복사 → lineage receipt 발급 → `sellerpilot_create_ai_job` 생성 |
| 검증 | 실제 호출로 작업 생성 확인(`2ea6ffff…`, `a138a655…`, `77f46275…`, `be8b192f…`, `6dce1ac4…` 모두 202 queued), 맥 워커가 claim 후 Codex 시작까지 확인 |
| 앱 연결 | "상세페이지 제작 시작" 버튼이 이 엔드포인트를 호출하고, 화면이 `/api/ai/jobs/{id}`를 폴링해 생성 자산을 1차 이미지 자리에 병합 (`def0b42`) |
| 미검증 | 생성 완료까지의 실측(계정 한도로 중단), UI 클릭 후 끝까지의 브라우저 실측 |

## 3. 채널별 코드 완성도

| 채널 | 구현 완료 | 남은 것 |
|---|---|---|
| 쿠팡 | 등록 파이프라인, 실제 등록 성공(`16375780938`) | 없음 |
| 11번가 | 원격 상품 존재(`9598600918` HTTP 200) | 게시상태 readback 계약 정리 |
| Qoo10 | readback 구현 + **진단 보강**(필드별 reason code, `sellerpilotPublicationDiagnostics`, 미지원 필드 명시 제외) · 커밋 `d9cb1ff` | 게시상태 실측 |
| eBay | 조회/검증/핸드오프 + **정책·재고위치 생성 부트스트랩**(`lib/channels/ebay-account-bootstrap.ts`, `app/api/admin/ebay-account-bootstrap/route.ts`, 15 테스트) · 커밋 `e2db0a8` | 부트스트랩 UI 연결(진행), 계정 정책 생성 권한 |
| Shopee | **숍 정체성 영구화**(`lib/channels/shopee-shop-identity.ts`, discovery 후 크레덴셜 저장, fence 병합) · 커밋 `338515b` + 대상 목록 원장 병합(`6c285b3`) | 실제 DB credential refresh 경로 라이브 검증 |
| Temu | mall/region 결속·attestation 구현됨(기존). 화이트리스트에 맥 IP `112.172.127.206` 등록 확인. 서버리스 레인 목록(`elevenst,smartstore`)에 temu 없음 → **맥 레인 전용** | IP 오류를 명시 코드로 노출(진행), 라이브 호출 검증 |
| 라자다 | **실패 사유 정규화**(`lib/channels/provider-listing-failure.ts`, 16 테스트) · 커밋 `a1c53c9` | 실제 프로바이더 오류 어휘 실측 |
| 스마트스토어 | 동일 정규화 적용 · 로그인 재완료(사용자) | 실제 등록 시도/자격검사 실측(응답 지연으로 재시도 필요) |

## 4. 운영 정렬 (반복 수행한 절차)

릴리스가 바뀔 때마다: Vercel Production Ready → `canary_activate` → 어댑터 8개 + 재조회기 재인증 →
`local_channel_executor_routes` 릴리스 재지정 → 게시 게이트 재개방 → 워커 재기동(버전/egress 일치 확인).
마지막 상태: 릴리스 `7da49c5…`, 게이트 `effectiveOpen true`, 라우트 9건 정렬, 어댑터 8/8 + 재조회기 200.

## 5. 워커 실행 환경

`scripts/product-ai-worker.mjs`에 `SELLERPILOT_CODEX_SANDBOX` / `SELLERPILOT_CODEX_READ_SANDBOX`를
추가해, Codex 샌드박스 헬퍼를 적용할 수 없는 호스트에서도 생성이 가능하게 했다(`7ec8551`).
`SELLERPILOT_LOG_RAW_FAILURE=1`이면 원본 오류를 로그로 남겨 계정 한도와 계약 실패를 구분할 수 있다.

## 6. 완료로 단정할 수 없는 것

- 8채널 전부에 대한 실제 상품 업로드 성공 (검증된 것은 쿠팡·11번가, Qoo10은 수정만 확인)
- 상세페이지 생성 완료 실측 (Codex 계정 한도)
- Shopee write-back의 프로덕션 DB 경로, 라자다/스마트스토어 실제 오류 어휘, eBay 정책 생성 실호출

## 7. 추가 반영 (같은 날 후속)

| 항목 | 내용 | 커밋 |
|---|---|---|
| eBay 부트스트랩 UI | 상품 등록 워크벤치에 `eBay 정책·창고 자동 확보` 액션/입력 폼 추가, 성공 시 해결된 정책·창고 값을 기존 핸드오프 저장 경로로 저장. 값은 운영자가 입력(사전 채움 없음) | `8d75808` |
| Temu egress 화이트리스트 분류 | Temu 호출이 IP 화이트리스트에 걸리면 `TEMU_EGRESS_IP_NOT_ALLOWLISTED` + egress sha prefix와 한국어 안내를 노출(attestation·authoritative read 양쪽). 워커가 측정한 egress만 표시하고, 관측값이 없으면 null로 명시 | `178330f` |

최종 운영 정렬: 릴리스 `8c6b35c…` 활성, 어댑터 8/8 + 재조회기 재인증 200, 실행 라우트 9건 정렬,
게이트 `effectiveOpen true`, 워커 2종 재기동(등록 워커 버전 `…+8c6b35c…`).

검증 범위 고지: 위 두 항목은 타입체크·eslint·단위테스트(각 10건)까지 확인했고, 라이브 eBay/Temu 호출과
브라우저 클릭 왕복은 아직 실측하지 않았다.
