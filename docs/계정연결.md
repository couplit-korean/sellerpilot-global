# SellerPilot 계정 연결 (이 파일만 보면 됨)

헷갈리면 이 문서가 정답이다. 다른 `.md`와 다르면 여기를 고친 뒤 따른다.

## 역할 (처음부터 이 구조가 맞음)

| 하는 일 | 계정 | 비고 |
|---|---|---|
| 코드, GitHub 푸시 | `@Kimchanghee` / `k931103@gmail.com` | Chrome 프로필 CHANGHEE |
| Vercel 배포, 환경변수 | `couplit.official@gmail.com` | Chrome 프로필 JEONGHUN, 팀 `couplitofficial-4206` |
| Supabase DB·Vault | `couplit-korean` / `couplit.official@gmail.com` | 프로젝트 `sqaoqucxakebqkiygdxb` |
| 쇼핑 셀러·개발자센터 | `k931103@gmail.com` | CHANGHEE |

**GitHub를 k931103으로 한 것은 맞다. Vercel/Supabase를 couplit.official로 한 것도 맞다.**  
문제는 그 둘이 서로 Git으로 안 이어진 것이다.

## 지금 실제 선

```
이 Mac
  git push origin  →  GitHub Kimchanghee/sellerpilot-global   ← 코드 여기 있음 (브랜치 integration-aside)
  git push couplit →  GitHub couplit-korean/sellerpilot-global ← 지금 빈 저장소, 403

Vercel sellerpilot-global
  로그인: couplit.official
  Git 연결: couplit-korean/sellerpilot-global  (빈 저장소)
  그래서 Kimchanghee에 푸시해도 배포가 안 됨

프로덕션 https://sellerpilot-global.vercel.app
  마지막 배포 SHA: 6f378b4 (약 4시간 전 integration-aside)
  IM dual-app 코드(faddf78 이후)는 아직 없음
```

## 절대 하지 말 것

- Vercel Git을 끊고 빈 `couplit-korean` 리포에 다시 붙이지 말 것
- 유료 Vercel Static IP를 임의로 사지 말 것
- Kimchanghee 리포의 코드를 지우지 말 것
- 토큰·키 원문을 이 문서에 적지 말 것

## 고치는 방법 (하나만 하면 됨)

목표: **Vercel이 `Kimchanghee/sellerpilot-global`의 `integration-aside`를 배포하게 한다.**

### A. 정석 (추천)

1. Vercel (couplit.official) → Project Settings → Git
2. 지금 연결된 `couplit-korean/sellerpilot-global` 해제
3. GitHub 네임스페이스를 `Kimchanghee`로 바꾼 뒤 `sellerpilot-global` Connect
4. Production 브랜치가 `integration-aside`인지 확인
5. 배포 SHA가 `faddf78` 이후인지 확인

Vercel GitHub 로그인이 `couplit-korean`만 있으면 3번에서 Kimchanghee가 안 보인다.  
그때는 Vercel Account → Authentication에 `@Kimchanghee` GitHub를 추가로 연결한다.

### B. 우회

`couplit-korean/sellerpilot-global`에 실제 코드를 넣고 Kimchanghee에게 write를 준다.  
지금은 그 리포가 비어 있고, 협업자 초대는 `@couplit-korean` GitHub 패스키가 필요하다.

## 이 Mac에서 쓰는 git 명령

```bash
cd ~/dev/sellerpilot
git checkout integration-aside
git push origin integration-aside    # 항상 이쪽. 코드 원본
# git push couplit 는 권한 생길 때까지 쓰지 않음
```

## 확인 체크

배포가 이어지면 아래가 동시에 참이어야 한다.

- [ ] Vercel Git 연결 이름이 `Kimchanghee/sellerpilot-global`
- [ ] Production Ready SHA가 `faddf78` 또는 그 이후
- [ ] SellerPilot 화면의 서버 배포 SHA가 위와 같음
- [ ] Lazada CS가 “IM 수신 미연동”이 아니게 바뀜 (dual-app 코드 반영)

이 네 개가 아니면 아직 안 이어진 것이다.
