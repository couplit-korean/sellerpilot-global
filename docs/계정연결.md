# SellerPilot 계정 연결

2026-09-05 운영 조회 기준. 계정 경계는 이 문서, 실시간 운영 완료 상태는 [현재상태.md](./현재상태.md)를 기준으로 한다.

## 역할 (처음부터 이 구조가 맞음)

| 하는 일 | 계정 | 비고 |
|---|---|---|
| 코드, GitHub 푸시 | `@Kimchanghee` / `k931103@gmail.com` | Chrome 프로필 CHANGHEE |
| Vercel 배포, 환경변수 | `couplit.official@gmail.com` | Chrome 프로필 JEONGHUN, 팀 `couplitofficial-4206` |
| Supabase DB·Vault | `couplit-korean` / `couplit.official@gmail.com` | 프로젝트 `sqaoqucxakebqkiygdxb` |
| 쇼핑 셀러·개발자센터 | `k931103@gmail.com` | CHANGHEE |

GitHub 코드 저장소와 Vercel/Supabase 계정은 별개다. 현재 Vercel은 `couplit-korean/sellerpilot-global`의 `main`을 배포한다. 계정이 다르다는 이유로 기존 Git 연결이나 마켓 앱 연결을 끊지 않는다.

## 지금 실제 선

```
이 Mac
  git push origin   →  Kimchanghee/sellerpilot-global     코드 원본 (integration-aside)
  git push couplit  →  couplit-korean/sellerpilot-global  Vercel이 이 리포를 배포함
                       읽기 확인됨. push 권한은 실행 시 별도 결과로 확인

Vercel sellerpilot-global
  로그인: couplit.official
  Git 연결: couplit-korean/sellerpilot-global
  Production 브랜치 설정: main
  작업 브랜치: integration-aside  (main에도 같은 SHA를 푸시해야 Production 자동 배포)

프로덕션 https://sellerpilot-global.vercel.app
  2026-09-05 19:45 KST Production SHA:
  c3d1bf117d61dfd5877d96af31c5bc8976a3c2bf
  DB active release / attested release도 같은 SHA
```

## 절대 하지 말 것

- Vercel Git을 끊고 빈 `couplit-korean` 리포에 다시 붙이지 말 것
- 유료 Vercel Static IP를 임의로 사지 말 것
- Kimchanghee 리포의 코드를 지우지 말 것
- 토큰·키 원문을 이 문서에 적지 말 것

## 작업·배포 경계

- 최신 유지보수 checkout: `/Users/kimchangheemac/dev/sellerpilot`, `integration-aside`.
- 사용자 폴더 `/Users/kimchangheemac/Documents/ChatGPT/ai 쇼핑 채널 등록 자동화`는 다른 브랜치·이전 SHA일 수 있다. 실행 전에 확인한다.
- 2026-09-05 복구 통합 작업은 `/Users/kimchangheemac/dev/sellerpilot-recovery-20260905`에서 분리했다.
- `origin/integration-aside`에 코드가 push됐어도 운영 배포 완료는 아니다. 실제 Vercel deployment·도메인·SHA와 DB runtime release를 대조한다.
- `couplit/main`의 변경은 production 자동 배포를 만들 수 있다. docs만 이 브랜치에 올려 release SHA를 깨뜨리지 않는다.
- Vercel CLI 읽기 조회는 이번 점검에서 성공했다. 과거 CLI 배포 차단 기록을 현재 모든 CLI가 불가능하다는 뜻으로 해석하지 않는다.

## 이 Mac에서 쓰는 git 명령

```bash
cd ~/dev/sellerpilot
git checkout integration-aside
git push origin integration-aside    # 항상 이쪽. 코드 원본
# couplit/main push는 별도 운영 release 절차에 포함할 때만 실행
```

## 확인 체크

배포가 이어지면 아래가 동시에 참이어야 한다.

- [x] Vercel Git이 `couplit-korean/sellerpilot-global`의 `main`을 사용
- [x] Production Ready SHA `c3d1bf117d61dfd5877d96af31c5bc8976a3c2bf`
- [x] DB active/attested release가 같은 SHA. 새 복구 커밋 배포는 아직 별도 미완료
- [ ] Lazada IM 실호출: 판매자센터는 137451·137571 Connected. Vault `provider_account_subject` 없음. 이메일 OAuth short-code 오류. CS UI IM 미연동

Git·배포 연결과 마켓 CS 연결은 별개로 판정한다. 브라우저 사용 전에 Chrome 프로필 목록을 확인하고 판매자센터는 CHANGHEE, Vercel/Supabase는 JEONGHUN으로만 접근한다.
