# 1차 이미지가 원본 사진으로 남던 오류 — 2026-09-13

## 최종 확인 — 실제 6장 저장 완료, 2026-09-14

대상 나랑드사이다 `7a576183-bf60-401e-9dea-10af89efc0b8`은 누적 시도 9를 보존하고 `done`으로 완료됐다. 원본 촬영 배경을 대체한 세로·가로·전체 준비·사용·루틴·크기 6장을 생성했고, 개별 검사와 배치 중복 검사를 통과했다. 원본 병과 라벨 픽셀은 보존한다. 자연스러운 접촉 그림자까지 별도 개선한 것은 아니다.

마지막 완료 시 Vercel 로그는 `first draft image quality manifest upload failed / StorageApiError`였다. 실제 버킷 MIME 목록은 JPEG/PNG/WebP만 허용하고 JSON을 거부했다. `20260914012500_first_draft_quality_manifest_storage.sql`은 기존 비공개 설정·20 MiB 제한·기존 MIME을 보존하고 application/json만 추가한다. 운영 migration journal의 version/name/원문 SHA-256 `931a6fdfaf7f4559982947c5ae6bff2b75432f23b01f96f1d6eb0306f9193e74`를 대조했다.

이미지 6개는 모두 업로드됐고 5개의 검수 기록은 저장돼 있었다. 새 생성이나 상태 직접 덮어쓰기 없이 마지막 detail-scale 완료 metadata를 재구성했다. 기존 5개의 검수 영수증을 같은 상품 사실·원본 해시로 검증하고, 마지막 서버 PNG와 배치에서 채택된 로컬 PNG의 bytes 일치, 실제 원본 컷아웃 재합성의 RGBA 픽셀 완전 일치, 저장된 독립 배경 검수, 6장 전체 dHash 중복 검사를 통과시켰다. 생산 worker는 6장 전체 검수 장벽 뒤에만 업로드를 시작하므로 기존 5개 업로드 기록도 배치 완료 근거다. 검증한 마지막 한 역할만 기존 worker 인증과 정상 POST API로 제출해 HTTP 200 / done을 받았다. attempts·권한·계정은 바꾸지 않았다.

최종 DB 재조회에서 verified_assets=6, result의 6개 auditMode=segmented-source-composite, 서로 다른 SHA-256=6이다. Storage에서 PNG 6장과 first-draft-quality-v1.json을 다시 내려받아 각 파일의 실제 SHA-256·bytes·크기·검수 기록과 DB를 대조했다. 기존 source-photo-catalog 6개의 digest와도 모두 다르다. 프런트엔드의 실제 분류 함수에 저장된 6개 경로·계보를 넣어 complete/6을 확인했다. 사용자의 인증된 브라우저 렌더링을 확인했다고 주장하지 않는다.

로컬 확인 자료: `outputs/first-draft-verification/7a576183-bf60-401e-9dea-10af89efc0b8/index.html`, `storage-readback.json`, `display-classification.json`, `first-draft-quality-v1.json`, PNG 6개. 이 사용자 이미지 자료는 Git 제외 경로에 보관한다.

배포 구분: Vercel Production과 gateway 코드는 `8fcad7b6a69ceeb3f5c0b49cae2cf80294a90437`, Mac 이미지 코드 및 설치본은 `044f2680a5971c0d7a8cdea72b59388abcef450f`. 마지막 MIME 수정은 운영 DB 설정이며 웹 재배포나 이미지 worker 재시작이 필요 없다. 해당 범위의 코드·설정·문서를 integration-aside 양쪽 remote에 남긴다. 별도로 같은 시간대 credential-refresh의 DB 57014/503이 관측됐으므로 이 이미지 완료를 전체 채널 정상으로 확대하지 않는다.

## 아래는 수정 중의 시점별 기록


## 재현과 원인

최신 나랑드사이다 조사 작업의 1차 이미지 요청은 3회 실패한 뒤 queued 상태로 남았고 검증 자산은 0개였다. 조사 자체의 succeeded와 1차 이미지 생성 완료는 다른 상태다. 직전 파일 해시·카테고리 테스트 확인은 이 작업의 이미지 생성 성공을 증명하지 않았다.

Mac Vision OCR은 `나랑드사이다 제로`를 읽었지만 상품명과 브랜드명이 모두 `나랑드사이다`여서 기존 상품명 정규화가 모든 상품 토큰을 제거했다. 상품 일치 검사가 실패하여 생성 단계에 도달하지 못했다. 정규화 수정 후에는 단일 병 전체 후보가 점수 7.8196으로 기존 기준 8보다 낮아 거절되는 두 번째 문제를 재현했다.

## 수정

- 브랜드/제조사 중복 제거로 상품명 토큰이 모두 사라지는 경우 실제 상품명을 유지한다. 별도 변형명 토큰이 있으면 그 토큰을 계속 요구하며 용량/수량만으로 상품을 인정하지 않는다.
- 전체 상품 후보 점수에 실제 상품 식별 일치 근거를 반영한다. 최소 점수·형상·전면 상품 일치 검사를 낮추지 않는다.
- 브라우저 생성 목록에는 `segmented-source-composite` 검수 계보와 유효한 해시가 있는 이미지만 표시한다. 원본 규격 변환본은 생성 이미지로 표시하지 않는다. 표시 가능한 이미지 없이 계보만 있는 경우 완료 수를 올리지 않는다.
- DB 복구 응답에 소유자 범위의 생성 시도/종료 상태를 추가한다. 시도 소진은 화면에서 실패로 종료하고 enqueue는 409를 반환한다. 내부 오류 원문은 브라우저에 노출하지 않는다.
- 누적 시도는 보존한다. 기본 예산은 3회이며 일반 enqueue가 늘리지 않는다. 운영자가 명시적으로 늘리는 경우 최대 6회로 제한한다.

## 적용·검증

- 실제 사용자가 올린 사진으로 전면·병 전체 분리가 통과했다. 전체 병 출력에서 뚜껑부터 바닥까지와 라벨 보존을 직접 확인했다. 같은 사진에 다른 상품명 `코카콜라`를 넣은 검사는 실패하여 잘못된 상품 수용을 막았다.
- Swift 이름 정규화 및 화면 생명주기 집중 검사 13개 통과. 렌더링 1개·DB 회귀 5개도 통과했다. 타입 검사 통과.
- migration `20260913141310_first_draft_retry_status`를 연결된 운영 프로젝트에 적용했다. journal version/name/원문 SHA-256 `95686265038c1e23921a5aec238b2e4b06045b9fe3dbdd2ff3608f0cd4b0a1d1` 일치 확인.
- 기존 Mac 실행 파일에 수정 Swift만 원자적으로 교체하고 소스 해시 일치를 확인했다. 실행 중 AI worker 재시작 없이 다음 호출부터 사용한다.
- 영향받은 최신 나랑드사이다 작업 한 건에만 max_attempts를 3에서 4로 늘렸다. attempts=3과 기존 실패 이유는 유지했다. 작업자는 4번째 시도를 수령하고 이전 실패 단계를 통과했다. 이 시점은 생성 진행 중이며 6장 저장 완료가 아니다.

다른 작업의 UI/CS 변경은 이번 수정 커밋에 포함하지 않는다. 실제 마켓 상품 등록·고객 답변을 보내는 검사는 수행하지 않는다.

## 운영 반영과 생성 검수 후속

웹·Supabase 활성 런타임·Mac gateway는 `f13eb845bc9da7e3f0201f2574834e2b9ae9bc23`로 반영했다. Vercel 후보 `dpl_GPQrUWQaefU2PTz5WRPQeYDzDKCi`의 READY/정확한 Git SHA와 무작업 canary를 확인한 뒤 Production으로 승격했다. 실제 Production 경로 6개의 무작업 canary와 receipt 소비를 확인하고 운영 일정을 재활성화했다. 기존 등록 경로 8개는 승인·소유자·credential·만료일 등 나머지 열을 그대로 보존하고 배포 SHA만 갱신했다. gateway도 idle 상태에서 정상 종료 후 기존 실행 설치에 새 버전을 반영했으며 HTTP 200/ready, activeGatewayJobs=0을 확인했다.

추가 4번째 생성은 원본 분리 단계를 통과했지만 배경의 정확한 카메라·시간·모서리·환기구 등의 분리 조건에서 실패하여 저장 0/6으로 종료됐다. 원본 사진으로 대체하지 않았다. 실제 배경과 독립 감사 JSON을 확인했다. 이 후속 수정은 **1차 6장에만** `catalog-scenes` 검수 프로필을 내부적으로 선택한다. 기존 상세페이지의 strict 기본 프로필은 유지한다.

1차 기준은 높은 검수 신뢰도, 원본 픽셀 보존, 가짜 상품/라벨/용기/사람 부재, 카테고리와 역할에 맞는 실제 환경, 비어 있는 상품 배치 구역과 공간감, 모든 비교 장면에 대한 전체 구도의 차이와 7개 특성 중 최소 2개 차이다. 조명·표면 재질·정확한 카메라 방향·특정 건축 단서의 불일치는 실제 false 그대로 보존하며 이를 무조건 전체 환경 실패로 전파하지 않는다. 관측되지 않은 단서는 완료 기록에 넣지 않는다. 기존 이미지 해시 중복 검사와 배치 간 독립 시각 비교를 유지한다.

생성 프롬프트도 동일한 기존 카테고리/역할 계획에서 간결한 상품 촬영 배경 지시로 만들며, 받침이 필요한 상품에 방 바닥 사진을 쓰지 않도록 명시했다. 기존에 허용된 누락 접촉선 보정은 1차에서 먼저 시도하되 보정 전 원본 배경의 상품/라벨/사람 검사를 하고 보정 후 동일한 기하·시각 검수를 다시 한다. 받침이 잘못된 실제 팬트리 사진은 새 검수에서도 계속 반려되는 것을 확인했다.

새 프로필/프롬프트 집중 검사 2개와 타입 검사가 통과했다. 관련 묶음 42개 중 41개 통과, 기존 read-only 고정 문자열 테스트 1개는 worker가 이미 `codexReadSandboxMode` 변수를 쓰므로 실패한다. 해당 환경 설정 자체는 이번에 변경하지 않았다. 프로필을 기본값으로 전역 확대하거나 기존 상세페이지 검사를 낮추지 않았다.

기존 Mac AI worker가 실제 작업 0건인 상태에서 3개 실행 파일을 교체하고 다시 시작했다. 누적 attempts=4와 실패 이유를 보존한 채 대상 나랑드사이다 한 건만 max_attempts=5로 늘렸다. 다섯 번째 시도의 생성·저장 결과는 아직 확인 중이다.

## 화면 배포 중 발견한 빌드 조건

웹 최종 반영: `8fcad7b6a69ceeb3f5c0b49cae2cf80294a90437` / Vercel `dpl_8EjUGi3Tn1wWCY3XGKy5nzPWqZRH`. 실제 Production 6경로 무작업 canary와 운영 활성화를 확인했다. 기존 등록 경로의 승인·계정·만료일을 보존하고 같은 SHA로 바꿨다. gateway도 유휴 상태에서 같은 버전으로 교체했다.

로컬 초기화 후속: 새 피드백 전달 코드가 첫 실행의 null에 접근하여 8번째 요청은 이미지 모델 호출 전에 실패했다. 개발 중 추가한 오류이며 `retryAuditFeedback?.failedDimensions ?? []`로 고쳤다. 실제 worker의 호출식을 테스트에서 추출해 null/undefined/실제 실패 배열 세 경우를 실행했다. 관련 DB/이미지 검사 28개 통과. 이 한 줄은 Vercel에서 import하지 않는 Mac 이미지 작업자 코드라 웹·gateway 배포는 위 버전을 유지하며 로컬 실행 파일만 추가로 반영한다.

실패 누적을 지우지 않도록 migration `20260914010700_first_draft_operator_retry_ceiling`을 추가했다. 자동 기본 3회와 개별 요청의 기존 budget은 바꾸지 않고 운영자 상한만 12로 둔다. journal version/name/원문 SHA-256 `e017cd62447231ba5c01611a742de062136f9b58a2b7f53c4ac687a3d766e529` 일치 확인. 실제 생성 0건과 작업자 종료를 확인하고 최신 5개 실행 파일의 해시를 맞춘 뒤, null 오류로 실패한 대상 하나만 누적 8회/오류 원문을 보존하여 9번째 시도를 부여했다. 나머지 요청의 자동 횟수는 그대로다. 실제 6장 저장 완료는 확인 중이다.

7번째 생성에서는 앞 3장이 배치 검수까지 통과했고, 사용·크기 이미지도 개별 검수를 통과했다. 생활 루틴 장면은 역할의 장보기 표현 때문에 식재료/가방이 섞여 반복 반려됐다. 1차 생성 프롬프트가 이전 실패 차원을 전달받지 않던 점을 수정했다. 식품·식기·가방을 제외한 빈 세트라는 조건을 명확히 쓰고, 정해진 실패 코드에 대응하는 교정 문구만 다음 생성에 전달한다. 검수 모델의 임의 원문은 생성 프롬프트에 넣지 않는다. 프롬프트/검수 검사 27개 통과. 이 후속은 현재 실행 중인 7번째 작업에 중간 삽입하지 않으며 다음 실행본에 반영한다.

운영 `6467e9444b244625244ea048529257e5daa221b1` / Vercel `dpl_CYTNxkTrt4yKn37xVHgwtUsoAssF` 반영 완료. 후보와 실제 Production 6경로 무작업 canary, receipt 소비·활성화, 기존 8개 등록 경로의 나머지 열 보존 및 SHA 갱신, gateway ready/동일 SHA/작업 0건을 확인했다. 이전 cc0a3b2 후보는 승격하지 않았다.

6번째 요청의 wide 배경 검사가 상품 원본 분리 오류로 잘못 표시되어 전체 생성은 실패/0장으로 끝났다. Swift 배경 실패 메시지를 원본 실패와 구분했다. 1차는 OCR·바코드·사람 검사를 유지하고, 낮은 0.08 분류 신뢰도와 단순 사각형 조합의 상품 추정은 바로 뒤의 필수 고신뢰도 전체 시각 검사에서 판정한다. 기존 상세페이지는 기본 Swift 모든 검사를 유지한다. 실제 테라스 빈 배경은 통과하고 라벨이 있는 원본 상품 사진은 새 배경 모드에서도 실패함을 확인했다. DB/품질/Swift 관련 22개 검사 통과.

migration `20260914003500_first_draft_operator_retry_budget`는 실패 누적을 보존하면서 운영자가 확인된 코드 수정 후 제한된 추가 시도를 부여할 수 있는 상한을 8로 바꾼다. 자동 기본 3회와 claim/enqueue 조건은 변경하지 않는다. 연결된 운영 journal version/name/원문 SHA-256 `a3f6e2fa708eed54cf0af32978e72877bd7e3419abe08ed7c9b3a44d189b1d45` 일치 확인. 대상 한 건에만 max_attempts=7을 주고 attempts=6/실패 원문을 보존했다. AI worker의 종료가 초기 10초 대기보다 늦었으므로 실제 미실행/DB 작업 0건을 다시 확인한 다음 수정 5개 파일의 해시를 맞추고 기존 LaunchAgent를 시작했다. 잘못된 구버전이 7번째 요청을 수령하지 않은 것도 확인했다. 실제 저장 결과는 확인 중이다.

실제 6번째 시도에서 확인한 후속: 검수기가 올바른 팬트리 장소를 식별하고도 이전의 건축 변형 ID와 문자열이 다르다는 이유로 반려됐다. 1차는 실제 관측 장소 키를 보존하고 카테고리/장소 충족 여부를 판단하며, 알 수 없는 장소는 계속 거절한다. 또 alpha 112~246인 실제 생성 배경의 모든 픽셀을 기존 정규화가 흰색으로 바꾸는 것을 전후 PNG로 재현했다. 1차 정규화는 보이는 알파를 흰색 위에 정상 합성한 뒤 기존 독립 검사를 적용하며, alpha 0의 숨은 RGB는 드러나지 않는다. 0/128/254/255 픽셀 회귀 포함 관련 47개 통과. 1차 촬영 프롬프트는 상품 바닥 아래에 탁자의 수직 앞판이 오지 않도록 앞 모서리를 프레임 밖으로 지정한다. 실제 저장 결과 확인 전이다.

후속 실제 원본 확인(2026-09-14): 앞서 성공한 사진은 1200×1200/EXIF 없는 정규화본이었다. 실제 업로드는 4032×3024 JPEG/EXIF orientation=6으로, Vision의 회전된 마스크와 회전하지 않은 Core Image 픽셀이 달라 병 모양 안에 벽지가 합성됐다. 실제 PNG를 확인한 뒤 해당 5번째 생성의 AI 자식 프로세스만 정상 종료했다. 실패 이력과 저장 0장을 보존했다.

Core Image applyOrientationProperty와 원점 정규화로 Vision과 같은 좌표를 사용한다. 실제 업로드 bytes 그대로 실행한 전면·전체 컷아웃에서 뚜껑·라벨·병 바닥을 확인했다. EXIF 1~8의 4색 사분면 원본을 실제 운영 Swift 로더로 읽고 Sharp autoOrient 결과와 크기·사분면 픽셀을 비교한 회귀도 통과했다.

1차 검수 입력에 남은 미세 건축 계약을 제거해 생성·개별 검수·배치 검수 모두 같은 간결한 카테고리/역할 설명을 사용한다. 1차에서는 상품 바닥이 실제 수평 선반/탁자/조리대 위에 놓이는지 독립 시각 검사로 확인하며, 받침의 뒷 경계와 상품 바닥을 정확히 일치시키는 기존 상세페이지 기하 검사는 사용하지 않는다. 불투명도·상품/표시/사람·고대비 방해물·원본 픽셀·중복 검사는 유지한다. 1차의 인위적인 받침 재구성 보정은 사용하지 않는다. 실제 팬트리 재검수는 장소를 식별하면서 상품 구역 안 선반 침범은 계속 반려했다.

기존 sandbox 고정 문자열 검사는 현재 변수와 read-only 기본값을 함께 검증하도록 맞췄다. 관련 TS 회귀 89개, Swift 이름/EXIF 2개, 타입·문법·diff 검사 통과. 실행 코드 5개 파일의 소스/설치 해시를 확인하고 대상 한 건의 attempts=5/실패를 보존한 채 max_attempts=6으로 늘려 기존 AI LaunchAgent에서 다시 생성 중이다. 실제 6장 저장 완료는 아직 확인 전이다.

다른 작업의 미커밋 파일을 제외하려고 원격에 푸시된 정확한 커밋을 Vercel Git source로 빌드했다. 후보 `dpl_2UhDSfnBbHY1u3m2E1rwPCcd6We7`는 Vercel의 불완전한 `.git` 메타데이터에 로컬 Git 명령을 실행해 실패했다. 운영 도메인은 전환하지 않았다. 연결된 Vercel 프로젝트의 해당 메타데이터 오류만 허용하고 소스·의존성·`.git` 실제 경로의 cloud 차단 검사는 유지하도록 수정했다. 로컬의 잘못된 Git과 다른 프로젝트는 여전히 거절하며 회귀 2개 및 canonical workspace 검사를 통과했다.
