# SellerPilot 현재 상태

기준 시각: 2026-09-04 02:20 KST  
이 파일이 운영 사실의 단일 원장이다. 작업이 끝날 때마다 이 문서를 고치고, 다른 `.md`는 여기와 모순되면 여기를 따른다.

## 서비스

- 이름: SellerPilot 멀티채널 커머스 운영센터
- 운영 URL: https://sellerpilot-global.vercel.app
- 스택: Next.js + Supabase(Auth·Postgres·Vault) + Vercel Production
- 활성 채널 8: Qoo10 Japan, Shopee, Lazada MY, 쿠팡, 11번가, 네이버 스마트스토어, eBay, Temu
- 준비 채널: Alibaba.com, 1688 (비활성)

## Git·배포

| 항목 | 값 |
|---|---|
| 작업 브랜치 | `integration-aside` |
| 코드 push 가능 remote | `origin` = `Kimchanghee/sellerpilot-global` |
| Vercel Git 연결 | `couplit-korean/sellerpilot-global` (2026-09-04 재연결) |
| 이 Mac에서 couplit-korean push | 403 (`Permission denied to Kimchanghee`) |
| 최신 코드 SHA (origin) | `faddf78` Lazada IM dual-app routing |
| 관측된 Production Ready SHA | `6f378b4` Shopee oauth.exchange 게이트 축소 (IM 라우팅 미포함) |

배포가 코드와 어긋나는 이유: Vercel 팀 `couplitofficial-4206`의 GitHub 네임스페이스는 `couplit-korean`만 보인다. Kimchanghee 계정에 Vercel GitHub App은 설치돼 있지만 팀 로그인 연결에는 안 뜬다. `npx vercel` 같은 CLI 배포는 이 환경에서 차단된다.

`faddf78`을 프로덕션에 올리려면 (1) Vercel Login Connections에 Kimchanghee를 붙이거나 (2) `couplit-korean/sellerpilot-global`에 Kimchanghee push 권한을 주거나 (3) Vercel Dashboard에서 해당 SHA를 수동 배포한다.

## 인프라

- Vercel 팀: `couplitofficial-4206`
- Supabase 조직: `couplit-korean`, 프로젝트 ref `sqaoqucxakebqkiygdxb`
- 로컬 작업공간: `~/dev/sellerpilot`
- Aside 브라우저 계정: 쇼핑/개발자센터는 CHANGHEE(`k931103@gmail.com`), Vercel/Supabase는 JEONGHUN(`couplit.official@gmail.com`)

## 송신 IP (Vercel Static IP 미구매)

유료 Vercel Static IP는 쓰지 않는다. 채널 개발자센터 화이트리스트에 관측된 공인 IP를 등록한다.

| IP | 역할 | 비고 |
|---|---|---|
| 112.172.127.206 | 이 Mac 공인 IP | Shopee·Lazada·Temu 화이트리스트 |
| 178.105.152.68 | 기존 등록 IP | Shopee·Lazada |
| 54.180.114.210 | Vercel 동적 egress 관측분 | Shopee 2026-09-04 01:56 등록 |
| 3.34.137.35 | Vercel 동적 egress 관측분 | Shopee 2026-09-04 02:07 등록 |

Vercel 동적 IP는 바뀐다. `orders.list` 등이 `source_ip_undeclared`로 실패하면 에러에 나온 IP를 Shopee 콘솔에 추가한다.

코드 게이트: `lib/channels/serverless-cs-gateway.ts`의 `isEligibleClaim`은 Shopee `oauth.exchange`만 static-egress attestation을 요구한다. 나머지 Shopee 읽기/진단은 이 게이트를 통과한다. 쿠팡·11번가·Temu 목록은 그대로 둔다.

## 채널 연결 실측 (2026-09-04)

| 채널 | Vault | 최근 실측 | 남은 문제 |
|---|---|---|---|
| Qoo10 | 등록 | 읽기 진단 정상 (기존) | 실주문 0건 |
| Shopee | 등록, lineage `provider_certified_v1` | `get_shop_info` 성공 (Couplet Seoul / SG / NORMAL). 연결 검사 정상. access_token은 Mac IP에서 refresh | `orders.list`는 Vercel 동적 IP 미등록 시 실패. Partner Key 만료 2026-09-15 |
| Lazada | 커머스 앱 137451 + IM 앱 137571 토큰 동일 Vault 슬롯 | CS Bot OAuth 완료. `/im/session/list` 로컬 성공, 세션 1건 | 프로덕션 코드가 아직 `faddf78` 이전이라 앱 IM 경로는 구코드를 탐 |
| 쿠팡 | 등록 | 연결 검사는 고정 IP 워커 메시지 | static-egress 정책 미활성, 백로그 존재 |
| 11번가 | 등록 | 상품 읽기/등록 이력 있음 | 배송·CS 범위, 고정 IP 워커 |
| 스마트스토어 | 등록 | 기존 읽기 이력 | 고정 IP 워커 |
| eBay | 등록 | User OAuth 유지 | 연결 검사 워커 메시지 |
| Temu | 미등록 | Vault 키 없음 | 컴플라이언스·토큰 |

### Shopee 상세

- 자격증명 id: `fd44199e-4e16-4fa8-af4b-38ac4316fcab`
- Live Partner ID: `2031489`, Shop ID: `1719148844`
- 2026-09-03 access_token 만료 → 이 Mac에서 `/api/v2/auth/access_token/get` refresh 성공 후 Vault 갱신
- seller lineage를 `legacy_unattested`에서 `provider_certified_v1`로 인증. 이전 queued 잡은 `seller_account_key` null이라 claim 불가라 취소함
- 이후 새 `diagnostic.test`는 성공
- `orders.list`는 Shopee가 호출 IP를 강제함. Static IP 상품이 아니라 화이트리스트 등록으로 대응

### Lazada 상세

- 커머스: App Key `137451` (주문·상품)
- IM: App Key `137571` Couplit CS Bot, ERP IM Chat Active
- 콜백: `https://sellerpilot-global.vercel.app/`
- 판매자: Couplet Seoul / Malaysia
- Vault 필드: `app_key`/`app_secret`/`access_token` = 커머스, `im_app_key`/`im_app_secret`/`im_access_token`/`im_refresh_token` = CS Bot
- 코드: `lib/channels/protocols.ts` `resolveLazadaRequestPayload`가 `/im/*`만 IM 키 사용. 필드 없으면 `LAZADA_IM_CREDENTIALS_MISSING`

## 코드 원칙 (이 세션에서 확인)

- 신규 운영 사실은 이 파일에 먼저 적는다.
- 비밀 원문(토큰, Partner Key, App Secret)은 md·Git·채팅에 넣지 않는다.
- 이미 적용된 Supabase migration 파일은 수정하지 않는다. 새 파일만 추가한다.
- force-push 금지.

## 다음 작업

1. Vercel Git을 `Kimchanghee/sellerpilot-global`로 연결하거나 couplit-korean push 권한 확보 후 `faddf78` Production 배포
2. Shopee `orders.list`가 새 화이트리스트 IP로 통과하는지 재확인. 실패하면 에러 IP를 또 등록
3. Shopee Live Partner Key 로테이션 (만료 2026-09-15). Reset 후 사람이 전체 키를 복사해 Vault 동시 갱신
4. 쿠팡/11번가/스마트스토어/Temu static-egress·키 미등록 해소
5. 실주문 기반 배송·CS E2E (전 채널 0건)
