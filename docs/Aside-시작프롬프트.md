# Aside 복사·붙여넣기 실행 지시 — 기존 작업을 이어서 실제 등록까지

작업 폴더는 `/Users/kimchangheemac/dev/sellerpilot`, 브랜치는 `integration-aside`다.
다음 문서 2개를 읽고 바로 실행해.

- `/Users/kimchangheemac/dev/sellerpilot/docs/Aside-남은작업-병렬재개-20260906.md`
- `/Users/kimchangheemac/dev/sellerpilot/docs/Aside-작업원장-20260906.md`

첫 목표는 **이미 생성된 스마트스토어 등록 작업을 정상 처리하고 원격 상품 ID·실제 상세를 확인하는 것**, 다음 목표는 쿠팡 등 두 번째 채널 확보야. 채널을 처음부터 다시 고르거나 새 create를 보내지 마.

기존 job은 `66147e5d-0479-4c51-896e-97e782af99e1`, attempt는 `0d2c492e-2025-4717-bb3f-0fd2b886fd4f`야. 마지막은 queued/실행0/전송시작없음/원격ID없음이야. 현재 상태를 한 번 조회하고 동일 작업을 이어가. 결과가 불확실하면 새 멱등키·새 등록·새 recon 작업 생성/재조정 요청·수동 drain으로 우회하지 마.

총괄은 첫 등록·원격 재조회와 필요한 통합·운영 변경에 집중해. **현재 허용되는 최대 병렬 슬롯을 실제로 사용하고, 가능하면 하위 8개를 동시에 가동해.** 기존 8개 담당 ID·결과를 문서에서 확인하고 같은 담당에게 미완료 범위만 전달해. 순서는 DB/P0독립검증, 쿠팡, 기존상품채널, CS, 재고·배송, 상세·전용워커, Shopee, Lazada·Temu야. 기존 담당이 진행 중이면 중복 생성하지 마. 실제 인원과 전달 성공 여부를 기록하고 배정표만 쓰고 끝내지 마. 플랫폼 한도·모델 호출 제한은 우회하지 말고 현재 사용자 모델 설정을 유지해.

현재 작업 폴더의 읽기·쓰기가 되는지 먼저 확인해. 예전 작업의 `Path escapes Project and session roots`가 계속되면 설정값 변경만으로 해결됐다고 하지 말고 정상 프로젝트 컨텍스트에서 기존 task ID·증거를 인계받아 재개해. 권한을 우회하지 마.

스마트스토어 처리 지연은 서버리스 claim 제외 조건과 고정 IP 로컬 워커 경로를 구분해서 확인해. 21:04 후보 SQL은 적용 완료가 아니야. DB 담당이 원인·최소 수정·양성/음성 검증을 제공하고 총괄만 필요한 운영 변경을 처리해. 기존 소유권 수정 a3e3d08과 제목 없는 이미지 섹션 수정 d409055를 다시 만들거나 무조건 재배포하지 마.

로컬의 CS·재고·배송·상세·Shopee 수정과 원격에만 있는 커밋을 덮어쓰지 말고 정상 통합해. 통과한 동일 판본 테스트는 재사용하고 미실행 회귀/통합 영향만 검증해. 승인 외부 상세v2/8컷과 이미 확인된 판매 조건을 보존해.

공용 원장은 총괄만 수정해. task ID·실제 담당·상태·소유 파일/탭/채널·job/attempt·완료 증거·다음 행동을 기록해. 같은 파일·탭·외부 상품을 동시에 쓰지 마. 하위 종료를 운영 완료로 계산하지 마. 원격 ID와 등록 내용 재조회가 끝나야 첫 채널 완료야.

보호 미추적 SQL `supabase/migrations/20260903150000_unblock_shopee_second_oauth_deadlock.sql`은 읽기·수정·삭제·복사·커밋·테스트·적용 모두 제외해. 현재 사용자 브라우저 지침을 준수해. 고객 답변·실출고·유료 구매·main 변경은 실행하지 마.

첫 성공은 다른 하위 완료를 기다리지 말고 즉시 채널명·원격 ID·판매/심사 상태·사용 화면 경로와 함께 보고해. 막히면 현재 job 상태·정확한 차단·다음 행동을 알리고 다른 독립 작업은 계속해. **첫 응답은 실제 배정 결과와 바로 실행할 동작만 짧게 말하고 작업을 진행해.**
