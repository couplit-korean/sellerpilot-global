# 2026-09-06 운영 복구 및 반영 기록

## 이 기록의 기준

이 문서는 9/6 03시 인계 이후의 실제 실행을 기록한다. 코드 저장, 운영 SQL, 실제 읽기 진단, 이미지 승인, 배포와 외부 게시를 구분한다. 보호 미추적 SQL은 모든 작업에서 제외한다.

## 사용자 승인

- 사용자는 쿠팡 추가판매정보 비밀번호 인증을 완료했다고 알렸고, 운영 작업을 계속 실행하도록 요청했다.
- Supabase Nano → Micro 확인창의 **추가 $0/월**을 제시했다. 기존·변경 후 모두 월 추정 $9.68, 자동 재시작 및 평소보다 긴 중단 가능성을 고지했고 사용자는 **무료 전환 진행**을 선택했다. 이 한 번의 전환을 실행했다. 다른 증설·추가 유료 옵션·spend cap 변경은 하지 않았다.
- 원본 `Kimchanghee/sellerpilot-global`은 private, 배포 연결 `couplit-korean/sellerpilot-global`은 public임을 확인했다. 코드뿐 아니라 운영 문서·커밋 이력도 공개됨을 고지했고 사용자는 **새 코드 공개 허용**을 선택했다. 이 승인은 비밀정보·고객 원문 노출 허용이 아니다. 이 공개 승인만으로 main/Production을 변경하지 않았다.
- 이후 별도로 **검증된 738934e의 Production 승격 허용**을 받았다. Git main과 유료 설정은 변경하지 않는다. Vercel이 Production 환경으로 새 빌드하는 것을 확인했고, 기존 Production의 비밀이 아닌 `SELLERPILOT_RELEASE_SHA=c3d1bf1…`가 새 Git SHA와 충돌하므로 이 값 하나만 승인 커밋 전체 SHA로 수정한 재빌드를 진행했다. Preview 환경·키·모델 설정은 변경하지 않았다.

## 실제 DB 복구

- 공식 화면에서 자원 소진 경고와 Nano/Micro 동일 요금을 확인했다. 전환 후 실제 `t4g.micro`, 1GB가 선택된 상태를 확인했다. 과거 그래프 수치를 현재 사용률이나 근본 원인으로 단정하지 않는다.
- 실제 Management SQL 정상 응답, 현재 로그인 관리자의 owner-proof RPC **200**, 상품 publish-context **200**, 기존 비공개 원본 JPEG **200 / 155,900 bytes**를 확인했다. 원본 SHA256 `4c9f5bf32d93251bb21f5baf622daa66c2e45484c3b9e1d5ab119c4e5135cc16`.
- Storage 사용자 list의 0건은 실물 부재 증거로 쓰지 않았다. 기존 정책에는 sellerpilot-ai 업로드/삭제만 있고 일반 SELECT 정책은 없다. 서버가 제공한 기존 원본 signed URL의 실제 다운로드로 읽기를 별도 검증했다. signed URL/토큰은 기록하지 않는다.
- 로그인 화면의 기존 키 미등록/권한 오류 대신 원장 연결과 운영 키 7개가 다시 표시됐다. DB 역할 조회 오류를 비밀번호·관리자 권한 부족으로 오판하던 코드는 별도로 수정했다.
- 증거: 세션 `tmp/micro-recovery-functional-evidence.json`, `tmp/supabase-free-micro-review.png`.
- 기존 Supabase 티켓 SU-464710의 접수 당시 접근·재시작 미승인 기록은 역사적 사실로 보존한다. 위 사용자 승인 후 무료 Micro 전환만 새로 수행했다. 지원 접근 OFF와 기존 중지 routine은 그대로다.

## 쿠팡·스마트스토어 실제 연결 반영

### 쿠팡
- WING 판매자 A01601472, 등록 IP 112.172.127.206, 실제 키 만료 2027-02-11 01:51 KST 확인. 기존 Vault 키와 WING 키의 두 해시가 일치했다.
- 기존 `sellerpilot_update_credential_schedule`의 본문과 실제 두 trigger를 검토했다. 현재 사용자/admin/shared-workspace proof 및 메타데이터를 직전 검증하고 정상 RPC로 만료일만 저장했다.
- 05:23:45 KST 저장 **204**, 즉시 메타데이터와 proof 재조회 검증. 만료 UTC `2027-02-10T16:51:00+00:00`; v1, fingerprint F95F4754AFAE, 원래 생성자, 회전 180일·경고 14일·last_rotated_at·status 모두 불변.
- 09:32 KST 새 실제 상품 GET 후 정상 진단 RPC 기록 성공. 현재 IP·복호화 전후 권한/버전/지문·정확한 조회 목적지·실제 provider 호출을 검증했다. 키 회전·새 job·기존 diagnostic job 완료 위조 없음.

### 스마트스토어
- SELF 인증 및 seller/account의 실제 200을 확인했던 기존 앱/키를 유지했다.
- 09:33 KST 현재 IP·권한/지문을 다시 검증하고 정상 인증/계정 읽기를 실행, 진단 RPC 기록 성공. client secret 교체 없이 필요한 정상 access-token 요청만 수행했다.
- 현재 UI의 읽기 진단 집계는 **5/8**이다. 이번에 새로 기록한 것은 쿠팡·스마트스토어 두 채널이며 나머지 세 통과 기록을 이번 fresh 호출로 계산하지 않는다. 문의·주문 queued 상태나 상품 게시 완료와도 구분한다.
- 일회성 인증 파일은 실행 시 삭제됐다. 쿠팡 첫 호출은 파일 mode 644를 입력 단계에서 거절해 네트워크/기록을 하지 않았다. private 디렉터리 700과 파일 600으로 강화한 뒤 실제 한 번 실행했다. 보호 검사를 제거하지 않았다.
- 증거: `tmp/coupang-expiry-update-result.json`, `tmp/private-connection-20260906/*.receipt.json`, `tmp/connection-read-summary-current.png`. receipt에는 비밀정보가 없다.

## 운영 SQL: 실제 적용 및 사후검증

기존 130000 적용 기록은 유지한다. 아래 **열한 개 신규 migration**은 실제 적용·사후검증됐다. 이력은 **282개**, 최대 version은 20260906060000이며 마지막으로 적용한 파일은 20260906053000이다. 적용 순서는 version 정렬 순서와 달라도 실제 기록을 그대로 남긴다.

| migration | 실제 상태 | 원문 SHA256 |
|---|---|---|
| 20260906010000 | 먼저 201 수락, Micro 전환 후 본문/ACL/이력/실제 사용자 RPC 200 확인 | 1259c605a882636d4ddd1e7a6f93a63f904d908079e123ec200ab4a423ee6e80 |
| 20260905141000 | 09:25:20 KST 201, 이력273·원문 exact·함수5개/ACL 검증 | b11fac71bd38db7440b25cc6ac7590fe09406ed57badffabed962b5e8041b196 |
| 20260905142000 | 09:37:27 KST 201, 이력274·원문 exact·함수6개/ACL 검증 | 44a1dede016c9b2545b474fa35054d5d724927f8d674bf5b0678e96f8b5c7b83 |
| 20260906011000 | 09:37:45 KST 201, 이력275·원문 exact·service-only ACL 검증 | e42b10f28d8599d84d6bb94cedd63063991a4b11237a1b79c8eb2fe43f122f26 |
| 20260905140000 | 09:44:42 KST 201, 이력276·원문 exact·함수5개/ACL 검증 | 48ad520707b79c740fba3f846974fc979302d6efefa8c66e7ab86fd0c67675f1 |
| 20260906030000 | 10:51:09 KST 201, 이력277·원문 exact·함수7개/alias/ACL/terminal trigger 검증 | fc43861c980c74aa3ec135ab657669609fadfcfe097393253dfcd523395683a5 |
| 20260906050000 | 10:57:08 KST 201, 이력278·원문 exact·함수4개/ACL 및300 원문 보존 검증 | 32a9bd6c4d6f475c2a47d4d60a8cea45b94d78f508f1ad0826253d46e72b4b12 |
| 20260906060000 | 10:57:52 KST 201, 이력279·원문 exact·함수6개/ACL 및300 원문 보존 검증 | ff2099afdd470f642d3ad1c94803047f0ca18e1c86aee270679b4ca5e1e05205 |
| 20260906040000 | 11:17:06 KST 201, 이력280·원문 exact·RPC/비공개 PNG bucket/ACL 검증 | ad85277f4503e643bed79f82f4bf6b7574886d6c920961d15f7a7e4f65d77289 |
| 20260906043000 | 11:17:58 KST 201, 이력281·원문 exact·함수2개/ACL 검증 | 75cd390378716df067f53b6b1e3a7ae76c0093629b3d21c760dd98338931d9ee |
| 20260906053000 | 11:18:47 KST 201, 이력282·원문 exact·함수/원래 alias12개 및 전체 post97 검증 | 9399b9eda3c6a716fa13a3c6e06957d5f3c18ddfcb4f3d37c27c58e0659b9ce2 |

- 원문 SHA, 직전 이력, 함수 preimage를 고정하고 history 잠금·5초 lock timeout·30초 statement timeout·원문 그대로의 history 기록을 포함했다. 새 함수 signature/body/definer/owner/권한과 기존 상품·주문 컬럼 digest 불변을 같은 트랜잭션에서 검사했다. 각 적용 후 별도 READ ONLY 검증을 했다.
- 142의 최초 코드가 기존 Lazada 주문 provider_context를 초기화하는 P1을 로컬 fixture에서 발견했다. 원문을 남기고 새 blocked 필드만 설정하도록 수정했다. 실제 fulfillment 마스킹 및 confirm/acknowledge 차단 회귀를 유지했다. digest 제외 확대 없음.
- 140 첫 시도는 변수명 substring guard에서 **400**으로 거절됐다. READ ONLY로 history272·기록0·새 테이블 없음·원문 hash 불변을 확인했다. 운영 함수는 삭제 필터·비-Lazada 이력을 포함한 현재 wrapper였으므로, 실제 readback의 함수/의존성 fixture로 고치고 정확한 hash/ACL/ABI를 고정했다. 구 guard 완화나 단순 재시도가 아니다.
- 140은 native와 같은 **500개 / JSONB UTF-8 1,000,000바이트** 상한을 쓰기 전 검사한다. 삭제 tombstone을 격리 경로에도 적용해 삭제된 원문을 다시 보관하지 않는다. 범위 내 정상 buyer commit + 격리 partial 계약은 유지한다.
- 실제 재고 bootstrap, 수량 변경, 원격 상품 변경, 고객 답변 또는 송장 생성은 수행하지 않았다. 대상 상품의 11번가 create/Qoo10 update/Shopee create reconciliation은 그대로다.
- 증거: `tmp/post-reviewed-*.json`, `tmp/apply-reviewed-*-attempt.json`, `tmp/cs-apply-failure-readback.json`.

## 코드 저장 및 검증

- `5e9046d`: 인증·직접 읽기·최초 전용 OAuth 연결 등 이전 단계.
- `bce56e6`: 권한 서비스 장애 503/실제 토큰401/실제 권한없음403 분리, RPC redirect:error. 부모 93개 회귀.
- `3d6b3e8`: external_generated 상세 승인, immutable Storage·원본/문서 해시·채널 게시 경계. 부모 140개 회귀, 별도 failed/succeeded 실제 handler 각각7개.
- `7272058`: 기존 Lazada 주문 원문 보존. 부모 관련6개 회귀.
- `fbba80f`: Shopee/Lazada exact OAuth 및 새 실제 진단 실행기. 부모 통합107개 회귀.
- `8b2ef00`: 실제 삭제-aware CS preimage·의존성·상한·삭제 재유입 차단. 부모32개 회귀.
- 전체 `build:vercel`, 이후 순차 `tsc --noEmit --incremental false`, 변경 웹 코드 ESLint 통과. 위 묶음은 중복된 독립 검토 수치를 더해 전체 suite 통과로 과장하지 않는다. 역사 재생14실패와 native 보안 동등 복원 미검증은 유지한다.

## 다음 실제 반영 경계

- **SQL 적용·검증 완료:** external400/430/530, Shopee300, Lazada500/600까지 적용했다. 300 부모 현재-chain17개 및 wrapper9개, Lazada 현재-overlay/실행기/UI47개+별도500 fail-closed1개, wrapper 실패주입8개, 상세 실제-chain/3단계 wrapper 부모3개 및 실패주입9개를 구분해 검증했다. 합산을 전체 suite 통과로 쓰지 않는다.
- 상세는 실제 Storage 두 정책·관련17개 테이블 owner/RLS/ACL/47FK 및 현재 원천/trigger를 검토했다. OAuth 설치 후 기존47 trigger와 추가300 guard1개를 확인했다. 상품 전체 digest가 과거와 달라 중지해 확인했고, 경쟁가격 모니터링 조회시각 갱신을 관측했다. 과거 전체 행 동일성을 주장하지 않고 현재 상품·소유자·버전·원천을 다시 검토한 fresh baseline을 승인했다. 같은 baseline으로 **pre125와 post97** 통과; 각 wrapper 안에서도 전체 행 전후 digest를 유지했고 경쟁가격 컬럼 제외를 늘리지 않았다. 설치 직후 import/audit/신규 저장 객체는0, bucket은 private/PNG/10MB이며 상품 승인은 미실행이다.
- 근거: `tmp/post-shopee300-reviewed.json`, `tmp/post-lazada{500,600}-reviewed.json`, `tmp/post-external{400,430,530}-reviewed.json`, `tmp/external-parent-approved-baseline.json`, `tmp/external-{pre125,post97}-current.json`.
- **Preview 실제 배포 완료:** 공개 전 28개 커밋 범위와 최종 CS7파일/운영 문서의 비밀·고객 원문 검사를 마친 뒤, 문서 포함 `738934ecf5b90582a75b95355cf42d8cf7214d53`를 origin 및 공개 Couplit의 integration에 정상 fast-forward했다. 두 main은 불변. Vercel 자동 Preview가 **Ready / 1분49초**, `https://sellerpilot-global-jp8bmhor4-project-e59d.vercel.app`에서 저장된 sample 로그인과 실제 원장(키7/8, 저장 진단5/8, 상품1)을 확인했다. 새 Next 서버의 대상 상품 publish-context도 **200 / detailVersion1 / externalImportStatus:unavailable**로 미적용 SQL을 정상 구분했다. 재고 GET의 만료 처리는 실행하지 않았다. 이것은 SQL 설치 전 관측이다. 설치 후 Preview에서 실제 GET200, `externalImportStatus:available`, 서버 원본6경로/3종 해시를 확인했다. Production은 별도 승인 후 같은738934e를 Production 환경으로 재빌드·승격했다. 릴리스 SHA 설정을 일치시킨 배포 `sellerpilot-global-417rvraxp-project-e59d.vercel.app`은 Ready/2분12초, 운영 도메인·관리자 원장·현재 서버 SHA를 확인했다. 무작업 canary6개 후 별도 DB 읽기에서 active=true/activeRelease738934e/scheduleCount6/unsafePendingMutations0을 확인했다. 이후 검토된 콜백 코드용 Production `SELLERPILOT_SHOPEE_EXACT_CALLBACK_READY=1`만 추가하고 같은SHA를 다시 빌드한 `sellerpilot-global-394ebnnrr-project-e59d.vercel.app`도 Ready/2분14초다. 두 main은 실제 ls-remote로 불변 확인했다. Aside 지원 루틴과 유료·모델·키 설정은 변경하지 않았다. 증거 `tmp/preview-738934e-evidence.json`, `tmp/preview-publish-context-read-safe.json`.
- Shopee/Lazada exact는 actor·credential owner·worker issuer를 분리한다. 현재 활성 gateway token의 issuer는 7f448e38…이며 credential owner와 다르다. 기존 global workspace/scope 계약을 재사용하고 token·owner를 발명/수정하지 않는다. 완료 시 claim NULL을 그대로 두고 immutable receipt로 증명한다.
- 실제 generic/scheduled reaper가 exact OAuth를 다시 일반 큐에 내놓는 결함은 전용 terminal guard/만료 처리로 수정했다. 최신300에는 새 scheduled-reaper wrapper가 있다. 이전300에 wrapper가 있었다고 쓰지 않는다.
- **정식 상세8컷 승인 완료:** import `08acb37f-7ed0-40b0-8fb3-4a217a7ac912`, 12:11:16 KST reserve → 12:11:42 verify → 12:19:01 approve. 지문 `8f1ebe5d61834100351b96385d84f063f39b329e7e68b9b255abb286caf056f2`, 현재 상세버전2/외부 approvedDetailVersion2, legacy Studio approvedVersion0. 서버 원장·비공개 저장 객체8·검증 영수증8·서로 다른 픽셀 해시8·감사3건을 독립 READ ONLY로 확인했다. ko/ja/en 문서와 원문/픽셀 지문을 로컬 파일 근거와 대조했다. 첫 부모 비교 스크립트의 크기 필드 위치 가정을 바로잡아 request asset이 아닌 실제 receipt와 별도 파일 근거를 비교했으며, 서버 업로드/검증은 재실행하지 않았다.
- 서버 원본6경로/3종 JPEG는 로컬 고해상도 작업 참조와 동일 촬영 계보지만 바이트·픽셀·색이 다르다. 위치 정렬 명도 상관0.978~0.983·1200×1200 여백 규칙 대응을 기록했고 정확한 인코더 이력은 미검증이다. raw 서버 SHA3개와 로컬 작업 SHA3개를 audit에 분리했다. 기존 원장의 권리 확인true와 사용자 제작·등록 요청을 근거로 삼되 별도 라이선스·실물 검수를 주장하지 않는다. 모든 문서 reviewNote는 **AI 대행검수, 실물확인아님**이다.
- 승인 뒤 로컬 폼이 초기화된 상태에서 서버 승인본을 재로딩했다. **현재 승인본/version2/이미지8/8 로드**를 확인했다. 기존 sourceJob·listings·assignments·sourceObjectMetadata 네 전체 digest는 SQL 직전 기준과 동일하고, 실패 리비전09f8…는 여전히failed다. 새 상세의 원격 게시0/8, 기존 remote ID와 재고·가격·원천 lineage를 보존한다.
- 증거: `tmp/detail-import-parent-preapproval-proof.json`, `tmp/external-import-approved-postread.json`, `tmp/external-postimport-capture.json`, `tmp/detail-import-approved-proof.png`, `tmp/production-runtime-738-read.json`. 정식 외부 승인은 채널 게시·법규 적합·실물 확인·Studio 성공이 아니다.
- eBay USD가격/국제반품 선택, 11번가 이메일 미도착·정확한 유료배송 API 계약, Temu 분류/심사 답변, 승인된 실문의/출고 대상은 별도 남은 조건이다. 키가 이미 있는 채널을 미등록으로 다시 취급하지 않는다.


## 13:17 KST 후속: 선별 커밋·실제 exact 실행·로그인 경계

- 선별 공개 검사 16개 판본 SHA 일치 후 `39e64028ed20a9eb81ecb63ab8d194f293a6449c` 커밋, origin/공개 Couplit의 integration-aside에 정상 push. 부모 external 실경로3+UI45=48/48, skip0, 순차 tsc와 변경 UI ESLint·diff 검사 통과. 보호 SQL은 읽거나 stage하지 않았고 untracked로 유지했다. Production은 승인된738934e 그대로이며 새39e6402를 승격하지 않았다.
- 공개 검사: 외부 테스트/fixture·문구·동결문서10개는 JSON 문자열3,150개/JS 문자열691개를 디코딩 검사했고 실제 비밀·Vault 행·고객 원문 없음. 별도 부모 문서4개 diff 및 Lazada test/catalog 판본도 검사했다. 이 결론은 검사한 판본·범위 한정이다.
- 외부 실제-chain 테스트의 session tmp/artifacts/고정 사용자 경로 의존 제거. 단, `SELLERPILOT_BASELINE_FOLDER`의 비공개 `baseline.enc`/`baseline.key`는 필수이다. 없으면 exit1/skip0이며 공개 저장소만으로 완전 자립 실행된다는 주장을 하지 않는다. 자식은 repo와 다른 cwd 모두3/3, 부모는 위48/48을 확인했다.
- Production Shopee callback 준비값이 없음을 UI로 확인하여 비밀이 아닌 Config `SELLERPILOT_SHOPEE_EXACT_CALLBACK_READY=1`을 Production에만 추가했다. 동일738934e를 최신 설정으로 재빌드한 배포 `7Z9amvQMmM16eLDMEChuV91dE5vE`/`sellerpilot-global-394ebnnrr-project-e59d.vercel.app` Ready/2분14초와 운영 도메인을 확인했다. 두 main 원격 지문 불변. branch Stale 표시를 계기로 실제 배포 목록·두 원격 integration SHA를 읽어 다른 소스 승격이 없음을 확인했으며, 표시의 내부 원인을 추측하지 않는다.
- 인증 없는 빈 JSON 경로 확인: Shopee/Lazada admin exact는400 invalid_request, worker exact는401 unauthorized. 소스상 DB/prepare/claim 전 거부 경로이며 실제 실행 준비 완료와 구분했다.
- 기존 macOS Keychain의 정확한 Gateway Worker 항목 존재·값의 SHA에 대응하는 DB token02955/issuer7f/scope gateway/active/11월28일 만료를 검증했다. 환경변수의 다른 토큰 override 없음. 실제 egress112.172.127.206 일치, Node22.23.2에서 전용 parser/provider/Lazada executor 모듈 로딩 성공. 일반1.13 워커 설치·기동·키 회전 없음.
- **Shopee 실제 prepare/runner 수행, 교환은0회:** 세션494c3581-43cf-4844-90b2-d4d2cad2b043. actor768/credential owner528/worker issuer7f를 분리해 정상 결속했고 실제45초 pulse를 DB로 확인했다. 공식 인가 화면은 별도 비밀번호 재확인을 요구했다. Aside 및 사용자 승인 후 연결된 Apple 암호에 대응하는 Shopee 메인 로그인 후보가 없었다. 인증 코드·비밀번호는 이 문서에 기록하지 않는다.
- 이후 부모가 기존 한국 셀러센터를 먼저 확인하지 않고 사용자에게 로그인을 요청한 안내 순서 잘못을 인정했다. 기존 탭은 `gjrxn:main / Couplit.kr`, SG gjrxn.sg로 로그인되어 있었다. 사용자가 별도 공식 인가 탭을 닫았다. 자동 로그아웃·프로필 전환·연결 해제는 하지 않았다.
- Shopee 단일 실행기는9분 대기 만료로 종료. 별도 DB 사후읽기에서 jobId=null/delivered=false/freshPulse=false와 원래credential ID를 확인했다. 같은 세션·코드 재실행 없음. 현재 Merchant Setting → Partner Management → Couplit은 merchant Linking(2027-08-30), Using8/Frozen0/Expired0/Unlink0, 각숍2027-08-26 만료. 가능한 action은 Unlink뿐이며 누르지 않았다. 이 현재 승인은 실제 API 읽기 성공이나 새 OAuth lineage가 아니다.
- **Lazada 이어서 진행 중:** 기존 MY Seller Center에서 Commerce137451 및 CS Bot137571 Connected를 확인했다. 새 Production 탭의 관리자 세션을 재사용해 exact 세션b4154653-11ba-451e-bc32-cdcdd746b002를 한 번 준비하고 전용 실행기 시작. DB 읽기에서 actor/owner768, token02955/issuer7f, freshPulse=true, oauth/readback jobId=null 확인. 이 시점 공식 인가·교환·seller readback은 아직이다. 완료 후 이 절에 결과를 추가한다.
- 증거: `tmp/production-exact-route-denials.json`, `tmp/exact-selected-worker-metadata.json`, `tmp/parent-portable-external-ui-final.log`, `tmp/shopee-494c-readiness.json`, `tmp/shopee-494c-expired-postread.json`, `tmp/shopee-eight-using-continued.png`, `tmp/lazada-b415-readiness.json`. 전용 로그/PID는600으로 저장. 프로세스 목록 조회는 OS에서 거절되어 우회하지 않고 실제 DB pulse로 실행 상태를 확인했다.

- **13:25~26 후속:** 사용자 요청에 따라 어제9/5 23시대 문서와 대조한 `진행률-비교-20260906.md`를 작성했다. 키87.5% 불변, 읽기37.5→62.5%(+25%p), 새상세승인100%, 새상세게시0%를 분리했다. SQL이력 어제271→현재282(+11). 과거20:30의270을 어제최종값으로 사용하지 않았다. 전체출시의 임의가중%·E2E완료 주장 없음. WQ가 최초2문서판본의비밀/고객원문부재와분모정직성을독립검사했다.
- Lazada 공식 페이지는 기존로그인을인식, 앱137451/Couplit Commerce/countrymy/운영redirect일치. 보고작성중browser9분기한경과를확인해Authorize미실행. DB별도읽기는oauth/readback jobId모두null, 실행기NO_RETRY종료이며완료로그없음. DB15분세션잠금은13:30:09에정상만료하므로이를삭제/단축하거나동일세션재실행하지않는다. 다음새세션에서이어간다. 증거 `tmp/lazada-b415-before-new-session.json`.
- 39e6402 자동Preview `sellerpilot-global-13s0av4vp-project-e59d.vercel.app` Ready/1분20초를VercelUI에서확인. Production738934e유지. 증거 `tmp/progress-preview-production-20260906.png`.

- **13:32 Lazada 공식 인가 거절:** 정상만료후 새세션7c2120e9-b462-4d1c-9f7a-704a48782ee4로prepare/전용실행/start. 공식client_id137451/countrymy/운영redirect/기존gjrxn계정인식 확인. Authorize1회04:32:41Z 후 ‘The specificed seller short code is invalid. Please add the seller code into your app’s whitelist…’로거절. 응답페이지에서인가코드/콜백없음. Commerce137451의AuthorizedSellerWhitelist에MY4NNISR2D/Malaysia가이미존재하고, 실제로그인MY Settings의CoupletSeoul/SellerID300872000183/ShortCodeMY4NNISR2D와일치한다. 같은인가반복·허용목록삭제/재추가·임의seller값변경·CS Bot변경 없음. 공식AuthorizationUpgrade안내와현재요청계약을별도읽기전용조사중. 새세션DB읽기도oauth/readback jobId=null을확인했다.
- 증거: `tmp/lazada-official-whitelist-error.png`, `tmp/lazada-whitelist-my-present.png`(캡처범위는파일실물기준), `tmp/lazada-actual-seller-code-matches.png`, `tmp/lazada-7c2120e9-posterror.json`. 최초진행률/실행기기록3문서는독립검사후ba0d65b7cfc0a08b2ffb052c15ebf12d034c156e로두integration에push. 새13:32추록은그이후변경이다.

## 13:55 KST Lazada 공식 문의58021 접수·후속 채널 전환

- 공식 문서108260(2026-03-05판)은client_id/redirect_uri/response_type=code필수이며현행exact요청은충족한다. short_code/seller_id/account_platform쿼리추가요구없음. CB도MY단일인가선택가능. 공지1989는상업앱마켓구독변경이며이번오류를구독미비로단정하지않는다.
- ABA whitelist의SellerID는numericID가아닌SellerCentershortcode, 이메일/비밀번호는별도필드. CB는China또는6국가모두등록지침. MY사업자화면은CORPORATE/SouthKorea이나플랫폼내부CB분류는확정하지않았다. 현행목록5국가/Indonesia없음은사실이나그누락을오류원인으로단정하지않는다.
- 부모가Commerce Add Seller모달을열어Country선택지와China선택후SellerID/Email/Password필드유지를읽었다. 필드값/비밀번호를입력하거나Submit하지않고Cancel로닫았다. 허용목록/다른앱/계정/키변경0.
- 공식Contact us에서날짜2026-08-01~09-06을실제달력으로선택·Search하여NoData확인. AI Assistant의Ticket issues공식링크가새CreateTicket폼을열었다. API Error유형은실제로없던requestID필수여서사용하지않고API Consulting/others로분류했다. 분류는cross-border상담이지만본문에내부계정분류미확정·확인요청을명시했다.
- **티켓58021 접수성공/Pending**, 제목`OAuth whitelist mismatch: app 137451 / MY4NNISR2D`, 상세화면표시접수2026-09-06 13:54:57. 목록은06:54:57로표시되어시각표시차이를그대로보존하며타임존을추측하지않는다. 앱137451·실제seller300872000183/shortcode·오류UTC04:32:41·MY기존허용목록일치·계정분류확인요청을보냈다. 본문전체의공백정규화일치확인후OK1회,Create Ticket Success와새58021행,View상세의영속본문을확인했다.
- 비밀/토큰/인가코드/state/고객원문/사업자서류/주소/전화번호첨부0. 기존연결해제/키초기화금지를본문에명시했다. 영문본문contenteditable fill이값검증오류를냈지만실제본문은완전입력되어snapshot+innerText대조후재입력하지않았다.
- 새7c2120e9전용실행기도NO_RETRY종료·completed로그없음. 새교환/키v6완료로계산하지않는다. Lazada는외부지원응답대기,스마트스토어/쿠팡의외부승인v2게시조건을각1개읽기전용워커로병렬검토시작. 부모만실제운영변경을소유한다.
- 증거:`tmp/lazada-ticket-58021-created.png`, `tmp/lazada-ticket-58021-details.png`. 티켓상세는SPA여서page.url의createTicket=true를직접상세링크로주장하지않는다.

## 14:10 KST 게시 게이트 실제 차단 원인 확정

- 스마트스토어·쿠팡 병렬 검토 결과, 두 채널 모두 서버에 외부상세v2 정규화 경로가 존재한다. 스마트스토어는 `originProduct.name`/`detailContent` 바인딩과 `ko-KR`, 쿠팡은 제목100자와 `items[].contents` 바인딩이며 `detailAssetSource=external_generated`는 서버 쓰기 시에만 결속된다. 워크벤치UI의 `재제작 필요`·이미지0/8 표시는 legacy Studio 품질/`generatedImages` 기준이라 외부승인과 별개다. 이를 이유로 Studio를 성공으로 바꾸거나 우회하지 않는다.
- 한채널 강제 개방은 불가하다. `open_channel_gate`는 Qoo10 전용이고, 스마트스토어 닫힌게이트 예외는 **다른 상품** `ddccde35…` exact QA뿐이다. 대상 상품 ID를 그 자리에 넣지 않는다. 쿠팡 케이블클립 exact 경로도 이 상품과 무관하다.
- 실제 차단값을 DB에서 확인했다. 전역 `reconciliationRequired`는 `public.sellerpilot_301100_listing_gate_status_pre_publication_review`가 **listing.create/update/stop의 reconciliation_required만** 세며, 현재 정확히3건이다. 전체 미결 조정9건 중 나머지6건(Lazada orders.list/oauth.exchange2, Shopee orders.list/categories.suggest, Qoo10 publication.verify)은 이 집계에 포함되지 않는다.
- 게시 게이트를 막는3건:
  - qoo10 `listing.update` `089467c1-cadb-4d31-93a8-d5882c46d753`, listing `13858f41-78fd-463f-9390-e8f06e71e538`, 원격 적용 여부·후속 조회 미확정.
  - shopee `listing.create` `99d3b370-4ac6-4007-8bc3-b863c2badd17`, listing `76921966-50bb-4994-b4d4-1cd46c229043`, `SHOPEE_SHOP_IDENTITY_MISSING`.
  - elevenst `listing.create` `b9faa28e-a73f-4457-bb34-d643cf9a9a74`, listing `61b343f8-2e61-42a8-8a45-750f8b834edc`, 채널 작업 결과 저장 실패.
- 즉 **8채널 게시 개방의 선행조건은 상세 승인이 아니라 이3건의 정상 해소**다. 상태를 임의로 바꾸거나 삭제하지 않고 지원되는 해소 경로를 코드 근거로 조사 중이다. 경로가 없으면 없다고 기록한다.
- 증거: `tmp/gate-recon-current.json`, `tmp/gate-pre-review.json`, `tmp/gate-base.json`. 읽기 전용 트랜잭션만 사용했고 보호 SQL은 열지 않았다.

## 14:25 KST 확정: 전역 게이트 카운터의 설계 공백

- `public.sellerpilot_301100_listing_gate_status_pre_publication_review`는 `20260830095000`의 본문을 이름만 바꾼 함수이며, listing.create/update/stop의 `reconciliation_required`를 **예외 없이 원시 집계**한다. 이후 이 본문을 교체한 마이그레이션은 없다.
- 반면 exact 복구 설계는 **원본 source 접수기록을 영구히 `reconciliation_required`로 보존**하는 것을 계약으로 삼는다. Qoo10 `20260831056700` 주석이 이를 명시하고, 11번가 `20260905012000`은 `b9faa28e`가 계속 recon이어야 bind가 성립하며 트리거로 update/delete를 막는다.
- 예외 헬퍼(`qoo10_exact_s1_source_reconciliation_resolved`, `temu_safe_test_source_reconciliation_resolved`)는 라이브 래퍼의 **채널 한정 `qoo10ReconciliationRequired`와 개방 SQL**에만 needle 패치됐고, 전역 301100 본문에는 적용되지 않았다.
- 결과적으로 **전역 8채널 게이트는 강제 상태변경·삭제 없이는 영구히 열 수 없다.** 세 건 각각도 다음과 같다.
  - qoo10 `089467c1`: 정상 경로는 `qoo10-shipping-s1-release`의 `complete_reverify`이지만 대상은 activation `e09ab646`이며 source job을 닫지 않는다.
  - shopee `99d3b370`: 해소 헬퍼도 종료 경로도 없다. 현재 코드의 `acceptSignedRequestBinding: true`는 새 작업 예방일 뿐 기존 terminal 행을 닫지 않는다.
  - elevenst `b9faa28e`: GET-only bind는 listing 투영만 하고 job은 불변이며, 해소 헬퍼가 없다.
- **판단:** 이는 외부 채널 차단이 아니라 우리 쪽 게이트 카운터의 설계 공백이다. 올바른 해결은 강제 상태 변경이나 게이트 우회가 아니라, 기존 불변 증거 기반 예외 계약을 전역 카운터에도 **동일 기준으로** 적용하고, shopee/elevenst에 대해서는 증거 기반 해소 술어를 새로 정의하는 것이다. 증거 없는 일괄 제외·건수 하향은 하지 않는다.
- 이 수정은 새 검토 SQL과 사후검증이 필요하므로 이번 턴에서 임의 적용하지 않았다. 상태 변경·삭제·게이트 개방·건수 조작 **0**.

## 14:40 KST 차단 3건의 실제 증거와 수정 착수

운영 DB 읽기 전용 조회로 세 건의 실제 상태를 확인했다. 상태 변경·삭제 없음.

| 작업 | 채널·유형 | provider 변경 시작 | 응답 | 연결 listing 현재 상태 |
|---|---|---|---|---|
| `99d3b370` | shopee listing.create | **없음(NULL)** | 없음 | `76921966` remote_id **NULL**, failed, `SHOPEE_SHOP_IDENTITY_MISSING` |
| `b9faa28e` | elevenst listing.create | 2026-09-04T13:59:21Z | 없음 | `61b343f8` remote **9598600918**, SKU `AUTO-780720401E2D4E4EA45F`, failed |
| `089467c1` | qoo10 listing.update | 2026-09-04T14:38:01Z | remoteId **1217536689** | `13858f41` **published / live / S2** |

- **쇼피**: provider 호출 자체가 시작되지 않았고 원격 식별값도 없다. 즉 원격에 고아 상품이 생겼을 수 없는 실패다.
- **11번가**: 기존 GET-only bind로 원격 `9598600918`이 이미 확인·결속됐다. 원격 결과는 미상이 아니다.
- **Qoo10**: 기존 증거 헬퍼 `sellerpilot_private.qoo10_exact_s1_source_reconciliation_resolved('089467c1…')`가 **현재 `true`** 를 반환한다. 이미 해소로 판정되는데도 전역 카운터가 그 헬퍼를 참조하지 않아 계속 센다.
- 따라서 전역 카운터가 3을 유지하는 것은 실제 미해소 위험이 아니라 **집계 계약의 공백**이다. 그럼에도 건수를 임의로 낮추거나 job 상태·불변 트리거를 건드리지 않는다.
- 수정 방향: 301100 본문·복구 계약·job 상태를 그대로 두고, 라이브 래퍼의 전역 `reconciliationRequired`에만 기존 qoo10/temu 술어와 신규 증거 술어(쇼피=provider 미시작·원격 식별값 없음, 11번가=실제 bind 결속 확인)를 동일 기준으로 적용한다. 개방 경로에도 같은 술어 집합을 일관 적용한다. 증거 없는 일괄 제외는 만들지 않는다.
- 현재 새 migration 초안과 정적 검증 테스트를 작성 중이며, 이번 기록 시점에는 **DB 적용·커밋 없음**.
- 증거: `tmp/gate-jobs-evidence.json`, `tmp/gate-listing-evidence.json`, `tmp/qoo10-helper-state.json`.

## 채널별 현재 상태 요약 (14:40 기준)

- **Lazada**: 공식 인가가 `seller short code invalid` 로 거절. 실제 허용 목록·판매자 코드는 일치하므로 우리 요청값 문제가 아니라 플랫폼 매핑/분류 문제로 보고 티켓 58021 접수. **외부 응답 대기.**
- **Shopee**: 전용 세션 준비·pulse는 정상. 공식 인가 화면이 별도 로그인 비밀번호 재확인을 요구해 중단. 기존 8개 숍 승인은 `Using`으로 유지. **사용자 인증 한 단계 필요.**
- **Qoo10**: 기존 상품 live/S2 확인. 새 상세 게시는 게이트 해소 후.
- **쿠팡·스마트스토어**: 키·읽기 정상, 외부상세 v2 정규화 경로 존재. 게시는 게이트 해소 후.
- **11번가**: 원격 상품 결속 확인. 유료 배송 공식 계약 미확보는 별도 차단으로 유지.
- **eBay**: USD 가격·정확한 정책 handoff 미확정.
- **Temu**: 공식 심사 응답 대기.
- **어느 채널도 새 상세 게시 완료 아님(0/8).**

- 14:45: 수정안 작성용 하위 작업자가 외부 AI 사용량 한도(약273분 대기)로 중단됐다. 재시도하지 않았고 초안 파일은 생성되지 않았다(`20260906070000_*`, 신규 테스트 모두 없음). 저장소에는 문서 변경만 남아 있다. 이 한도는 SellerPilot 운영 계정이 아니라 보조 작업자 모델 쿼터 문제이며, 게이트 수정은 부모 세션에서 직접 이어서 작성한다.

## 15:05 KST 게이트 카운터 수정 작성·적용 완료

- 새 migration `supabase/migrations/20260906070000_evidence_based_global_publication_gate_counters.sql`와 정적 검증 `tests/global-publication-gate-evidence-counters.test.mjs`(9/9)를 작성했다. 커밋 `7edf53d`.
- 방식: 운영에 적용된 두 함수의 **실제 원문을 그대로 읽어** 앵커 치환으로만 수정했다. 301100 본문, 접수기록, job 상태, 불변 트리거, 응답 스키마 키 집합은 변경하지 않았다.
  - 상태 함수: 전역 `reconciliationRequired`만 술어 4개로 재계산해 덧붙임(뒤 값이 우선).
  - 개방 함수: 기존 qoo10·temu 예외에 신규 술어 2개를 같은 조건절로 추가. queued drain 요구와 예외 문구는 유지.
- 신규 private 술어 2개는 각각 자기 증거를 요구하며 전 롤 revoke·definer·빈 search_path다.
  - `unstarted_listing_create_reconciliation_resolved`: provider 변경 미시작 + 응답 없음 + write key 없음 + listing 원격 식별값 없음.
  - `elevenst_bound_listing_create_reconciliation_resolved`: 관측 `bound_at` 존재 + prodmarket 수락 + 코드 일치 + listing의 remote_id/SKU/상품 일치.
- 첫 적용 시도는 **400 구문 오류로 거절**됐다. `pg_get_functiondef` 출력에 종결 세미콜론이 없어서 두 번째 CREATE 앞에서 파서가 실패했다. 사후 조회로 새 술어0개·상태함수84줄 그대로임을 확인해 **부분 적용이 없음**을 검증한 뒤, 원문 의미는 그대로 두고 종결자만 보정해 다시 1회 적용했다. 오류 원인을 추측으로 넘기거나 가드를 낮추지 않았다.
- 적용: 201, 검토 원문 SHA `40c7b12ede1883fc443f0ca24a8751c332ed0f43acc4d7d17ddb8561caa42f7e`, 1회 시도 영수증 `tmp/apply-gate-counter-attempt.json`(600).
- **독립 사후검증(읽기 전용):** 새 술어 2개 존재, 보존된 접수기록 여전히 **3건 그대로**, 전역 게이트 상태 `reconciliationRequired 0`, `queuedOrRunning 0`, `orphanPendingReviews 0`, 어댑터 `8/8`, 재조회기 true.
- 접수기록을 지우거나 상태를 바꾸지 않고 카운터만 증거 기준으로 정확해졌다. 게이트는 여전히 **닫힘**이며 자동 게시는 없다.

### 다음 차단: 게시 기준 SHA 불일치

- 현재 `attestedRelease`는 옛 `c3d1bf117d61dfd5877d96af31c5bc8976a3c2bf`, `activeRuntimeRelease`는 운영 중인 `738934ecf5b90582a75b95355cf42d8cf7214d53`이며 `runtimeReleaseMatches=false`다.
- 즉 개방 전에 현재 배포 SHA 기준으로 8개 어댑터와 재조회기를 **다시 확인·기록**해야 한다. 이는 각 채널의 원격 상태 계약을 실제로 검증한 뒤 기록하는 절차이며, 화면에서 버튼만 눌러 통과시키지 않는다.

## 15:35 KST 현재 SHA 어댑터 기록과 8채널 게이트 개방

- 어댑터 코드는 `738934e`와 HEAD가 동일함을 확인한 뒤, 게시·리스팅 테스트 391/393 통과와 운영 11820 마커 실측을 근거로 8개 채널 어댑터와 재조회기를 **두 단계 확인 기록**했다. 상품 게시는 자동 실행되지 않았다.
- 기록 후 UI: 어댑터 8/8 현재 SHA 일치, 재조회기 현재 SHA 일치, 런타임 현재 SHA 일치, 조정 0, 게시 작업 0, 고아 0, **게이트 개방 조건 충족**.
- 독립 DB 읽기: `open=true`, `state=open`, `effectiveOpen=true`, `openedChannel=null`(전역 8채널), `attestedRelease=activeRuntimeRelease=738934e`, `runtimeReleaseMatches=true`, `reconciliationRequired=0`.
- 확인 대화상자의 경고대로 게이트를 열면 이후 승인된 등록·수정·중지 요청이 외부로 전달될 수 있다. 자동 게시 아님. 채널별 실제 게시·readback은 별도다.
- 아직 새 상세 원격 게시 0/8. 다음 단계는 승인된 외부 v2를 채널별 조건이 맞는 곳부터 정규화·게시·원격 재조회하는 것이다. Studio 실패 리비전은 그대로 failed다.

## 15:25 KST 현재 배포 기준 어댑터 검증

- 배포된 `738934e`와 현재 HEAD를 비교해 **게시 어댑터·재조회 관련 소스가 하나도 다르지 않음**을 확인했다. 변경은 UI 문구2, 문서6, 테스트9, 새 SQL1뿐이다. 즉 로컬에서 검증한 어댑터 코드가 운영 코드와 동일하다.
- 게시·리스팅·재조회 관련 테스트 파일49개를 실제 실행: **393개 중 391 통과, 2 실패**.
- 실패 2건은 모두 `tests/provider-listing-lineage-rebind.test.mjs`의 로컬 전체 재생이며 원인은 동일하다: `11820 Shopee in-list marker count=0`. 이는 `20260904211500_allow_local_shopee_category_and_diagnostic_claims.sql`의 needle 치환이 PGlite의 `pg_get_functiondef` 출력 형식에서 문자열을 찾지 못해 발생한다. 어댑터 계약 위반이 아니라 로컬 재생 한계이며, 이전부터 알려진 미해결 항목이다.
- 적용된 마이그레이션 원문은 이미 운영 이력에 있으므로 다시 쓰지 않는다. 대신 그 의도가 **운영에서 실제로 달성됐는지 직접 조회**했다: 패치된 `or (false and j.channel = 'shopee' ...` 존재 **true**, 패치 전 마커 **false**, `diagnostic.test` 포함 **true**. 즉 운영 정의는 정확히 의도대로다.
- 따라서 어댑터 검증 상태는 다음과 같이 기록한다. 로컬 계약 테스트391 통과 + 운영 코드 동일성 + 해당 1개 항목의 운영 실물 확인. 로컬 재생 2건은 **여전히 미해결 환경 제약**으로 남기며 통과로 세지 않는다.
- 증거: `tmp/adapter-attestation-suite.log`, `tmp/shopee-marker-live.json`.
