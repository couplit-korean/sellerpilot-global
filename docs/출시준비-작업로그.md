# SellerPilot 출시 준비 작업 로그

기준: 2026-09-04 06:00 KST  
이 파일은 **이번 세션에서 한 일과 남은 막힘**을 고치기 쉽게 적어 둔다.  
운영 사실의 단일 원장은 여전히 [현재상태.md](./현재상태.md)다. 계정 혼선은 [계정연결.md](./계정연결.md).

**8/8 채널 연결 완료가 아니다. 실주문 E2E 완료가 아니다.**

---

## 1. 한 줄 상태

| 항목 | 값 | 고칠 때 |
|---|---|---|
| 목표 | 운영 UI를 모바일/태블릿/PC에서 쓸 수 있게, 배포 SHA와 게시 게이트를 맞춘 뒤 재시작 | 이 표만 먼저 고친다 |
| UI 반응형 | `f811491` 이후 추가 핫픽스(타임아웃 재시도, iOS 입력 16px, 게이트 조회 분리) | 커밋 SHA는 푸시 후 여기 적는다 |
| Production URL | https://sellerpilot-global.vercel.app | 바꾸지 말 것 |
| 작업 브랜치 | `integration-aside` | |
| Production 브랜치 | `couplit` remote의 `main` | docs만 올리면 RELEASE_SHA 충돌 |
| 직전 Production SHA | `f81149124ce079dba9a06c83f24c58a2d1417a5a` | 40자 전체. 핫픽스 푸시 후 이 칸을 새 HEAD로 바꾼다 |
| 유료 Static IP | 구매 안 함 | 제안 금지 |
| Lazada | 외부 잠금. 기록만. Deauthorize 금지 | |

> SHA 칸을 고칠 때: `git rev-parse HEAD` 결과를 그대로 넣고, Vercel Production env `SELLERPILOT_RELEASE_SHA`도 같은 40자로 맞춘 뒤 **그 SHA로 새로 배포**한다. 옛 배포를 env만 바꿔 재배포하면 conflict가 난다.

---

## 2. 출시 가능 범위 / 불가능한 범위

### 이 작업으로 출시 가능한 것
- 관리자 로그인 후 운영 화면을 iPhone·Android·태블릿·PC에서 읽기
- 하단 네비, 상단 제목, 채널 마크, 표, 검수 체크리스트가 뷰포트 밖으로 크게 깨지지 않음
- 배포 SHA와 운영 일정 재시작 절차를 문서화

### 이 작업으로 출시 불가능한 것 (외부 잠금)
- 8채널 실주문·실문의 E2E
- 스마트스토어 고정 3-IP vs Vercel 회전 IP
- 쿠팡 최종 승인 대기
- Temu Compliance Reviewing (재제출 금지)
- Lazada short-code 거절 + fenced reconciliation (끊고 재연결 금지)

---

## 3. 체크리스트

### 코드·배포
- [x] 반응형 CSS 감사 후 수정 (모바일 375 / 태블릿 768 / PC 1440)
- [x] `customer-experience.css`는 의도적 미import. 수정하지 않음
- [x] CSS 커밋 `f811491` → `origin/integration-aside` + `couplit/main`
- [x] Production env `SELLERPILOT_RELEASE_SHA` = `f81149124ce079dba9a06c83f24c58a2d1417a5a`
- [x] Production Ready. 화면에서 SHA `f811491…417a5a` 확인
- [x] 운영 일정 재검증·재시작 실행. AI Gateway 실호출 통과
- [ ] 운영 스냅샷 30초 타임아웃 후 자동 재시도 (이번 핫픽스)
- [ ] 게시 게이트 조회를 AI 작업 조회와 분리 (이번 핫픽스)
- [ ] iOS 입력 확대 방지 `font-size: 16px` (이번 핫픽스)
- [ ] 위 핫픽스 커밋·푸시·RELEASE_SHA 맞춤·재배포·재시작
- [ ] 새 SHA에서 8 어댑터 + 재조회기 확인 기록 → 게시 게이트 열기
- [ ] 375/768 라이브 스크린샷 (Aside REPL은 viewport 변경 API 없음. 배포 후 별도 검증)

### 채널 (원장, 이번 세션에서 닫지 않음)
- [x] Qoo10 키·읽기 정상. 실주문 0
- [x] Shopee 키·읽기 정상. IP 화이트리스트 유지
- [ ] Lazada 외부 잠금
- [ ] 쿠팡 최종 승인 대기
- [x] 11번가 키 있음. 04:29 읽기 정상. 키 재수집 금지
- [ ] 스마트스토어 3-IP 캡
- [x] eBay 키·읽기 정상. 실주문 0
- [ ] Temu Vault 없음 / Reviewing / 재제출 금지

---

## 4. 반응형에서 고친 것

파일 우선순위(layout import 순): `globals.css` → `operations-system.css` → `commerce-ux-refactor.css` → `style-learning-center.css` → **`mobile-optimization.css`(승자)** → `interaction-layers.css` → `product-publish-workbench.css`

| 증상 | 원인 | 수정 위치 |
|---|---|---|
| 상단 제목 잘림 | 412px에서 max-width만 풀고 ellipsis 유지 | `mobile-optimization.css` `.topbar-title h1` |
| 채널 마크가 제목 위로 겹침 | grid 1열이 38px 고정, 마크는 min-width 54px | `operations-system.css` `min-content` |
| 연결표 세로글자 | first-column nowrap 없음 | `operations-system.css` `.capability-table th:first-child` |
| Gate 목록 잘림 | minmax 200px/82vw | `mobile-optimization.css` 160px/70vw |
| 자격증명 푸터 버튼 압착 | 3열 | `operations-system.css` 480px에서 1열 |
| CLI 카드 태블릿 잘림 | 720px만 스택 | 900px 스택 추가 |
| 문의함 열기 잘림 | 마지막 열 auto + nowrap | `commerce-ux-refactor.css` |
| CS 검증 2열 겹침 | 720px override가 768에서 짐 | 820px + 부모 prefix |
| 하단 네비 라벨 | 344px에만 ellipsis | 720px로 확대 |
| 데이터 배지 잘림 | 76px 박스에 텍스트 재표시 | 390px 아이콘만 |
| 주문/마진 표 태블릿 스크롤 | 카드화가 720px만 | 721–900px 카드화 |
| 검수 게이트/체크리스트 | min-width 650px | 2열 카드, min-width 0 |
| 환율 겹침 | title 164px + nowrap | 210px, small wrap |
| 브리핑 숫자 잘림 | 25px + overflow hidden | 480px에서 19px |
| 경쟁가 empty 3장 | 1열 스택 | n+2 hide |
| 등록 화면 하단 네비 가림 | padding 42px가 이김 | 86px `!important` |
| 서비스 레일 지저분 | 2열 grid `!important` 테두리 | 해당 블록 삭제 |
| 사용량 히어로 잘림 | 480px만 스택 | 900px로 올림 |
| 운영 조회 30초 후 멈춤 | 재시도가 5분 interval뿐 | `use-operations-snapshot.ts` 4초×3회 |
| 게이트 SHA가 늦게/실패로 보임 | listing GET이 AI jobs와 Promise.all | `ai-cli-runtime-card.tsx` 분리 |
| iOS 로그인 확대 | input 12px | 720px 이하 16px |

고아 파일: `app/customer-experience.css`. `interaction-layers.css` 주석대로 import하지 않는다.

---

## 5. 게시 게이트를 다시 여는 절차

관리자 세션 Bearer로만. SHA를 화면에 입력하지 않는다.

1. Vercel Production `SELLERPILOT_RELEASE_SHA` = 라이브 커밋 40자
2. **그 SHA의 새 배포**가 Ready인지 확인. 옛 배포 Redeploy 금지
3. 채널 연결 · 상태 → 채널 연결 설정 → `운영 일정 재검증·재시작`
4. SHA가 보이면 채널 8개 `확인 기록` + `재조회기 확인 기록`
5. `게시 게이트 열기` (조건: attested === activeRuntime === current, queued 0, reconciliation_required 0)
6. 화면이 **8개 채널 게시 허용**인지 확인

채널 키: `qoo10` `shopee` `lazada` `coupang` `elevenst` `smartstore` `ebay` `temu`

Lazada fenced `reconciliation_required` 행은 raw SQL로 지우지 않는다. 게이트가 안 열리면 이 로그의 “남은 막힘”에 적고 중단한다.

---

## 6. 배포 절차 (반복용)

```text
1. git status   # 다른 에이전트 변경 되돌리지 말 것
2. CSS/코드만 스테이징. 아래 SQL은 넣지 말 것
   supabase/migrations/20260903150000_unblock_shopee_second_oauth_deadlock.sql
3. git commit
4. NEW=$(git rev-parse HEAD)
5. Vercel Production env SELLERPILOT_RELEASE_SHA=$NEW 저장. Redeploy 배너는 닫기
6. git push origin integration-aside
7. git push couplit integration-aside:main
8. Deployments에서 Production Ready + 커밋 메시지 확인
9. 운영 일정 재검증·재시작
10. 게시 게이트 절차(5장)
```

docs만 있는 커밋은 `couplit/main`에 올리지 않는다. `origin/integration-aside`만.

---

## 7. 브라우저 프로필

| 작업 | Chrome 프로필 | 계정 |
|---|---|---|
| 쇼핑/판매자/개발자센터 | CHANGHEE = Profile 2 | k931103@gmail.com |
| Vercel / Supabase | JEONGHUN = Profile 4 | couplit.official@gmail.com |
| Aside 기본 | Default | aicp0101@gmail.com 쓰지 말 것 |

SellerPilot 로그인: Aside Password Manager `SellerPilot | 멀티채널 커머스 운영센터` / `sample` → `sample@couplit-official.test`

---

## 8. 남은 막힘 (우선순위)

1. **게시 게이트 0/8** — 근본원인: `authenticateAdminRequest`가 `sellerpilot_is_admin` 타임아웃을 403으로 처리. 수정: 타임아웃은 503, listing GET 제한 25초, UI 1회 재시도. 이 커밋 배포 후 5장 실행
2. **운영 스냅샷 30초 타임아웃** — 콜드스타트. 재시도 핫픽스로 완화. 원인 API가 30초 넘게 걸리면 서버 쪽을 따로 본다
3. **모바일 375/768 라이브 샷** — Aside page에 setViewportSize 없음. 배포 후 실제 폰 또는 디바이스 모드
4. **채널 외부 잠금** — 2장. UI 출시와 별개

---

## 9. 검증 메모 (2026-09-04 05:50–06:00)

- 로그인: 기존 세션은 12초 권한확인 타임아웃. 쿠키 지우고 sample 재로그인 성공
- 데스크톱 1440: 제목 안 잘림, 사이드바 `큐텐`/`쇼피` 마크 안 겹침, 환율 캡션과 숫자 분리
- 연결 설정: 키 7/8 (Temu 없음). Qoo10/Shopee/11st/eBay 최근 검사 정상. Lazada/쿠팡/스마트스토어 검사 실패 메시지(고정 IP 워커)
- 게시 게이트: SHA `f81149124ce079dba9a06c83f24c58a2d1417a5a` 한 번 표시됨. 탭 재진입 시 다른 API에 묶여 `확인 불가`로 후퇴 → 이번 핫픽스로 분리
- 운영 배너: `운영 데이터 조회가 30초를 초과했습니다`

---

## 10. 관련 파일

```text
app/mobile-optimization.css
app/operations-system.css
app/commerce-ux-refactor.css
app/globals.css
app/platform-usage-page.module.css
app/use-operations-snapshot.ts
app/ai-cli-runtime-card.tsx
tests/mobile-registration-client-fences.test.mjs
docs/출시준비-작업로그.md   # 이 파일
docs/현재상태.md            # 원장. SHA 표는 배포 후 맞춰 고친다
```

수정 규칙: 한 이슈 = 이 파일 표 한 줄. 장문 금지. 비밀값·토큰 적지 말 것.
