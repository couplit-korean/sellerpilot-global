# Codex 게시 준비 검토 — 2026-09-07

## 범위와 소유권

- 작업 저장소 `/Users/kimchangheemac/dev/sellerpilot`, 검토 시작 HEAD `1b91383`.
- 총괄에게서 받은 범위: 스마트스토어 단위가격 입력·preflight·provider·승인 계약과 실행 판본 점검. 이후 발견한 UI 결함의 최소 수정을 추가로 배정받음.
- 수정 소유: `app/product-publish-workbench.tsx`, `lib/channels/listing-preflight.ts`의 단위가격 관련 부분, 새 전용 회귀 테스트. 기존 Smartstore preflight 테스트 fixture 1줄도 새 계약에 맞게 보완.
- 운영 DB·브라우저·배포·워커 재시작·Git 커밋은 수행하지 않았다. 공통 원장과 보호 SQL은 변경하지 않았다. 보호 SQL 내용 접근·테스트 없음.
- Next 로컬 `node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`를 읽고 기존 Client Component 상태/이벤트 경계 안에서 수정.

## 읽기 검토 결과

1. `4159f8c`에는 공식 카테고리를 조회해 단위가격을 검증하는 provider guard가 있다. create와 update 모두 이미지 업로드 전에 검증한다. 총용량·기준량·단위는 추정하거나 기본값을 넣지 않는다.
2. 기존 화면은 공식 payload textarea 수정은 가능했지만 단위가격 전용 입력과 preflight가 없었다. 따라서 화면에서 '모든 입력값 준비'가 보여도 런타임에서 막힐 수 있었다.
3. 가격·수량·포장 등 공통 값을 바꾸면 `buildSynchronizedDraftMap`이 payload를 다시 만들고 기존 `manualPath` 항목만 보존했다. 단위가격은 해당 목록에 없어 JSON으로 보완한 값이 사라지는 실제 공백이 있었다.
4. 기록상 이 상품의 확인값은 `unitPriceYn=true`, `totalCapacityValue=315`, `unitCapacity=10`, `indicationUnit=g`, 판매가 3190원이다. 이번 코드는 이를 상품 기본값으로 하드코딩하지 않는다. 법령·실상품 값은 기존 확인 기록을 인계받았으며 이번 읽기 검토에서 마켓을 재조회하지 않았다.

## 이번 수정

- 단위가격 표시 여부(명시적 선택), 판매단위 총용량, 단위가격 기준량, 용량 단위를 화면에 노출.
- 화면 입력은 boolean/number/string으로 각각 보존하며 문자열 'true'·'315'를 JSON 직접 편집으로 넣은 경우에는 사전 검사에서 차단.
- 총용량 범위·소수 정밀도, 기준량 정수 범위, 공식 단위를 검증. UI에서 '비대상'을 선택하더라도 실제 필수 카테고리는 기존 provider 조회에서 계속 거절한다.
- 비대상 선택 시 이미 입력된 수량·단위를 자동 삭제하지 않으며 모순된 값은 차단. 사용자가 각 입력을 비우면 그 속성만 제거한다.
- 공통 필드 동기화에서는 unitCapacity 전체를 정확한 JSON 타입 그대로 보존. 잘못된 타입·null·추가 키도 임의로 정상화하지 않아 문제를 숨기지 않는다.
- 일반 배열 setter, 쿠팡 배송·고시 로직, 승인/요청 해시 함수, provider guard는 수정하지 않았다.

## 승인·요청·provider 연결

- UI draft의 `body.originProduct.detailAttribute.unitCapacity`가 `channelArguments`와 mutationContract에 포함된다.
- 클라이언트 fingerprint와 서버 canonical request fingerprint가 전체 arguments를 결속한다. 기존 작업 payload를 사후에 몰래 바꾸는 수정이 아니다.
- 외부 상세 binder는 승인된 이름·본문·이미지 계보를 적용하면서 unitCapacity를 지우지 않는다. 승인 상세 snapshot은 product.updated_at와 approved_product_updated_at, 상세 버전, 원본·픽셀 해시를 계속 검증한다.
- 따라서 이미 발생한 승인 시각 불일치나 기존 `reconciliation_required`는 이번 UI 수정으로 해제되지 않는다. 총괄의 정상 재조정·정확한 재조회가 선행돼야 한다.
- 실제 전송 전에 기존 provider guard가 공식 정확 카테고리의 UNIT_PRICE 여부를 읽고 입력값을 검사한다. UI 준비 상태는 마켓 등록 성공을 뜻하지 않는다.

## 검증 증거

사용 Node: `/Users/kimchangheemac/.nvm/versions/node/v22.23.2/bin/node`.

- 새 `tests/smartstore-capacity-workbench.test.ts`: **7/7 통과**. 실제 buildSynchronizedDraftMap 가격·재고·포장 변경 경로, typed 입력, 잘못된 JSON 보존, 미입력 기본값 금지, 비대상 모순 및 provider 필수 카테고리 거절 검증.
- 변경 영향: `tests/listing-preflight.test.ts` 10개와 `tests/listing-shipping.test.ts` 9개 중 최초 18개 통과. Smartstore 준비완료 fixture가 새 표시 여부를 누락해 실패한 1개는 명시적 비대상 fixture로 수정하고 그 테스트만 다시 실행해 **1/1 통과**.
- 초기 새 테스트의 자산 비교는 JSON 직렬화에서 사라지는 undefined 속성 때문에 실패했다. 직렬화된 실제 요청 계약끼리 비교하도록 테스트를 바로잡았으며 제품 코드를 우회 수정하지 않았다.
- `git diff --check` 변경 파일 통과.
- 이미 통과한 4159f8c 전체빌드/과거 provider 테스트는 반복하지 않았다. 전체 타입 검사·통합 빌드·UI 실제 렌더 확인은 총괄 통합 단계에 남긴다.

## 실행 판본·배포 차이

- 이번 ps 읽기에서 PID `53979`는 `2026-09-06 22:08:14` 시작된 `node --import tsx scripts/ai-cli-worker.mjs --gateway-only --no-scheduler`로 계속 실행 중이었다.
- 해당 프로세스가 시작 후 생긴 `4159f8c` guard나 이번 변경을 자동 재로딩했다고 볼 증거가 없다. 총괄이 활성 job/lease를 확인한 뒤 정상 중지·새 판본 시작·health 검증을 맡는다.
- 인계 기록의 Vercel 후보는 `dpl_47SVboRyVgsNhFbEb8vwxibvRdEY`, source/env/canary `4159f8c`, READY이며 운영 도메인 미승격이다. 이번 담당은 Vercel을 재조회하지 않았다.
- `4159f8c..1b91383` 코드 차이는 기록·DB 마이그레이션·DB 테스트이며 해당 구간에서 unitCapacity UI 보완은 없었다. 이번 추가 UI 수정은 새 통합/배포가 필요하다.

## 총괄에게 인계할 다음 행동

1. 이미 수동 등록·판매중인 Smartstore SKU는 다시 create하지 않는다. 과거 실패 job/attempt/listing을 불변으로 두고 별도 수동 채택 receipt만 검증·적용한다.
2. 이번 변경을 통합 검사 후 운영 릴리스에 포함하고 실제 작업을 소비하는 워커 판본을 일치시킨다.
3. Smartstore 원상품번호 `13688607602`와 공개번호 `13749310594`, 승인 자산과 request/response hash를 receipt에 결속하고 운영 readback을 남긴다.
4. 두 번째 채널인 쿠팡은 exact SKU/GTIN 0건 확인과 폼 준비를 마쳤으므로, 사용자 실행시점 승인 후 `상품등록` 1회와 원격 상품 readback으로 마감한다.

상태: **코드 수정·통합 검증 완료 / Smartstore 실제 등록·원격 확인 완료 / 수동 채택 운영 적용·쿠팡 최종 등록 미실행**.

## 통합 직전 리뷰 후 P2 두 건 추가 수정

- 발견·재현: 쿠팡 `facts.weightAttribute=315g`를 JSON으로 보완해도 가격 변경 시 `buildSynchronizedDraftMap`에서 사라졌다. 스마트스토어 unitCapacity가 배열·문자열·숫자면 새 typed 입력 이벤트가 setter 예외를 React updater 안으로 전달했다.
- 쿠팡 확정 순중량을 정식 수동 입력 `facts.weightAttribute`로 노출하고 공통 동기화에서 해당 속성의 원래 JSON 타입·값을 그대로 보존했다. 가격·재고·포장 중량을 변경해도 315g은 그대로이며, 미입력 값은 배송 중량으로 생성하지 않는다.
- 스마트스토어 typed 편집은 React updater 전에 성공/실패 결과를 받는다. 잘못된 컨테이너이면 기존 draft를 그대로 두고 '채널 공식 payload 최종 검토'에서 unitCapacity를 객체로 고치라는 안내를 표시한다. 실패 값을 자동으로 빈 객체로 바꾸지 않는다.
- 추가 테스트: 잘못된 컨테이너 5종×4필드의 비파괴 오류 처리, JSON 수정 후 정상 typed 입력 복구, 쿠팡 확정 중량의 가격·재고·포장 변경 보존과 raw 타입 보존.
- Node22 실행 결과: `smartstore-capacity-workbench` 9개 + `coupang-weight-workbench` 3개 + `listing-draft-value` 12개 + `listing-preflight` 10개 = **34/34 통과**. 위 초기7개/영향검사와 중복되는 테스트를 별도 성과로 합산하지 않는다.
- 전체 `tsc --noEmit --incremental false` exit0, 소유 소스·테스트 5개 scoped ESLint exit0, diff whitespace 검사 exit0.
- `provider-listing-runtime.ts`, 운영 DB·브라우저·배포·워커·Git 커밋은 이 후속 수정에서 변경하지 않았다.
- 현재 상태: **두 P2 수정·타입검사·국소 회귀 완료, 총괄 통합/배포 대기**.

## 통합 리뷰 후 P1 순중량 검증

- 명시한 `facts.weightAttribute`가 임의 문자열이어도 필수 쿠팡 중량으로 통과할 수 있던 문제를 수정했다.
- 값은 `양수 숫자+단위` 형식이어야 하고, 단위가 해당 중량 attribute의 공식 `usableUnits`에 정확히 들어 있을 때만 provider payload에 결속된다.
- `315g` 보존과 배송·포장 중량 자동 추론 금지는 유지했다. `banana`, `315`, `0g`, `-1kg`, 허용되지 않은 `315lb` 회귀를 grouped/single 메타 모두에 추가했다.
- Node22.23.2 관련 **32/32 통과**, `tsc`, scoped ESLint, whitespace 검사 통과. 운영 배포·브라우저·DB 쓰기는 하지 않았다.
