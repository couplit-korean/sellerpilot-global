# Aside 잔여 운영 실행 기록 — 2026-09-05

## 실행 기준과 확인 범위

- 시작 21:42 KST. 실제 작업 경로 `/Users/kimchangheemac/dev/sellerpilot`, `integration-aside`, 시작 HEAD `86a0d8ce069a5300a78c766b4139bfd468668f5c`.
- 시작 변경은 보호 대상 미추적 `supabase/migrations/20260903150000_unblock_shopee_second_oauth_deadlock.sql` 하나. 파일 수정·삭제·스테이징·커밋·운영 적용 없음.
- Aside 실제 세션 저장값: 총괄 및 하위 8개 모두 `openai-codex / gpt-6-astra / high / fastMode=false`. 22:02 KST 총괄 1 + 하위 8 동시 `running` 확인. 현재 계정 모델 목록에 `gpt-6-astra`가 있으며 별도 `gpt-6` 식별자로 보고하지 않는다.
- 5개 영역 담당을 유지하고 DB baseline·로그인 경쟁조건·11번가 공식 배송 계약 읽기 검토 3개를 추가했다. 외부 쓰기와 통합은 총괄 단독, 코드 파일 소유권은 영역별로 제한했다.
- 사이트별 기존 Aside 세션을 사용했다. 별도 Chrome 프로필 전환·앱 연결 해제 없음.
- 아래는 실제 실행 기록이다. 10시간 경과나 8채널 운영 완료를 주장하지 않는다.

## 실제 운영 변경: 재고 RPC 2개

**22:08:45 KST 신규 migration `20260905130000_reject_inventory_idempotency_conflicts.sql` 적용 및 재조회 완료.**

- 소스 코드 커밋: `526b15e`.
- SQL SHA-256: `740877fadd9678aafb0b5048a569b07dbb41b9f14b49255d85dac3b0f6b60967`.
- Supabase ref: `sqaoqucxakebqkiygdxb`.
- 적용 전 원본 `prosrc` MD5·인자/기본값·service_role 전용 ACL·SECURITY DEFINER·빈 search_path를 읽기 대조했다.
- 같은 멱등키의 수량/주문/채널 충돌, 콜론 결합키 충돌, 반품의 부분 참조, 과도한 참조 길이, 명시적 NULL 수량을 사전 거부한다.
- 기존 키 체계·원장 행을 바꾸지 않는다. 새 원장에 기존 상품 재고를 입고하거나 주문 처리를 연결한 변경이 아니다.
- 소스 해시 고정, 5초 lock/30초 statement 제한, 기존 동일 이력 부재 확인 후 한 트랜잭션으로 함수 패치와 해당 신규 이력 1건만 기록했다. HTTP 201을 완료 근거로 삼지 않고 다음 정의·권한·카탈로그 재조회를 수행했다.

| 함수 | 적용 전 MD5 | 적용 후 MD5 |
|---|---|---|
| `sellerpilot_inventory_reserve` | `99a6cf343eabafa12445c07055c3fd19` | `7b26b840009583fee8178d6199320c9f` |
| `sellerpilot_inventory_return_received` | `529ede0b37736f731b5b67f76d64c583` | `04c696516cb3b7cd2b790b6575722ded` |

전후 catalog 비교: **위 함수 2개 변경 + 신규 migration 이력 1건 추가만 확인**. 820함수/118관계 수 유지. anon/authenticated 실행 불가·service_role 실행 가능 유지. inventory items/ledger/reservations 각 0 유지.

새 이력 수는 **271**, 최종 version `20260905130000`. 이전 270개 이후 누락된 역사 이력을 복구했다는 뜻이 아니다.

## 코드 수정과 집중 검증

| 영역 | 수정 | 확인 | 운영 경계 |
|---|---|---|---|
| 상세 이미지 | 이미 배정된 역할이 fallback 역할 큐를 소모하지 않도록 수정 | manifest 7/7, 내부 혼합 256/256 조합 | 기존 degraded 결과를 고친 것은 아님 |
| CS | ticket 단위 대신 메시지 inboundKey 중복 제거, Lazada 구매자·판매자 정상 텍스트 대화 보존 | TS 집중 검사, DB 저장·중복·순서 검사 통과 | 실제 문의/답변 0. 비텍스트 보존은 미검증 |
| CS 교차검토 | 시각 미확정 판매자 이벤트가 수집시각으로 최신 답변이 되는 P1 재현·차단 | TS 27/27, 원장 2/2 | 시각 미확정 묶음은 적재 전 실패. 별도 원문 quarantine 저장은 미구현 |
| 주문/배송 | Lazada 잘못된 타입·누락·중복·100개 초과·불일치 상세와 수신 묶음 간 remote item 충돌 차단 | 집중 130/130, 격리 DB 저장→fulfillment 재조회→shipment-draft 확인 | 잘못된 상세는 표시/수량에도 사용 안 함. 서로 다른 수신 묶음 간 영속 소유권 검사는 미구현 |
| 재고 | 순수 원장과 서비스 RPC의 충돌 거부, 참조 완전성 보강 | 최종 DB 12/12, TS 원장 25/25 | RPC 2개만 운영 적용. 원장 운영 연결은 미완료 |
| 11번가 배송 | 서버 저장 배송 사실 결속, 브라우저 metadata 누락/0원 변조 차단, legacy content 분류 유지 | 집중 TS100/100·route7/7, 교차리뷰 재검증3/3. 3,000원 요청 provider0회 | 코드 `4c9253f`. 기존 상품 수정/create/binder/recon 재실행 없음 |
| DB 감사 | 고정 READ ONLY catalog SELECT, 함수·권한·관계·RLS·trigger·이력 지문 비교 | 9/9 | 비밀·업무행을 읽지 않으며 함수 설정도 원문 대신 hash 저장 |

CS/주문/manifest 통합 집중 **61/61**, 최종 11번가 부모 집중47/47. 담당별 검사와 겹치므로 수치를 합산하지 않는다. 전체 기존 TypeScript1,999개를 처음부터 반복한 수치가 아니다. 코드 커밋 `1f81cc2`, 재고 `526b15e`, DB 감사/fixture `2b6cd9b`, 11번가 최종 `4c9253f2e18b5267a17f4b730c79159993d10892`.

최종 코드의 **Vercel용 build exit0, 빌드 후 `tsc --noEmit` exit0**, 변경 파일 ESLint·diff-check 통과. 중간에 tsc와 Next 빌드를 병렬 실행해 `.next/types` 재생성과 읽기가 충돌한 검증 순서 오류가 있었다. 빌드 완료 후 순차 타입검사로 해결했으며 소스 타입오류로 보고하지 않는다.

## DB 선행 이력: 남은 실패를 숨기지 않음

- 기존 전체 MJS/DB 15실패 중 역사적 static-egress fixture에 후속 overlay가 섞여 존재하지 않는 policy를 참조하던 1건을 fixture 시점 경계로 고쳤다. 집중 1/1 통과. 과거 migration guard는 수정하지 않았다.
- 최초 로컬 DB 검사에서 보호 미추적 SQL을 디렉터리 열거가 자동 포함하는 것을 발견했다. **일회성 로컬 PGlite 재생에 포함된 경로가 있었으며**, 이후 전범위 두 검사에 입력 제외 helper를 적용했다. 원본 파일 및 운영 DB에는 해당 SQL을 적용하지 않았다. 입력 제외 검사는 실패 원인 migration `20260904211500`이 계속 포함되는 것도 확인한다.
- 현재 두 잔여 suite **22개 중 8통과/14실패**, skipped 0. 14개 모두 `20260904211500`의 `11820 Shopee in-list marker count=0`로 막힌다.
- `20260904232000` 후속 snapshot을 누락 선행 원본으로 바꾸거나 순서를 이동하지 않았다. 기존 SQL 수정·이력 위조·전체 과거 재적용 없음.
- `scripts/audit-db-schema-baseline.mjs`가 확보한 것은 현재 catalog 지문이다. **전체 DDL 복원 패키지나 새 PostgreSQL/Supabase 환경 round-trip 복원 성공이 아니다.** 함수·테이블 외 enum/domain/sequence/default privileges 및 플랫폼 확장 의존성까지 포함한 실행 DDL, 외부 송신 비활성 별도 실제 DB 환경이 더 필요하다.
- 순차 역사 재생 문제와 위 재고 RPC 국소 운영 패치를 구분한다.

## 현재 사이트·상품·실문의·주문

### 서비스/배포

22시대 Vercel API 읽기에서 `team_Y4vAMBqZlfQ4gXvkGieFh5aG`, 프로젝트 `sellerpilot-global / prj_9fRYsoTT4fD6XVEMe4NX9mpPlljA`, Git `couplit-korean/sellerpilot-global`, production branch `main` 확인.

Production은 **`c3d1bf117d61dfd5877d96af31c5bc8976a3c2bf`**, deployment **`dpl_9ckWtKSrz6uBSFbiowHL5QeZf5oi`**, READY. DB 22:09 읽기에서 active release 같은 SHA, scheduleCount 6, unsafePendingMutations 0, 전체 recon 9, mutation 관련 recon 8. 일정 활성화는 실제 최근 작업 성공을 의미하지 않으며 여러 최근 wake 결과가 queued이다.

### 현재 대상 상품

21:49:55 KST DB 읽기: 롯샌 `1ed4acfc-7603-48ec-a638-241131e59358`, SKU `AUTO-780720401E2D4E4EA45F`, `products.on_hand=1/reserved=0`. 상세/승인 v1/v1, manifest 8장, revision 0. 현재 AI job `e0ae47da-1493-4c6d-934f-d984e179b264`는 9/2 succeeded이나 품질검사상 degraded이다.

CS tickets/inbound/reply deliveries 각 0. 실제 주문은 `58bbcf27-407d-4576-bab1-e35e72f52bf6` 쿠팡 cancelled 1건, product_id null, 송장 없음, shipment_write_status never. 출고 가능한 실주문 없음. 재고 원장 각 0. 실제 답변·송장 테스트용 허위 데이터 생성 없음.

| 채널 | 새 확인 | 완료 판정/다음 행동 |
|---|---|---|
| Qoo10 | 공개 `1217536689`, Couplet Seoul, 1,871엔, 장바구니/무료 TracX 배송. DB published/live/S2. 제목은 일반 폴백. 기존 QSM `zrlawjdgns (Couplet Seoul)` 로그인 확인이나 정확 상품코드 검색은 0건 | 공개 접근은 확인. 새 승인 manifest/정확 제목·배송 검수, QSM 검색과 API 판매자 계보 대조 필요. create 금지 |
| 11번가 | GET sellerprodcode/prodmarket 각200, SKU 일치, `9598600918`, 103 판매중. 공개 3,190원. 요약 무료배송 vs 승인 상세 3,000원 불일치. GET `prdStckQty=0` 해석 미확정 | 판매 품질 완료 아님. 공식 유료배송 명세·기존 배송설정 확인 후 기존 ID 수정 판단. create/binder/recon 금지 |
| Shopee | 기존 Seller Centre `gjrxn:main / Couplit.kr`, SG `gjrxn.sg` 로그인, 출고 대기0. DB 대상failed/remote없음 | UI 로그인과 API refresh 거절/허용 IP 해제는 별개. 기존 recon 재시도 안 함 |
| Lazada | 기존 판매자 탭 `ERR_TOO_MANY_REDIRECTS`. 대상 listing 없음 | 로그인 리디렉션 복구·공식 session seller subject 결속/IM 권한. 기존 Connected 앱 해제 금지 |
| 쿠팡 | Wing 김정훈/커플릿 로그인, 보안 일시 비활성화 경고 유지, 출고/미답변0 | 판매자지원팀의 해제 절차 대기. 신규 상품/민감 API 차단 유지 |
| 스마트스토어 | 기존 탭 네이버 커머스ID 로그인 화면. 대상 listing 없음 | 실제 인증 및 허용 IP/쓰기 실행 경로 확인. Mac GET 과거 성공으로 쓰기 허용 안 함 |
| eBay | 공식 taxonomy 20473/LOTTE/Cookie & Biscuit, location ENABLED. SKU inventory GET404. 정책 GET 성공 | 검수 영어 제목/manifest, USD 확정가·손익·반품정책 결속 미완료. 다른 테스트상품800531801262 제외 |
| Temu | Partner 앱 Compliance Rejected / Inactive 재확인 | 심사 후속 조치 대기. 임의 재제출/키 저장 없음 |

11번가 기존 로그인은 이메일 OTP 화면. 이메일 재전송 1회 후 해당 Gmail 계정의 최근/전체메일 검색에서 인증 메일 미확인. SellerPilot 기존 화면은 idle 로그아웃, 저장 로그인 시도 후에도 대시보드 미진입. 코드만으로 로그인 실패 원인을 확정하지 않았다. 다른 사이트 전체 로그아웃으로 확대하지 않는다.

## 상세·이미지 사실 증거 및 실제 생성 차단

- 원본6장+정규화본6장 다운로드·디코딩·바이트/크기 대조. 원본 1=4, 2=5, 3=6이 파일/픽셀 해시 모두 같아 **고유 원본3장**이다. 대표 원본 SHA는 기존 source_photo_sha256과 일치한다.
- 공식 [롯데웰푸드 315g 상품](https://lottesweetmall.com/product/건과롯샌-파스퇴르-순우유맛-315g/4877/category/106/display/1/)의 품질표시에서 롯데웰푸드 제조판매원, 총315g(52.5g×6), 밀/대두/우유/달걀, 쇠고기/땅콩 혼입 가능, 1봉당265kcal 확인. 이 자료를 저장 상품 사실에 임의 반영하지 않았다.
- 동일 GTIN 계열 유통사 근거: https://lottemartzetta.com/products/OS8802259030799/details . 선반 가격표 바코드와 실물 포장 바코드를 구분한다.
- Research는 sources가 비고 사용자 텍스트 근거였다. Studio human review는 관리자 승인 기록이 있으나, 배송포장 **0.4kg/28×20×7cm**의 실측표/출처는 없다. **seller-confirmation-required**. 원산지 대한민국 직접 표기와 실재고 소비기한 날짜도 별도 확인 필요.
- 설치 worker는 **1.13 / 기본 gpt-5.6-sol**, 저장소1.60/claim 최소1.59. Aside 모델과 다른 사실을 구분한다. 전체 범위 설치기를 임의 재시작하지 않았다. product-only 전환 시 worker 큐 재확인·외부 백업·검증된 commit·부하 제한이 필요하다.
- 생성 POST 0회. 기존 job 경로를 새 job에 재사용하지 않으며, 필수 사실·readiness·검증 release/runtime이 확정된 뒤 새 revision 1회 실행해야 한다.

## 마무리 확인

- 최종 수정 코드 SHA: **`4c9253f2e18b5267a17f4b730c79159993d10892`**. 이후 문서 커밋은 코드/운영 SHA와 구분한다. 코드·문서는 `origin/integration-aside`에 선택 커밋·푸시하고 원격 SHA를 확인한다. `couplit/main`에는 푸시하지 않는다.
- **운영 반영:** 재고 RPC2개 신규 패치만 완료. Vercel Production 전환·candidate 배포·게시 게이트 해제는 미실행이다. 새 baseline 복원 및 누락 선행 정의에 대한14실패가 남아 인계의 release 선행조건을 충족하지 못했다.
- **외부 완료:** Qoo10·11번가 기존 공개 접근/GET 확인만 추가. 새 상품 등록·상품 수정·CS 답변·송장·AI 생성 없음. 11번가 공개 재조회도 무료배송/3,190원/기존 이미지이며 새 등록 증거가 아니다.
- **[blocked] 상세/이미지:** 새 revision·실제 결과 검수는 미실행. 검증된 release/runtime, 기존 관리자 확인값 중 배송포장 실측/현재 소비기한 근거가 더 필요하다. 공식 표시사항과 기존 human review는 증거로 보존했다.
- **[blocked] CS/배송 실증:** 실문의·실출고 대상이 없다. 고객 대상·내용 승인 없이 전송하지 않았다. Lazada 시각 미확정 원문의 별도 영속 보관, 비텍스트 수신, 수신 묶음을 넘는 주문품목 소유권 검사는 추가 내부 작업이다.
- **[blocked] 상품/재고:** 신규 RPC 국소 안전성은 보강했지만 기존 products 기준 재고와 새 원장의 운영 cutover·주문/취소/반품 연결 및 원격 재고 일치는 미완료. DB 기준/운영 통합을 확정한 뒤 기존 product1/예약0 기준으로 멱등 이관·동시변경 방지를 검증해야 한다.
- **[blocked] 업로드:** 11번가 유료배송 공식 계약, eBay USD 가격·정책 결속·영문 검수, Shopee OAuth/IP, Lazada session/계보/IM, 쿠팡 제한, Temu 심사, 스마트스토어 인증/쓰기 경로가 남는다. 판매자센터 로그인과 API 권한을 분리한다.

이번 세션의 계획/테스트/작업 접수는 위 미완료 항목을 완료로 바꾸는 근거가 아니다. 보호 SQL과 과거 migration은 수정하지 않았다.


## 사용자 재지시 이후 실제 재생성 · 2026-09-05 22:30~23시대 KST

아래 결과는 위 마무리 표기의 `생성0회` 및 `대시보드 미진입`을 갱신한다. 전체 완료 기록은 아니다.

1. 기존 Aside SellerPilot 세션의 저장 로그인으로 운영센터 진입·인증된 API200을 확인. 세션은 Supabase SSR 쿠키에 저장되며 자격증명 원문을 문서에 남기지 않는다.
2. 생성 직전 READ ONLY 조회: AI research/studio succeeded 각1, queued/running0, revision0. Gateway queued42/running0/reconciliation9. 상품ID/SKU를 보존하는 전체수정 UI에서 원본2→main, 원본3→barcode(선반 표기), 원본1→extra를 선택했다. 중복6장을 다시 올리지 않았다.
3. 공식315g 표시 근거의 52.5g×6, 1봉265kcal, 함유/혼입 가능 알레르기, 보관 문구를 **새 생성 요청 입력에** 반영했다. 배송포장 관리자 수치·가격·재고는 바꾸지 않고 실측/재고 소비기한을 확정했다고 표현하지 않았다.
4. `09f8ea94-e140-4811-9dec-ef9ad8ac8045` 리비전은 23:03:01 KST 접수, 실제 running/attempt1, 23:03:55 failed. Revision GET의 안전 원인 `gateway_rate_limited`, appliedAt=null. AI job GET은 기존 sanitization으로 일반 오류만 보여 원인을 별도 revision API로 확인했다. 새 리비전 생성/무분별한 재시도/외부 게시 없음.
5. 오류 allowlist에 정확한 gateway_rate_limited(및 고정 server 접두사)만 추가하고 재시도 자제를 알리는 고정 판매자 문구를 매핑했다. 임의 provider body/credential 문자열은 계속 숨긴다. `tests/ai-worker-error-safety.test.ts` 6/6 및 lint 통과.
6. Image Generation 실물 기반 대표 시안1·보정시안1 생성. 1차 `imagegen-ousKPAO2dTasNtdd.png`의 비율/과한 채도를 독립 리뷰했고 2차 `imagegen-sBEUortRFB5dRLdJ.png`는 약1.59 비율·주요315g/6봉/원료 함량 일치. 생성 재묘사 위험이 있어 시안이며 정식 job 해시·승인 manifest로 직접 편입하지 않았다. 기존 앱에 외부 완성 이미지 import API가 없다는 코드 검토도 완료.
7. Puck ko/ja/en 문안 초안은 tmp/reviewed-detail-copy.json에 보존. 공통 문구의 locale 누출을 실제 렌더/HTML export에서 수정·검증하고 `fbf4040` 저장·푸시. Production 미전환.
8. 현 catalog 암호화 bundle/키를 tmp 외부 private backup 폴더에 복사하고 바이트 동일·권한600/상위700을 확인했다. 실행 DDL export와 독립 native 복원 성공은 분리한다.

CS/재고/배송 신규 영속 계약은 독립 검토에서 발견한 P1을 수정 중이다. 테스트 통과만으로 운영 적용하거나 미해결 항목을 목록에서 빼지 않는다. Runtime staging/개별 channel 상태와 실제 다음 실행은 후속 기록으로 남긴다.
