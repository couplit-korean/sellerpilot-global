# Aside 잔여 운영 실행 기록 — 2026-09-05

## 실행 기준과 확인 범위

- 시작 21:42 KST. 실제 작업 경로 `/Users/kimchangheemac/dev/sellerpilot`, `integration-aside`, 시작 HEAD `86a0d8ce069a5300a78c766b4139bfd468668f5c`.
- 시작 변경은 보호 대상 미추적 `supabase/migrations/20260903150000_unblock_shopee_second_oauth_deadlock.sql` 하나. 파일 수정·삭제·스테이징·커밋·운영 적용 없음.
- Aside 실제 세션 저장값: 총괄 및 하위 8개 모두 `openai-codex / gpt-6-astra / high / fastMode=false`. 22:02 KST 총괄 1 + 하위 8 동시 `running` 확인. 현재 계정 모델 목록에 `gpt-6-astra`가 있으며 별도 `gpt-6` 식별자로 보고하지 않는다.
- 5개 영역 담당을 유지하고 DB baseline·로그인 경쟁조건·11번가 공식 배송 계약 읽기 검토 3개를 추가했다. 외부 쓰기와 통합은 총괄 단독, 코드 파일 소유권은 영역별로 제한했다.
- 사이트별 기존 Aside 세션을 사용했다. 별도 Chrome 프로필 전환·앱 연결 해제 없음.
- 아래는 실제 실행 기록이다. 10시간 경과나 8채널 운영 완료를 주장하지 않는다.

## 실제 운영 변경: 재고 RPC 2개

**22:08:45 KST 신규 migration `20260905130000_reject_inventory_idempotency_conflicts.sql` 적용 및 재조회 완료.**

- 소스 코드 커밋: `526b15e`.
- SQL SHA-256: `740877fadd9678aafb0b5048a569b07dbb41b9f14b49255d85dac3b0f6b60967`.
- Supabase ref: `sqaoqucxakebqkiygdxb`.
- 적용 전 원본 `prosrc` MD5·인자/기본값·service_role 전용 ACL·SECURITY DEFINER·빈 search_path를 읽기 대조했다.
- 같은 멱등키의 수량/주문/채널 충돌, 콜론 결합키 충돌, 반품의 부분 참조, 과도한 참조 길이, 명시적 NULL 수량을 사전 거부한다.
- 기존 키 체계·원장 행을 바꾸지 않는다. 새 원장에 기존 상품 재고를 입고하거나 주문 처리를 연결한 변경이 아니다.
- 소스 해시 고정, 5초 lock/30초 statement 제한, 기존 동일 이력 부재 확인 후 한 트랜잭션으로 함수 패치와 해당 신규 이력 1건만 기록했다. HTTP 201을 완료 근거로 삼지 않고 다음 정의·권한·카탈로그 재조회를 수행했다.

| 함수 | 적용 전 MD5 | 적용 후 MD5 |
|---|---|---|
| `sellerpilot_inventory_reserve` | `99a6cf343eabafa12445c07055c3fd19` | `7b26b840009583fee8178d6199320c9f` |
| `sellerpilot_inventory_return_received` | `529ede0b37736f731b5b67f76d64c583` | `04c696516cb3b7cd2b790b6575722ded` |

전후 catalog 비교: **위 함수 2개 변경 + 신규 migration 이력 1건 추가만 확인**. 820함수/118관계 수 유지. anon/authenticated 실행 불가·service_role 실행 가능 유지. inventory items/ledger/reservations 각 0 유지.

새 이력 수는 **271**, 최종 version `20260905130000`. 이전 270개 이후 누락된 역사 이력을 복구했다는 뜻이 아니다.

## 코드 수정과 집중 검증

| 영역 | 수정 | 확인 | 운영 경계 |
|---|---|---|---|
| 상세 이미지 | 이미 배정된 역할이 fallback 역할 큐를 소모하지 않도록 수정 | manifest 7/7, 내부 혼합 256/256 조합 | 기존 degraded 결과를 고친 것은 아님 |
| CS | ticket 단위 대신 메시지 inboundKey 중복 제거, Lazada 구매자·판매자 정상 텍스트 대화 보존 | TS 집중 검사, DB 저장·중복·순서 검사 통과 | 실제 문의/답변 0. 비텍스트 보존은 미검증 |
| CS 교차검토 | 시각 미확정 판매자 이벤트가 수집시각으로 최신 답변이 되는 P1 재현·차단 | TS 27/27, 원장 2/2 | 시각 미확정 묶음은 적재 전 실패. 별도 원문 quarantine 저장은 미구현 |
| 주문/배송 | Lazada 잘못된 타입·누락·중복·100개 초과·불일치 상세와 수신 묶음 간 remote item 충돌 차단 | 집중 130/130, 격리 DB 저장→fulfillment 재조회→shipment-draft 확인 | 잘못된 상세는 표시/수량에도 사용 안 함. 서로 다른 수신 묶음 간 영속 소유권 검사는 미구현 |
| 재고 | 순수 원장과 서비스 RPC의 충돌 거부, 참조 완전성 보강 | 최종 DB 12/12, TS 원장 25/25 | RPC 2개만 운영 적용. 원장 운영 연결은 미완료 |
| 11번가 배송 | 서버 저장 배송 사실 결속, 브라우저 metadata 누락/0원 변조 차단, legacy content 분류 유지 | 집중 TS100/100·route7/7, 교차리뷰 재검증3/3. 3,000원 요청 provider0회 | 코드 `4c9253f`. 기존 상품 수정/create/binder/recon 재실행 없음 |
| DB 감사 | 고정 READ ONLY catalog SELECT, 함수·권한·관계·RLS·trigger·이력 지문 비교 | 9/9 | 비밀·업무행을 읽지 않으며 함수 설정도 원문 대신 hash 저장 |

CS/주문/manifest 통합 집중 **61/61**, 최종 11번가 부모 집중47/47. 담당별 검사와 겹치므로 수치를 합산하지 않는다. 전체 기존 TypeScript1,999개를 처음부터 반복한 수치가 아니다. 코드 커밋 `1f81cc2`, 재고 `526b15e`, DB 감사/fixture `2b6cd9b`, 11번가 최종 `4c9253f2e18b5267a17f4b730c79159993d10892`.

최종 코드의 **Vercel용 build exit0, 빌드 후 `tsc --noEmit` exit0**, 변경 파일 ESLint·diff-check 통과. 중간에 tsc와 Next 빌드를 병렬 실행해 `.next/types` 재생성과 읽기가 충돌한 검증 순서 오류가 있었다. 빌드 완료 후 순차 타입검사로 해결했으며 소스 타입오류로 보고하지 않는다.

## DB 선행 이력: 남은 실패를 숨기지 않음

- 기존 전체 MJS/DB 15실패 중 역사적 static-egress fixture에 후속 overlay가 섞여 존재하지 않는 policy를 참조하던 1건을 fixture 시점 경계로 고쳤다. 집중 1/1 통과. 과거 migration guard는 수정하지 않았다.
- 최초 로컬 DB 검사에서 보호 미추적 SQL을 디렉터리 열거가 자동 포함하는 것을 발견했다. **일회성 로컬 PGlite 재생에 포함된 경로가 있었으며**, 이후 전범위 두 검사에 입력 제외 helper를 적용했다. 원본 파일 및 운영 DB에는 해당 SQL을 적용하지 않았다. 입력 제외 검사는 실패 원인 migration `20260904211500`이 계속 포함되는 것도 확인한다.
- 현재 두 잔여 suite **22개 중 8통과/14실패**, skipped 0. 14개 모두 `20260904211500`의 `11820 Shopee in-list marker count=0`로 막힌다.
- `20260904232000` 후속 snapshot을 누락 선행 원본으로 바꾸거나 순서를 이동하지 않았다. 기존 SQL 수정·이력 위조·전체 과거 재적용 없음.
- `scripts/audit-db-schema-baseline.mjs`가 확보한 것은 현재 catalog 지문이다. **전체 DDL 복원 패키지나 새 PostgreSQL/Supabase 환경 round-trip 복원 성공이 아니다.** 함수·테이블 외 enum/domain/sequence/default privileges 및 플랫폼 확장 의존성까지 포함한 실행 DDL, 외부 송신 비활성 별도 실제 DB 환경이 더 필요하다.
- 순차 역사 재생 문제와 위 재고 RPC 국소 운영 패치를 구분한다.

## 현재 사이트·상품·실문의·주문

### 서비스/배포

22시대 Vercel API 읽기에서 `team_Y4vAMBqZlfQ4gXvkGieFh5aG`, 프로젝트 `sellerpilot-global / prj_9fRYsoTT4fD6XVEMe4NX9mpPlljA`, Git `couplit-korean/sellerpilot-global`, production branch `main` 확인.

Production은 **`c3d1bf117d61dfd5877d96af31c5bc8976a3c2bf`**, deployment **`dpl_9ckWtKSrz6uBSFbiowHL5QeZf5oi`**, READY. DB 22:09 읽기에서 active release 같은 SHA, scheduleCount 6, unsafePendingMutations 0, 전체 recon 9, mutation 관련 recon 8. 일정 활성화는 실제 최근 작업 성공을 의미하지 않으며 여러 최근 wake 결과가 queued이다.

### 현재 대상 상품

21:49:55 KST DB 읽기: 롯샌 `1ed4acfc-7603-48ec-a638-241131e59358`, SKU `AUTO-780720401E2D4E4EA45F`, `products.on_hand=1/reserved=0`. 상세/승인 v1/v1, manifest 8장, revision 0. 현재 AI job `e0ae47da-1493-4c6d-934f-d984e179b264`는 9/2 succeeded이나 품질검사상 degraded이다.

CS tickets/inbound/reply deliveries 각 0. 실제 주문은 `58bbcf27-407d-4576-bab1-e35e72f52bf6` 쿠팡 cancelled 1건, product_id null, 송장 없음, shipment_write_status never. 출고 가능한 실주문 없음. 재고 원장 각 0. 실제 답변·송장 테스트용 허위 데이터 생성 없음.

| 채널 | 새 확인 | 완료 판정/다음 행동 |
|---|---|---|
| Qoo10 | 공개 `1217536689`, Couplet Seoul, 1,871엔, 장바구니/무료 TracX 배송. DB published/live/S2. 제목은 일반 폴백. 기존 QSM `zrlawjdgns (Couplet Seoul)` 로그인 확인이나 정확 상품코드 검색은 0건 | 공개 접근은 확인. 새 승인 manifest/정확 제목·배송 검수, QSM 검색과 API 판매자 계보 대조 필요. create 금지 |
| 11번가 | GET sellerprodcode/prodmarket 각200, SKU 일치, `9598600918`, 103 판매중. 공개 3,190원. 요약 무료배송 vs 승인 상세 3,000원 불일치. GET `prdStckQty=0` 해석 미확정 | 판매 품질 완료 아님. 공식 유료배송 명세·기존 배송설정 확인 후 기존 ID 수정 판단. create/binder/recon 금지 |
| Shopee | 기존 Seller Centre `gjrxn:main / Couplit.kr`, SG `gjrxn.sg` 로그인, 출고 대기0. DB 대상failed/remote없음 | UI 로그인과 API refresh 거절/허용 IP 해제는 별개. 기존 recon 재시도 안 함 |
| Lazada | 기존 판매자 탭 `ERR_TOO_MANY_REDIRECTS`. 대상 listing 없음 | 로그인 리디렉션 복구·공식 session seller subject 결속/IM 권한. 기존 Connected 앱 해제 금지 |
| 쿠팡 | Wing 김정훈/커플릿 로그인, 보안 일시 비활성화 경고 유지, 출고/미답변0 | 판매자지원팀의 해제 절차 대기. 신규 상품/민감 API 차단 유지 |
| 스마트스토어 | 기존 탭 네이버 커머스ID 로그인 화면. 대상 listing 없음 | 실제 인증 및 허용 IP/쓰기 실행 경로 확인. Mac GET 과거 성공으로 쓰기 허용 안 함 |
| eBay | 공식 taxonomy 20473/LOTTE/Cookie & Biscuit, location ENABLED. SKU inventory GET404. 정책 GET 성공 | 검수 영어 제목/manifest, USD 확정가·손익·반품정책 결속 미완료. 다른 테스트상품800531801262 제외 |
| Temu | Partner 앱 Compliance Rejected / Inactive 재확인 | 심사 후속 조치 대기. 임의 재제출/키 저장 없음 |

11번가 기존 로그인은 이메일 OTP 화면. 이메일 재전송 1회 후 해당 Gmail 계정의 최근/전체메일 검색에서 인증 메일 미확인. SellerPilot 기존 화면은 idle 로그아웃, 저장 로그인 시도 후에도 대시보드 미진입. 코드만으로 로그인 실패 원인을 확정하지 않았다. 다른 사이트 전체 로그아웃으로 확대하지 않는다.

## 상세·이미지 사실 증거 및 실제 생성 차단

- 원본6장+정규화본6장 다운로드·디코딩·바이트/크기 대조. 원본 1=4, 2=5, 3=6이 파일/픽셀 해시 모두 같아 **고유 원본3장**이다. 대표 원본 SHA는 기존 source_photo_sha256과 일치한다.
- 공식 [롯데웰푸드 315g 상품](https://lottesweetmall.com/product/건과롯샌-파스퇴르-순우유맛-315g/4877/category/106/display/1/)의 품질표시에서 롯데웰푸드 제조판매원, 총315g(52.5g×6), 밀/대두/우유/달걀, 쇠고기/땅콩 혼입 가능, 1봉당265kcal 확인. 이 자료를 저장 상품 사실에 임의 반영하지 않았다.
- 동일 GTIN 계열 유통사 근거: https://lottemartzetta.com/products/OS8802259030799/details . 선반 가격표 바코드와 실물 포장 바코드를 구분한다.
- Research는 sources가 비고 사용자 텍스트 근거였다. Studio human review는 관리자 승인 기록이 있으나, 배송포장 **0.4kg/28×20×7cm**의 실측표/출처는 없다. **seller-confirmation-required**. 원산지 대한민국 직접 표기와 실재고 소비기한 날짜도 별도 확인 필요.
- 설치 worker는 **1.13 / 기본 gpt-5.6-sol**, 저장소1.60/claim 최소1.59. Aside 모델과 다른 사실을 구분한다. 전체 범위 설치기를 임의 재시작하지 않았다. product-only 전환 시 worker 큐 재확인·외부 백업·검증된 commit·부하 제한이 필요하다.
- 생성 POST 0회. 기존 job 경로를 새 job에 재사용하지 않으며, 필수 사실·readiness·검증 release/runtime이 확정된 뒤 새 revision 1회 실행해야 한다.

## 마무리 확인

- 최종 수정 코드 SHA: **`4c9253f2e18b5267a17f4b730c79159993d10892`**. 이후 문서 커밋은 코드/운영 SHA와 구분한다. 코드·문서는 `origin/integration-aside`에 선택 커밋·푸시하고 원격 SHA를 확인한다. `couplit/main`에는 푸시하지 않는다.
- **운영 반영:** 재고 RPC2개 신규 패치만 완료. Vercel Production 전환·candidate 배포·게시 게이트 해제는 미실행이다. 새 baseline 복원 및 누락 선행 정의에 대한14실패가 남아 인계의 release 선행조건을 충족하지 못했다.
- **외부 완료:** Qoo10·11번가 기존 공개 접근/GET 확인만 추가. 새 상품 등록·상품 수정·CS 답변·송장·AI 생성 없음. 11번가 공개 재조회도 무료배송/3,190원/기존 이미지이며 새 등록 증거가 아니다.
- **[blocked] 상세/이미지:** 새 revision·실제 결과 검수는 미실행. 검증된 release/runtime, 기존 관리자 확인값 중 배송포장 실측/현재 소비기한 근거가 더 필요하다. 공식 표시사항과 기존 human review는 증거로 보존했다.
- **[blocked] CS/배송 실증:** 실문의·실출고 대상이 없다. 고객 대상·내용 승인 없이 전송하지 않았다. Lazada 시각 미확정 원문의 별도 영속 보관, 비텍스트 수신, 수신 묶음을 넘는 주문품목 소유권 검사는 추가 내부 작업이다.
- **[blocked] 상품/재고:** 신규 RPC 국소 안전성은 보강했지만 기존 products 기준 재고와 새 원장의 운영 cutover·주문/취소/반품 연결 및 원격 재고 일치는 미완료. DB 기준/운영 통합을 확정한 뒤 기존 product1/예약0 기준으로 멱등 이관·동시변경 방지를 검증해야 한다.
- **[blocked] 업로드:** 11번가 유료배송 공식 계약, eBay USD 가격·정책 결속·영문 검수, Shopee OAuth/IP, Lazada session/계보/IM, 쿠팡 제한, Temu 심사, 스마트스토어 인증/쓰기 경로가 남는다. 판매자센터 로그인과 API 권한을 분리한다.

이번 세션의 계획/테스트/작업 접수는 위 미완료 항목을 완료로 바꾸는 근거가 아니다. 보호 SQL과 과거 migration은 수정하지 않았다.


## 사용자 재지시 이후 실제 재생성 · 2026-09-05 22:30~23시대 KST

아래 결과는 위 마무리 표기의 `생성0회` 및 `대시보드 미진입`을 갱신한다. 전체 완료 기록은 아니다.

1. 기존 Aside SellerPilot 세션의 저장 로그인으로 운영센터 진입·인증된 API200을 확인. 세션은 Supabase SSR 쿠키에 저장되며 자격증명 원문을 문서에 남기지 않는다.
2. 생성 직전 READ ONLY 조회: AI research/studio succeeded 각1, queued/running0, revision0. Gateway queued42/running0/reconciliation9. 상품ID/SKU를 보존하는 전체수정 UI에서 원본2→main, 원본3→barcode(선반 표기), 원본1→extra를 선택했다. 중복6장을 다시 올리지 않았다.
3. 공식315g 표시 근거의 52.5g×6, 1봉265kcal, 함유/혼입 가능 알레르기, 보관 문구를 **새 생성 요청 입력에** 반영했다. 배송포장 관리자 수치·가격·재고는 바꾸지 않고 실측/재고 소비기한을 확정했다고 표현하지 않았다.
4. `09f8ea94-e140-4811-9dec-ef9ad8ac8045` 리비전은 23:03:01 KST 접수, 실제 running/attempt1, 23:03:55 failed. Revision GET의 안전 원인 `gateway_rate_limited`, appliedAt=null. AI job GET은 기존 sanitization으로 일반 오류만 보여 원인을 별도 revision API로 확인했다. 새 리비전 생성/무분별한 재시도/외부 게시 없음.
5. 오류 allowlist에 정확한 gateway_rate_limited(및 고정 server 접두사)만 추가하고 재시도 자제를 알리는 고정 판매자 문구를 매핑했다. 임의 provider body/credential 문자열은 계속 숨긴다. `tests/ai-worker-error-safety.test.ts` 6/6 및 lint 통과.
6. Image Generation 실물 기반 대표 시안1·보정시안1 생성. 1차 `imagegen-ousKPAO2dTasNtdd.png`의 비율/과한 채도를 독립 리뷰했고 2차 `imagegen-sBEUortRFB5dRLdJ.png`는 약1.59 비율·주요315g/6봉/원료 함량 일치. 생성 재묘사 위험이 있어 시안이며 정식 job 해시·승인 manifest로 직접 편입하지 않았다. 기존 앱에 외부 완성 이미지 import API가 없다는 코드 검토도 완료.
7. Puck ko/ja/en 문안 초안은 tmp/reviewed-detail-copy.json에 보존. 공통 문구의 locale 누출을 실제 렌더/HTML export에서 수정·검증하고 `fbf4040` 저장·푸시. Production 미전환.
8. 현 catalog 암호화 bundle/키를 tmp 외부 private backup 폴더에 복사하고 바이트 동일·권한600/상위700을 확인했다. 실행 DDL export와 독립 native 복원 성공은 분리한다.

CS/재고/배송 신규 영속 계약은 독립 검토에서 발견한 P1을 수정 중이다. 테스트 통과만으로 운영 적용하거나 미해결 항목을 목록에서 빼지 않는다. Runtime staging/개별 channel 상태와 실제 다음 실행은 후속 기록으로 남긴다.


## 후속 통합 검증과 현재 차단점 · 2026-09-06 00:20 KST

### 저장·검증

- `fbf4040`: Puck ko/ja/en 공통 문구·locale 계약.
- `9134e47`: 안전한 요청 제한 오류 표시 및 실제 리비전 증거.
- `54787b2`: products 원자적 재고 미러/미결 쓰기 guard, 암호화 baseline 도구, 실행기 동시성 옵션.
- **`8ab4b717c7f416faa2505a886c68acc97e69640d`**: CS V2 부분 수신·충돌 원문 격리 및 Lazada 영속 품목/매 외부쓰기 fresh guard 통합. origin/integration-aside 일치 확인.
- 부모 통합 실행: CS·배송175/175, 문의 DB15/15, 재고·baseline·실행기45/45. 별도 오류 표시6/6. `build:vercel` 성공 후 순차 tsc 성공, 변경 파일 lint 통과. 235개 집중 통과와 역사 누락14실패를 합쳐 전체 성공이라고 하지 않는다.
- 신규140000/141000/142000은 **코드 저장만**, 운영 미적용. 보호 미추적 SQL은 끝까지 그대로이며 커밋/적용 대상에 포함하지 않았다.

### 수정한 독립 검토 결함

1. 재고 bootstrap: reconciliation run/item/gateway 누락, 수량을 포함하는 listing.update 누락을 고쳤다. 실제 상품 쓰기6종(`listing.create/update/stop/activate`, `price.update`, `inventory.update`)을 소스와 대조한다. 검사 중5개 표의 보수적 SHARE NOWAIT 잠금은 즉시 실패할 수 있다. 실제 다중 세션 검증은 아니다.
2. Lazada 배송: owner를 넘어 같은 물리 seller+item 충돌을 막고, AFTER의 advisory/다른 주문 UPDATE 잠금 역전을 제거했다. 기존 queued와 claim, 값이 변하지 않는 COALESCE marker UPDATE도 검사한다. serverless 캐시 예외와 local/provider adapter hook을 연결해 pack·RTS 각각 직전 lease/fresh begin/lease를 수행한다. 격리 SQL+어댑터에서 pack 이후 충돌·취소 시 RTS0회. 원격 미수집 취소와 native 동시성은 미검증.
3. CS: quarantine1000건에서 정상 buyer까지 롤백하던 경로를 JSON V2 부분 수신으로 분리했다. 동일 ID의 서로 다른 본문/sender를 timestamp 보완으로 덮어 answered 처리하던 경로를 차단하고 양쪽 원문을 보존했다. 후속 verified 충돌도 SQL에서 검증한다. 미보관 원문을 누락한 재시도는 완료되지 않는다. legacy integer 부분 처리 거절·권한·감사도 독립 재현했다. 본문7일, digest 최초 관찰 후90일이며 관련 SQL 주석을 일치시켰다.

### 실제 데이터·시각 결과

- 후속 publish-context200: 원래 상품ID/SKU/재고1 유지, 설명·manualFields·source path가 리비전 전과 모두 동일. 실패 작업은 기존 상세/자산을 덮어쓰지 않았다.
- 운영 READ ONLY: 대상 상품에 11번가 listing.create, Qoo10 listing.update, Shopee listing.create가 reconciliation_required 각1건. 이 상태에서 원장 bootstrap은 차단되는 것이 정상이다.
- 해당23:02~23:05 Production revision 로그1건은429/unknown_rate_limit/upstreamProviderAttempted=false, retryAfterMs 없음. 팀/프로젝트 ID 일치. 로그에는 job UUID가 없어 시간대·API 경로 상관관계이며, credits/customerVerification 또는 일시적 요청 한도로 확정하지 않는다. 불확실한 원인으로 결제/설정 변경이나 반복 생성하지 않았다.
- 실제 산출물(현재 세션 artifacts): `imagegen-sBEUortRFB5dRLdJ.png`, `lotte-detail-preview-ko.html`, `lotte-detail-preview-ko-desktop.png`, `lotte-detail-preview-ko-mobile.png`. PNG magic 확인, desktop1000×4180/mobile390×3815, 모바일 가로 넘침 없음. 시안·미게시 표시. 앱·DB·manifest 편입/승인/채널 반영 증거는 아니다.

### 복원·runtime 차단

- 암호화 v4 baseline:8,593 DDL, 5,215,956bytes, SHA256 `536d3ae416bf84ef2364381076daa2999583b6924fa3796b236c3f0ed67ed9ea`. 신규 보안 속성에 대해 PGlite round-trip 전용13/13. native 복원이나 고객/Vault 데이터 복원이 아니다.
- v4 bundle/키는 `~/.aside/u/0/backups/sellerpilot/20260905-974d4cb-v4/`에 보존(폴더700/파일600, 바이트 일치). v2도 별도 보존. tmp 키만 남아 있는 상태가 아니다.
- 서명/해시 검증된 portable PostgreSQL17.6 다운로드는 실행 시 SIGKILL9·실행 파일 부재가 발생했다. 서버/datadir 생성 없음. 정확한 호스트 원인은 미확정이며 우회하지 않았다.
- runtime staging package 버전/메타데이터는 repo와 같지만 sharp/esbuild native 파일은 차단 뒤 없어져 native 바이트 동일성을 확인할 수 없다. staging 폴더에는 Aside 격리 표시가 관측됐다. 기존 repo 파일은 valid on disk이나 이를 staging 실행 성공으로 대체하지 않았다. 기존 runtime/plist 백업은 보존, 설치/claim/restart/token 변경 없음.

### 호스트 차단 확인에 필요한 한 단계

Aside의 OS 보안 로그 조회는 `Cannot run while sandboxed`로 차단된다. 일반 터미널에서 다음 **읽기 전용** 명령 결과를 받아 원인을 구분해야 한다. 바이너리 실행, 서명/격리 변경, 보호 해제는 포함하지 않는다.

```bash
/usr/bin/log show \
  --start '2026-09-05 22:40:00' --end '2026-09-05 23:25:00' \
  --style compact --info \
  --predicate '(process == "syspolicyd" OR process CONTAINS[c] "XProtect" OR process == "amfid") AND eventMessage CONTAINS[c] "sellerpilot-product-worker-974d4cb"'
```

출력이 비어도 정책 문제가 없다는 증거는 아니다. 그전에는 검증 실패한 installer를 실행하지 않는다. 외부 채널 권한·심사·미결 reconciliation 및 실문의/실출고 대상은 별도의 차단점으로 유지한다.


## 사용자 재지시 후 8명 병렬 실제 실행 · 2026-09-06 01:15~03시 KST

### 요청·담당·실제 산출물

사용자는 채널 연결을 계속 실행하고 상세 중간에 사실에 맞는 설정샷 약8개를 보강하라고 지시했다. 하위8명 running을 재확인해 이미지2명(각4장)·Shopee/Lazada/Smartstore/eBay/Temu/Coupang 각1명, 부모QSM/11번가/통합으로 시작했다. 이후 유휴 담당을 독립 검토·필요한 정확한 코드 수정에 재배정했다. 계획만 생성한 상태가 아니다.

- 실제 Image Generation8회 성공, 조건 없는 재시도0. 원본 박스와 앞서 보정한 대표 시안을 참고한 서로 다른 장면8개이며, 실제로 보지 못한 낱봉 디자인이나 비스킷 수량을 발명하지 않았다.
- 맛·커피·6봉구성·책상휴식·알레르기·영양·보관·배송포장 순서로 본문에 배치했다. 이미지8장 연속 갤러리만 붙인 형태가 아니다.
- `artifacts/lotte-detail-eight-preview-ko.html`은 대표1+상세8의 base64 self-contained 시안. 최종 PC1000×12143, 모바일390×10124. 이미지9개 로드·최소글자14px·가로넘침0. 필수 사실/캡션 PC·모바일 각8/8 검증.
- 전체 캡처 반복 오류는 실제스크롤25타일을 이어 수정했고, 별도 위치6곳 재캡처와 픽셀 대조6/6. 실제 이미지 자체를 스크린샷 오류의 폴백으로 바꾸지 않았다.
- 독립 검토에서 이미지 P0/P1 재생성 사유는 발견하지 못했다. 05/07의 단순투영비율 차이를 확정 형태 오류로 남기지 않았으며, 실물 픽셀/인쇄 동일성 미보증·AI연출·소품미포함·게시전검토를 유지했다.
- native Studio job/원장/승인 manifest/상품·원격채널 편입0. 기존 실패리비전09f8ea94를 재시도하거나 시안8장을 성공Studio결과로 가장하지 않았다.

| 이미지 파일 | 내용·필수 캡션 | SHA256 |
|---|---|---|
| detail-01.png | 순우유맛 설정, 우유컵/받침 소품미포함·우유양은 함량증거 아님 | 1b76d3badd6befab7f6046af6a0dfcbe9e950e93b0c4fc1299c1dd46d7bdb214 |
| detail-02.png | 커피 곁들임, 커피/식기미포함·창가는 보관권장이 아님 | 5891accee4336b5da485136f7ee44f6dd02d78f5de6add8e3c8cb6d82f2ee512 |
| detail-03.png | 315g 1박스에52.5g×6봉, 원6개는 박스/비스킷 개수가 아님 | f45f7a7df1bf64a8e7009ad5720790575d91a923bd7770ac5a951521a54c475a |
| detail-04.png | 책상 휴식, 문구류미포함·치수/집중력효능 주장 아님 | 38b9985af4b83b4efaf2b488ccf06daa0347e9184e74283e0b13b81a4cac0bb7 |
| detail-05-allergens-preview.png | 함유 밀/대두/우유/달걀과 혼입가능 쇠고기/땅콩을 구분 | d6fb6ee089dd89102a431fa3b1e6ef7de6f4333fa25a21e149d2c25a37c72578 |
| detail-06-nutrition-preview.png | 265kcal는1봉52.5g, 박스1590kcal와 구분 | 20838eaf8709f08c585e68722fa169ef1dbf20a42696c0571f036c7060cd2b93 |
| detail-07-storage-preview.png | 보관 연출, 실제 온습도/재고 소비기한 보증 아님 | 80cf4dfa173166a453ed06278aad459d89d566292fbb3aaab9a9cf4ce7f28f68 |
| detail-08-packing-preview.png | 관리자 외부포장/완충 정책의 연출예시, 실제 방식 차이 가능 | 0f88c024a5583dcc30458319b5003345f32ddde39d93b8c042c4a4bdbbad9c13 |

시안 서버PID44591 종료는 OS권한 거부로 남았다. 재시도·보안 우회 없음. self-contained 결과는 이 서버에 의존하지 않는다.

### 채널별 정정 및 실제 작업

- 사용자 재확인: 11번가 외 플랫폼 로그인 완료. 실제 최신 탭에서 QSM·Smartstore·Lazada가 로그인 화면에서 정상 판매자 화면으로 바뀐 것을 확인했다. 새 탭의 인증 실패를 플랫폼 전체 로그아웃으로 확장하지 않는다. Apple 초기 인증 요청은 철회했고 추가 코드 입력을 요구하지 않았다.
- 11번가: 이메일2단계만 유지, 사용자가 발송 미도착을 명시한 뒤 재전송 반복/SMS/카카오전환0. 옛8/31 인증메일을 새 OTP로 사용하지 않았다.
- QSM: 숫자문자열 selectOption이 의도와 달리 상품명을 선택하는 실제 UI 상태를 발견, label='상품코드'로 명시 선택 후 1217536689 검색. **1건/판매중/재고1/1871JPY**, `zrlawjdgns (Couplet Seoul)` 정체 일치. 이전0건으로 상품 없음/소유자 불일치를 유지하지 않는다. 원격쓰기0.
- eBay: jeon_57, 활성dc4e5e1e…v140, sellerRegistrationCompleted=true, privilege/정책/taxonomy/location GET200. 대상SKU404. handoff0건이며 return287803006015가 기존4건 적용이라는 사실만 확인했다. 기본값/이 상품 적합성을 임의승인하지 않았다. 반품정책과USD가격 필요. 부분handoff는현재정상API에서 지원하지 않아 임의 필드/빈return을 넣지 않았다.
- Smartstore: 활성SellerPilot Couplet 앱 a8UTRFG2t6lIow0Smj6ha, 3.36.92.188/3.39.243.106/112.172.127.206 등록. 기존40/40 2xx와seller/account2회는 공식 과거통계. 이번새GET성공과 분리.
- Coupang: 공식지원606157559,9/4 14:43의 정상복구 답변을 발견했다. 이전605805130 지연답변/경고배너로 계속정지라고 판단하면 오판. A01601472/김정훈/커플릿 확인. 추가판매정보는비밀번호재확인단계,추가OTP아님. 새API성공/키만료·IP는미확인.
- Lazada: gjrxn@naver.com→MY Couplet Seoul/Seller Full Access 로그인복구. Commerce137451(~9/24 20:54:43),CSBot137571(~10/4 01:01:56) Connected,CSBot ERP IM Chat Active,MY4NNISR2D whitelist 확인. 공식Push Verify는Unknown error로실패/Save비활성. 정확한서버원인은미확정이며서명문제로단정하지않는다. 새RPC/웹배포뒤?app_key=137571로정상검증필요.
- Shopee: Couplit.kr/gjrxn:main, SG1719148844 유지. 기존승인/등록IP 문제로 재설정하지않음. 새인가state/준비lease/전용executor/교환/계보저장 미완료. 전용executor는새DB/서버계약이 필요하다는설계검토이며구현완료가아님.
- Temu: 파트너couplitofficial@gmail.com,COUPLIT Shop635517741905839,앱정보/Security Approved,Compliance Rejected 'Cloud service provider is incomplete'. Supabase/Mac제공자분류를추측해선택하거나허위확인후재제출하지않았다.

### 저장 코드와 독립 검증

코드커밋 **30899f65ce9ebfa212baca50c150f68133525130**, origin/integration-aside 푸시. main/Production 배포0.

1. Shopee 기존Vault의primary/main/target집합보존: 기존SG우선교환,partial/final모두보존,누락/정체불일치실패closed. 부모17 및독립추가6통과.
2. Lazada webhook: IM전용키만서명,Commerce/env폴백삭제,원문HMAC,실문의정확1개판매자/국가/인증계보만수신. 공유앱의정상비메시지Verify는서명후200·저장0. 새service-role 후보RPC는최대32/overflow부분목록금지/secret미노출. 실제SQL반환→POST독립13시나리오포함검증.
3. 진단CLI: 기존암묵sample비밀번호랜덤변경·임의origin Bearer·원문응답·발명상품생성위험을제거. 이번원인조사에서는기존스크립트를실제로실행하지않았으며,현재인증실패가그비밀번호변경탓이라고확정하지않는다. 기존유효토큰→정확user/admin→활성credential→정식7필드owner-proof,복호화후재검증,30초예산. 새ownerRPC authenticated-only/실제created_by역할근거대조. 회전검출은외부요청까지원자잠금이라는뜻이아니다.
4. privacy1.4: 미사용Vercel Static IP문구수정. 운영자Mac은설계경로/Temu미활성상태,새데이터센터/AWS리전추정추가없음. 공개문구는배포전까지이전버전.

부모 최종6파일 집중 **101/101**＋별도기존인접10파일 **53/53**=**154/154**, skip0. `build:vercel`→순차`tsc --noEmit`,변경11개TS/MJS파일ESLint·diff통과. 개별하위집계/추가독립검사를이숫자에중복합산하지않는다. 기존역사재생14실패/native보안동등복원미검증은변하지않았다.

신규SQL `20260906010000_verify_channel_credential_owner.sql` 및 `20260906011000_route_lazada_im_webhook_credentials.sql`은 **미적용**. 전자의CLI/RPC,후자의서명candidateRPC,기존140000 V2가배포선행조건이다. 기존미적용141000/142000도유지. 보호20260903150000미추적SQL은제외.

### Supabase 장애 및 실제 지원 접수

- 01:47KST 단일READ ONLY timestamp/migrationcount271 성공후,자격증명 metadata·Vault·pgstat 요청에서544와연결timeout관측. 직렬화·제한시간후에도안돼반복조회중단. 별도정상service-role RPC 경로도안전조회성공을확인못했다. 후자의원인은고정오류문구로축약되어정확실패phase가미계측이므로provider GET/토큰교환성공으로보고하지않는다. 키원문·API전체응답을로그에남기지않았다.
- 02:24~26KST 공식프로젝트UI/로그: overallUnhealthy,Auth/Storage Unhealthy. Auth의localhost[::1]:5432 연결canceled와storage_admin PgBouncer127.0.0.1:5432 connecttimeout15s/60s관측. 'server down'은pooler오류문구이고DB전체중단의독립증명은아니다.
- 02:57:52KST 부모최종UI: **overallUnhealthy,Database Unhealthy,PostgREST/Auth/Realtime/Storage/EdgeFunctions Healthy**. 지표가변동하며기능복구는미확인. Pro/nano,t4g.nano표시. 과거CPU94.86/메모리1.58GB,개요15/60을현재자원정상/고갈증명으로사용하지않는다.
- 추가관측:02:30경ShareLock~10.39초대기,02:39 internal_schedule_requests_pkey중복,02:36~40 invalidworker403반복. 특정호출/잠금사실이며전체장애근본원인이나특정로컬프로세스로단정하지않는다. 다른scheduler/worker를임의중지·재시작하지않았다.
- **Supabase비공개지원 SU-464710**: Help→Contact support,Database unresponsive/High/Authentication·Database·Storage,본문2915자. 기본ON이던사람/AI프로젝트접근을OFF로명시확인후제출. 'Support request sent'와공식수신메일확인. 키/토큰/HAR/고객자료첨부0,재부팅·추가접근·설정/유료변경미승인명시. 공식회신은1~2영업일목표/물량증가로지연가능,우선지원구매하지않음.
- 수신스레드 `thread-f:1875515203757853583`,9/7 18:00KST 1회후속 `Pum4FRXEB92cR1m7`를현재세션에연결. 기존DB복구루틴은변경/재개하지않음.
- **Temu공식지원메일**:02:16KST `partner@temu.com`에제공자분류질문1회발송,보낸메일1건확인. `thread-a:r4293799064795491088`. 기존`B0LvceV5j6A7hzGc`의6시간주기는유지하며최신거절사유/이메일확인을추가했다. 도구가다음실행을9/6 08:19KST로재계산했다. 중복지원메일/새심사제출0.

### 현재 가장 작은 재개 조건

1. Supabase서비스가실제SQL/Auth/Storage까지회복했는지검증후기존상품/큐/키fingerprint재조회. UI Healthy하나만으로운영쓰기시작하지않는다.
2. 새SQL5개와웹코드는선행관계·권한·현재정의검토후별도통제적용. 역사14실패를숨기거나보호SQL로우회하지않음.
3. 11번가이메일발송문제,쿠팡추가판매정보재확인,Temu분류답변/심사,Shopee정식전용인가경계,eBay반품·USD가격,실문의/출고대상과새이미지정상승인은각각독립조건으로남는다.
