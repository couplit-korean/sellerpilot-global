# eBay010 create-body equality and current-account audit

Root reviewed eBay010 frozen four-file patch SHA-256 `9fd2ace1462d89a6188ce36f0a7336058e0634ad094d0a2a65f15840aa8575e3`. Three central files already contained later eBay009 integration changes, but every patch hunk applied cleanly. The rebased central SHA-256 values are `4234839e8ab9fc05878bc166d6d039cca2461236ad2fffbc4caf645222e1f8f2` for `ebay-create-preflight.ts`, `a53e3e62550d21de7ebbdbaa81fd5566ab6841458decde2e6abb260326151c61` for its test, `9342e5646fe868937b4e2947df2947c8fda4eb7e9a294396c78aa0af46787511` for create-resume boundaries, and `14a9d417bd5d3963978746b1a5947b01d7e9091a3c7495f4f307d2c34066f401` for resume identity completion.

The eBay preflight now requires Inventory `product.description` and Offer `listingDescription` to be byte-exact. A different body is rejected before provider access instead of allowing two independently non-empty descriptions. Central eBay create, resume and durable-reconciliation coverage passed `47/47`; full nonincremental TypeScript and changed-file ESLint passed.

The read-only account audit confirmed Seller Hub seller `jeon_57`, four active listings, exact target SKU search `0`, and exact current Korean source-title search `0`. This is only an active-listing duplicate check. There is no approved eBay English title, so its duplicate state is unknown. The stored credential records the required Sell scopes, but its current official Taxonomy GET returned HTTP 401. No OAuth refresh was attempted, so current Inventory/Offer absence, category/aspects, seller identity and merchant location remain unverified.

Seller Hub shows fulfillment policy `287802829015`, payment policy `287802924015`, and two return policies with materially different international-return settings. The fulfillment description says three-day handling while the active control says two business days, and neither return policy is bound to the target. The current SellerPilot draft has no eBay channel payload, positive USD price, approved English content, image manifest, target policy/location binding or internal listing. The safe state remains `EBAY_CURRENT_LINEAGE_UNVERIFIED`; no create or resume mutation is permitted.

No provider mutation, OAuth change, credential write, operating database write, deployment, commit or push was performed. Evidence remains `19/48`; integrated new CREATE plus remote/site readback remains `0/8`.
