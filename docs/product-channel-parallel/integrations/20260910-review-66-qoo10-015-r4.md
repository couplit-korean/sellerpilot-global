# Qoo10 015-r4 central integration review

## Decision

qoo10-015-r4 is integrated in the local central working tree.

- Frozen source patch: /Users/kimchangheemac/dev/sellerpilot-qoo10-common-r2-20260909/docs/channel-handoffs/qoo10-015-r4-gateway-atomic-recovery.patch
- SHA-256: b22027303d422adc83ad5ee6f00f290a5f95b030f5c8660db44381139d4c49fb
- Production migration, deploy, provider mutation, commit and push: not performed.
- Existing Qoo10 item 1217536689: untouched.

## Closed defects

- Both reachable Qoo10 CREATE runners now cross one durable atomic QSM source, job, attempt, listing, credential, fingerprint and provider mutation boundary.
- Qoo10 CREATE bypasses the generic provider mutation boundary, removing the two-RPC process-loss gap.
- A crossed provider boundary never repeats CREATE after response loss. One official SellerCode lookup classifies zero, one, multiple or unavailable matches and moves uncertain outcomes to reconciliation.
- Duplicate succeeded replay uses the claim's reserved listing identity before any remote lookup.
- The unreachable local success path and its obsolete response-loss test were removed.
- The new RPC names fit PostgreSQL's 63-byte identifier limit.

## Central validation

- Atomic recovery and reachable worker suite: 56/56 PASS.
- SmartStore/Qoo10 combined nonincremental TypeScript: PASS.
- Changed-file ESLint: PASS.
- git diff --check: PASS.
- Migration reservation: 20260910033500_qoo10_gateway_create_atomic_recovery.sql.

The frozen patch attempted to delete tests/qoo10-local-create-response-loss.test.ts, which was still an untracked central file. Git therefore rejected a literal full patch apply with "deleted file still has contents". The coordinator applied every other frozen hunk, then deleted that exact obsolete file. The resulting central behavior was validated with the suite above.

## Remaining completion boundary

This is local program logic and durability evidence. It does not prove an operating credential, a fresh QSM source, an actual provider response, official remote readback or buyer-visible publication.
