# 2026-09-09 첫 상품등록 동결 제출 통합 검토

Qoo10 소유 커밋 `8cec1138d4c61e5a2a34c11d0fea9fa3baa1c35b`의 상품 실행기와 전용 검사를 정본에 적용했다. 공통 요청 qoo10-001 r2, qoo10-002 r3는 원본 파일 SHA256과 patch 적용 가능성을 대조한 후 순차 적용했다. 옛 브랜치를 전체 병합하지 않았다.

일반 Qoo10 CREATE는 검증된 live publication 문맥을 공통 worker에서 자격 갱신·이미지 처리·provider mutation 전에 요구한다. 채널 실행기의 직접 호출도 같은 계약을 요구한다. SetNewGoods가 수락했더라도 GdNo가 없거나 형식이 틀리거나 다른 ID alias와 모순되면 후속 상세 쓰기를 중단하고 재조회 필요/자동 재시도 불가로 기록한다.

공통 검사의 계약 없는 옛 CREATE fixture 4개는 동일 동작을 검증하는 strict CREATE 전용 검사로 옮겼다. 공식 요청 버전/헤더/본문, 등록 ID 누락·충돌, 오류 메시지 마스킹, 상세 재조회 실패 후 중단 처리를 검증하며 단순히 실패 검사를 삭제한 변경이 아니다.

- 통합 Qoo10 CREATE·공통 protocols·공통 gateway: 129/129 통과.
- 업무 격리 40/40, 채널 격리 28/28, 양방향 의존 경계 위반 0.
- 변경 파일 ESLint, TypeScript, 로컬 Next build 통과.
- 실제 provider/운영 DB 쓰기와 배포 0. Qoo10 실등록 완료를 뜻하지 않는다.

8개 작업 상태는 active였다. 시작 보고만 남은 Shopee/Lazada/스마트스토어는 작업을 중복 재시작하지 않았다. 다음 제출은 구체 결함을 담당 작업에 전달해 새 revision을 기다린다.

| 제출 | 검토 결과와 재개 조건 |
|---|---|
| 쿠팡 962fb0a/common r2 | SKU 누락 시 필수값 및 원격 계정 검증이 우회된다. 직접 호출에도 누락/혼합/중복 SKU를 거부하고 UI와 실행기의 바코드·모델번호 판정을 일치시켜 재제출한다. |
| Temu common r1 | 동결 명세의 patch SHA와 실제 파일 SHA가 다르다. 새 revision의 코드/patch/명세 해시 일치 및 공식 필드 계약 근거가 필요하다. |
| 11번가 0b77e214/common r1 | 응답 상위 productNo와 Product.prdNo의 충돌을 거부하고, CREATE 전체 필드가 GET에 그대로 돌아온다는 가정을 공식 응답 projection과 대조해야 한다. |
| eBay bed95fe9/common r1 | 모든 카테고리에 Brand/Product aspect를 필수로 고정한 제안을 동적 taxonomy 요구로 바꾸고 공통 실행·회귀 patch를 보완한다. |

읽은 상태 snapshot 8개는 reviewed로 기록했다. 반려 요청은 blocked와 수정 근거를 남겼으며 새로운 수정본은 별도 pending이다. Qoo10 적용한 patch와 명세 4건만 integrated로 기록했다. 나머지 수집 항목을 일괄 완료 처리하지 않았다. 다음 자동 점검은 새 동결 revision과 미처리 항목을 계속 처리한다.

스냅샷 ID·검사 결과·적용 파일 해시는 [검토 원장](20260909-review-01.json)에 보관한다.
