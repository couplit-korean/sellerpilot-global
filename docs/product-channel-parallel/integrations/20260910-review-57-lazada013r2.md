# 중앙 검토57 — Lazada013 r2 공식 응답 정정본

검토 시각: 2026-09-10 04:22 KST

Lazada013-r1을 역적용한 뒤 공식 CreateProduct 응답 예제에 맞춘 누적 `lazada-013-r2.patch` SHA-256 `4dd7e7e6b37dd29450272e8ad0c62f2212a62c94440efbe451e0fcdd72eeb7da`만 중앙 로컬 정본에 적용했다.

r2는 CreateProduct 성공 응답에서 `code=0`, numeric `item_id`, exact ordered `sku_list`의 `seller_sku`와 numeric `sku_id`를 요구한다. 공식 응답에 없는 `item_status`는 요구하지 않는다. 상태와 요청 visibility는 후속 공식 `GET /product/item/get`에서만 확인한다. item_id만 있거나 SKU가 빠진 응답은 provider mutation 뒤의 reconciliation으로 보존하며 CreateProduct를 자동 재시도하지 않는다.

적용 순서는 여전히 012 current-read builder → 011 단일 CreateProduct → 013 exact item/SKU identity → exact GetProductItem → 010 completion → final lease → central internal completion이다. 현재 shared executor에는 이 단일 경로 연결이 아직 없으므로 adapter 코드 통과를 실제 등록 완료로 계산하지 않는다.

중앙 집중 검사는 `24/24`, nonincremental TypeScript와 변경 파일 ESLint가 통과했다. 운영 OAuth/provider/DB/deploy/gate/commit/push는 0이며 신규 CREATE와 공식 원격/판매자 화면 완료는 `0/8`, 진행률은 `19/48=39.6%`다.
