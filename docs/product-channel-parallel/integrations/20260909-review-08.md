# 여덟 번째 통합 검토 — Temu 판매구성, 11번가 배송비, eBay 갱신 검증

2026-09-09. 중앙 기준 `eb2486e872079bd70e2843ab9f7bb09ea11b08e6`. 이번 확인 때 상품등록 담당8개는 모두 idle이었다. 전체 상품등록 완료가 아니라 전용 수정본 제출, 중앙 통합, 인증/입력/운영 선행조건 대기였다. 중앙이 남겨 둔 아래3개를 직접 적용하고 검증했다.

## 반영

- Temu self-contained r6 SHA256 `1e5574643e3967f5c2ef89b31d42dfc4ead82f4d911531ede7e0e9fb3f8ba53b`, before6파일 일치 확인. 합성 Type=Standard 기본값을 판매자 확인 구성값으로 바꾸며, 판매자가 명시한 실제 Type=Standard는 일반계약에서 허용한다. 내부6봉을 판매수량6개로 자동 추론하지 않는다. 상세 구성문구는 workbench에서 보존하고 dropdown은 명시적인 정규 선택값만 인정한다. 잘못된 r5 commit은 적용하지 않았다.
- 11번가 최종후보 `e3882cfe5601dac3ed7437f48d16bba750999409`의 중앙 대비2파일만 before/after SHA256을 대조해 반영했다. 판매가1만원 이하 국내택배의 배송비5천원 예외를 보존하고 그 외50%/5만원 상한을 적용한다. 3190/3000을 허용하며 원본 값을 바꾸지 않는다. 진짜 초과값만 provider 호출 전에 수정 필요로 반환한다. 잘못된450b87/122124를 단독 적용하지 않았다.
- eBay `5efaeb503f78d18713d094d66564dcd1c82f30ae`의 신규 durable-refresh 테스트1파일을 확인한 SHA256 `9310012f4609b2aace7549325cd991ede548b81ad579c1cfd7777a9289d4300e`로 반영했다. 첫 갱신의 begin/stage/identity/완료 순서, 다음 작업의 토큰 재사용을 fixture로 검증한다. 공통 runtime 수정이나 실제 운영토큰 갱신은 없다.

## 검증

관련13개 파일의276/276검사, 전체 nonincremental TypeScript, 변경9파일 ESLint, 업무/채널 import 경계 감사 포함 로컬 Next 빌드 통과. 로그는 `.local/product-channel-inbox/review08-impact.log`, `review08-tsc.log`, `review08-lint.log`, `review08-build.log`다. provider 응답과 저장은 해당 테스트의 fixture이며 실제 등록 증거로 계산하지 않는다.

## 남은 책임

- 중앙: Shopee005 helper 검토 및 exact target API/cache 연결. eBay003의 진단4파일 검토.
- Shopee: 운영 target CAS table/RPC 미반영으로 안전한 토큰갱신 전 차단. 운영 배포 보류를 우회하지 않는다.
- Lazada: 정상OAuth start 이후 추가 확인 대기. 앞선10분 state는 만료됐으므로 동일 code/state를 재사용하지 않는다.
- 11번가: API관리2차인증 이후 현재키의 API GET 검증.
- Temu: 앱Inactive/심사Reviewing, 운영 자격/숍/실제 상품값 확인.
- 스마트스토어·쿠팡: 이미 등록된 동일SKU를 재생성하지 않고 실제 신규 상품 입력을 기다린다.
- Qoo10: 기존 UI 증거와 별도로 current credential QAPI 읽기 검증 필요.

기본 통합8/8, 전체단계19/48=39.6%, 이번 통합본 신규CREATE·원격/사이트 완료0/8을 유지한다. 이미 통과한 로컬/통합 단계를 재검증했다고 퍼센트를 추가하지 않는다. 이번 배포·운영DB·상품변경0. 새 담당작업을 켜기만 하거나 같은 외부 차단을 반복 호출시키지 않았다.
