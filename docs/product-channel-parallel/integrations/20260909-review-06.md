# 여섯 번째 통합 검토 — 로컬 통합 검증 완료

2026-09-09. 기준 커밋은 `0b403b60bd8e023f3282109079576c54f3927008`이다. 채널별 제출본과 현재 파일의 SHA256을 대조해 적용했다. **기본 상품등록 흐름 중앙 통합 검사 8/8, 실제 신규등록·원격 재조회·사이트 완료 0/8**이다. 이 수치는 현재 계정/API 권한이나 상품 필수값까지 모두 확인됐다는 뜻이 아니다.

## 반영한 범위

- Shopee owned f5a346→aa9d9aeb, common001-r2, requirements route002-r1, UI003-r2를 통합했다. 최종 UI patch SHA256은 `7553c4db11ec223fa43af025accf7bfed90d48986fc70d2688d9fc073a41511b`다. 브랜드·카테고리 속성·물류·창고·SG 숍은 공식 조회 결과에서 명시 선택하며 후보 하나를 자동 선택하지 않는다.
- 조회 route는 shop/merchant 토큰과 판매자 문맥을 각각 확인한다. 조회 중 OAuth refresh를 실행하지 않으며 비관리자·다중 활성 키·다른 숍·만료 토큰·취소 요청을 차단한다. SG 조회는 다른 시장 캐시에 의존하지 않는다.
- Shopee 미완성 입력도 초안으로 저장할 수 있다. 실행 직전에 현재 대상·입력·10분 유효기간을 다시 검증하며 다른 채널의 저장/실행에 Shopee 조건을 적용하지 않는다. 같은 대상 재조회, 공식 정책이 허용한 직접 브랜드명, 공백·쉼표 순차 입력을 지원한다. 표시 SGD 가격이 기본값과 같아도 저장 후 복원할 수 있도록 가격 patch를 명시 보존한다.
- Coupang owned15b6af20, OPTIONS-r2의 공통10파일을 모두 반영했다. 옵션 기준 SKU는 서버가 현재 상품 원장 값에 대조해 확정하고, 다른 상품/SKU 집합은 enqueue 전에409로 거부한다. 옵션별 SKU·가격·재고·metadata를 보존하고 접미사 추정 및 잘못된 숫자를 거부한다. 옵션 편집 초점과 저장/복원을 유지한다.
- 마지막 공통 workbench 변경은 3방향 병합으로 반영했다. CSS의 인접 수정 충돌은 검증한 원본 두 줄에 쿠팡 스타일을 적용하면서 Shopee 스타일을 보존해 해결했다. 이전 파일로 통째로 덮어쓰지 않았다.
- Qoo10 owned535f337f/common003-r2로 원본 이미지 보관과 승인 상세 이미지 조건을 구분했다. eBay owned7fcf8148의 테스트4개는 삭제한 상품 복구 앱 경로를 되살리지 않고 현재 구조에 맞췄다. 역사 migration 검사14개를 보존했다.

## 중앙 검증 증거

| 범위 | 결과 | 로컬 로그 |
|---|---|---|
| 화면 통합 전 공통·관련147파일 | 1,286/1,286 | review06-core-tests.log |
| 최종 화면 통합 후 Shopee·쿠팡·공통 등록 영향 | 341/341 | review06-final-impact.log |
| 실제 Chrome 로컬 React 입력·저장 | 2/2 | review06-final-browser.log |
| 업무 분리 / 채널 분리 | 40/40, 28/28 | review06-domain-tests.log, review06-channel-tests.log |
| 서버 쿠팡 상품 식별자 보강 관련 | 27/27 | review06-coupang-server-identity-tests.log |
| 최종 TypeScript nonincremental | 통과 | review06-final-typescript.log |
| 변경·관련82파일 ESLint | 통과 | review06-final-lint.log |
| 최종 로컬 Next 빌드 및 업무/채널 import 감사 | 통과 | review06-final-build.log |

로그와 파일 목록은 Git 제외 `.local/product-channel-inbox/`에 보존한다. 검사 범위는 서로 중복되므로 합산해 새 총 테스트 수로 보고하지 않는다. 전체 저장소 모든 테스트나 실제 판매자 인증/등록 검증은 아니다. Chrome 검사는 외부 HTTP를 차단한 로컬 fixture이며 사용자 판매자 프로필에 접근하지 않았다.

## 8개 작업 정지 상태 확인과 다음 실행

상태 조회 시 8개 모두 마지막 턴 completed, error null이었다. 당시 Shopee·쿠팡·Qoo10·eBay는 idle, Lazada·Temu·스마트스토어·11번가는 notLoaded였다. 이것은 작업 턴이 끝났다는 상태이며 상품등록 전체 완료가 아니다. 특히 중앙에 남아 있던 Shopee UI와 쿠팡 공통2파일 통합은 이번 검토에서 해소했다.

| 채널 | 현재 남은 실제 연결·등록 검증 |
|---|---|
| Shopee | 현재 판매자 로그인, merchant/SG shop/API 권한, 공식 필수조건과 실제 상품값 확인 |
| Lazada | Open Platform/MY 로그인, 현재 Commerce 권한·판매자 모드·티켓58021 상태 확인 |
| Temu | 마지막 관측 compliance Reviewing / app Inactive 해소 및 실제 goods 권한 확인 |
| 스마트스토어 | 현재 Commerce API 권한·카테고리·상품 인증값 확인. 기존 작업에 읽기 전용 검증을 배정했고 active/inProgress 확인 |
| 쿠팡 | 마지막 관측 WING 계정 제한 및 추가정보 재인증, API 권한·실제 카테고리 metadata 확인 |
| 11번가 | 현재 API 인증·실제 대상 필수값 확인. 이전 미배포 기간을 가정한 과거 projection 보정 제안은 운영 적용하지 않음 |
| Qoo10 | 저장된 승인 자격증명으로 QAPI 읽기 검증, 실제 신규상품·이미지·배송 조건 확정 |
| eBay | 현재 판매자 scope 및 EBAY_US 정책·상품 입력값 확인 |

외부 상태는 각 담당의 마지막 보고에서 확인한 범위이며 이 통합 배치에서 브라우저로 새로 확인한 결과가 아니다. 기존 상품번호나 이전 HTTP200은 이번 신규등록 성공으로 계산하지 않는다. 실제 등록은 중복 여부와 정확한 상품·판매자·SKU·가격·재고·이미지/배송조건을 확인하고 중앙이 한 실행 담당에 배정한다.

배포·운영 DB 쓰기·provider 쓰기·release gate 변경은 모두0이다. 검증된 코드 전달 대상은 private origin의 integration-aside뿐이다.
