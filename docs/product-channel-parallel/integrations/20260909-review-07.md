# 일곱 번째 통합 검토 — 실제 연결 오류 수정과 운영 선행조건

기준: 2026-09-09 18:34 KST, 중앙 커밋 `08e8821fdefaffa094ce539182cadacc5188d112` 이후 로컬 변경.

## 완료 기준과 현재 진척

채널별 6단계는 로컬 흐름, 중앙 통합, 현재 계정/API, 실제 새 등록 대상 필수값, 현재 통합본의 신규 CREATE, 원격 및 사이트 재조회다. 기본 통합 8/8은 review06의 검증된 기준본을 뜻하며 후속 수정 전체가 완료됐다는 뜻은 아니다. 현재 19/48=39.6%, 직전 17/48=35.4% 대비 2개 항목 증가. 현재 통합본 신규 CREATE와 재조회 완료는 0/8이다.

| 채널 | 항목 | 현재 확인 및 다음 작업 |
|---|---:|---|
| 스마트스토어 | 3/6 | OAuth, seller/account/channel, category, 검색 API 200. 저장된 유일한 실제 상품은 이미 등록됐고 다른 초안은 미입력. 신규 상품 링크/판매가/재고를 사용자에게 요청한 상태. |
| 쿠팡 | 3/6 | API 계정, category, 출고/반품지와 기존 상품/재고 GET 200. 과거 생성된 상품의 현재 조회 성공을 이번 통합본 CREATE로 계산하지 않음. 새 등록 대상 필요. |
| eBay | 3/6 | seller jeon_57 계정, scope, selling privilege와 location/policy 공식 조회 확인. 이번 eBay 전용 코드 수정 적용 중. 실제 USD 가격·이미지·반품 정책이 선택되지 않음. |
| Shopee | 2/6 | CHANGHEE SG 로그인 확인. shop/merchant access 만료, refresh 기간 유효. 운영에 target별 durable refresh table/RPC 없음. 외부 갱신 전 차단. |
| Lazada | 2/6 | MY 판매자와 앱 권한 활성 확인. 수동 auth 페이지에 state 없음 확인; 기존 화면 승인 금지. 정상 SellerPilot OAuth start 한 경로와 실행자 지정. |
| Temu | 2/6 | 앱 Inactive/심사 Reviewing. r5의 Standard 전면 금지 및 포장수량 해석을 재검토 요청. |
| 11번가 | 2/6 | 현재 API 계정 검증/2차 인증과 배송비 정책 검토 진행. 기존 상품 수정은 이번 신규등록 검증과 구분. 배송비 3000원을 임의로 1590원으로 바꾸지 않음. |
| Qoo10 | 2/6 | QSM 로그인과 기존 상품 확인. credential에 결속된 QAPI 조회 필요. review06의 owned535f/common003-r2는 이미 통합됨. |

계정/API 3단계는 해당 시점의 읽기 검증이다. 토큰 영속화와 다음 실행의 갱신 검증을 대체하지 않는다. 쿠팡의 과거 생성/현재 원격 확인은 유효한 별도 이력이며 새로운 통합본 성공 분자에는 넣지 않는다.

## eBay 수정

제출 `6694d08732ac348cdf87575b9928a10d4aaf8e8a`의 preflight 및 테스트 2파일을 before SHA256 일치 확인 후 적용했다. 정확한 getOffers HTTP404 / API_INVENTORY / 25713은 공식 실조회에서 확인된 미등록 응답으로 처리한다. 다른 404, 권한 오류, 혼합 오류는 여전히 차단한다.

추가로 errorId의 Number 강제변환을 제거했다. 배열 [25713], 중첩 배열, 객체, 공백·소수점 문자열을 상품 없음으로 오판해 CREATE하는 상황을 막고 숫자 및 정확한 숫자 문자열만 허용한다. inventory absence의 25702/25710도 같은 원칙으로 검증한다.

관련 preflight/실제 provider protocol/credential lineage 테스트 111/111, 전체 nonincremental TypeScript, 변경 2파일 ESLint 통과. 로그: `.local/product-channel-inbox/review07-ebay-tests.log`, `review07-tsc.log`. 이 배치는 이전 전체 빌드를 재실행하지 않았고 외부 API도 호출하지 않았다.

나머지 eBay 진단 helper/CLI/테스트 4파일은 아직 통합하지 않았다. CLI의 메모리 refresh 결과를 지속 연결 완료로 계산하지 않으며, 담당에게 정상 등록 경로의 durable refresh 검토를 배정했다.

## 운영 전환 전 확인할 정확한 항목

Shopee 담당의 `shopee-auth-004-r1.md`에서 운영 readback상 다음 3개가 없음을 확인했다. 이 배치 root가 직접 운영 SQL을 실행한 결과는 아니다.

- `sellerpilot_private.cs_shopee_target_refresh_claims`
- `sellerpilot_service_begin_cs_shopee_target_refresh_v1`
- `sellerpilot_service_prepare_cs_shopee_target_refresh_v1`

기존 migration `20260908142025_cs_shopee_target_refresh_cas.sql`과 target-aware gateway를 검토해 향후 허용된 운영 반영 후 다시 확인해야 한다. Shopee 실행자 `SHOPEE-SG-AUTH-20260909-01`은 갱신을 호출하지 않았고 쓰기 0이다. 운영 반영 허용 없이 우회 경로/수동 SQL로 실행하지 않는다.

별도 코드 결함: channel-targets 캐시 읽기 RPC에는 credential_id가 없는데 GET은 shop ID/all8 market만 맞으면 바로 반환한다. 옛 credential 캐시를 현재 것으로 오인하지 않도록 active credential 결속을 검증해야 한다. SG만 지정하는 POST와 credential 변경 전후 검증은 후속 중앙 변경 대상이며 아직 수정 완료로 표시하지 않는다.

Lazada 정상 인증 실행자는 `LAZADA-MY-AUTH-20260909-01`이다. 정상 admin authorize start → persisted state/HttpOnly cookie → MY OAuth → 기존 credential 저장 → seller/get 확인 순서만 허용했다. code/token/state 원문을 보고서에 저장하지 않는다. 아직 인증 완료 증거가 없으므로 단계는 올리지 않는다.

## 범위

이번 중앙 배치 상품/provider 변경·운영 DB 변경·배포·게이트 변경은 0이다. 인증 담당의 실제 실행 결과는 이후 별도 확인한다. 공유 코드와 문서는 0번만 통합하고, 각 담당은 전용 파일 또는 동결 제안으로 제출한다. 대기 중 작업을 전체 완료로 표시하거나 같은 외부 차단 호출을 반복하지 않는다.
