# Shopee009r2 historical source-chain integration

Verified frozen patch SHA-256 `ef5652b649b2da229b51b38b7ea97d2ba42baffc29c1554a3fb266de8519a6b8` and the three exact central before/after file hashes. The earlier r1 remained unapplied and is superseded by this full r2 patch.

The repaired migration accepts only three marker-cardinality states: the published source-only predecessor, the exact operator-old overlay, or the exact operator-new overlay. Duplicate operation markers, mixed published/operator states, partial attributes, mixed attributes, and unknown predecessor states fail closed. The source-only historical path can then reach the later canonical full claimant and the current Shopee SG execution fence.

Root independently ran the actual PGlite positive/negative cases and structural source replay. The Shopee tests plus the current SmartStore005/Shopee0130/SmartStore006 composition regressions passed `17/17`. Full nonincremental TypeScript, changed-test ESLint, business-domain boundaries and channel boundaries passed; no forbidden domain or cross-channel dependency was introduced.

This proves the selected historical/structural migration path, not the full all-channel migration chain or production installation. No operating SQL, deployment, OAuth, provider mutation, gate change, commit or push was performed by this integration.
