# 현재 실행: 네 독립 작업, 채팅 간 연락 없음

2026-09-13 사용자 최신 요청을 반영한다. 이전 README/status 문서의 타 담당 요청·응답 대기·공유 통합 절차보다 이 문서를 우선한다.

- 모델: 네 작업 모두 `gpt-5.6-sol`, reasoning `high`.
- 실행은 기존 프로젝트의 **Local** 환경. 앱에 등록된 옛 경로는 작업 컨텍스트일 뿐이며 모든 소스 명령의 workdir은 `/Users/kimchangheemac/dev/sellerpilot-app`이다. clone/worktree/별도 프로젝트를 만들지 않는다.
- 각 작업은 자기 `prompt.md`의 범위를 한 채팅 안에서 끝까지 해결한다. 다른 작업에 메시지를 보내거나, 상태/대화를 읽거나, 다른 작업을 깨우거나, subagent·새 채팅으로 다시 나누지 않는다.
- 다른 작업의 status/report를 읽거나 거기에 요청을 남겨 개발 의존성을 만들지 않는다. 자신의 결과 문서는 사용자에게 보여줄 산출물이다. 다른 작업은 이를 입력으로 삼지 않는다.
- 소유권은 `ownership.json` 그대로 유지한다. 공용 파일 변경이 필요해도 다른 소유 파일을 수정하지 않는다. 기존 계약을 유지하는 자기 모듈의 adapter/refactor로 해결하고, 불가능한 경계만 자기 최종 결과에 구체적으로 기록한다. 그 사유로 자기 범위 전체를 계획 단계에서 끝내지 않는다.
- 1~4번 모두 Git stage/commit/push/reset/stash/checkout/merge, 소유권 변경, 전역 포매팅, dependency 갱신, 공용 DB migration 적용·생성, Vercel 배포/환경변수 변경을 수행하지 않는다. 후속 통합·운영 적용은 네 구현 작업과 분리한다. 4번도 다른 세 작업을 통합하거나 기다리지 않는다.
- 공유 Next 출력 사용은 기존 `next` 잠금을 지킨다. 잠금이 점유됐으면 자신의 다른 구현/순수 테스트를 진행한다. 소스가 동시에 변경되는 동안 전체 build를 자신의 최종 결과라고 주장하지 않는다. 이번 각 작업의 기본 검증은 소유 모듈의 집중 테스트다. 전체 build는 사후 통합 검증이다.
- 3번만 기존 실행 환경의 복구를 runtime 잠금 안에서 할 수 있다. 다른 작업이 수정 중인 소스를 설치본에 복사하지 않는다. 실행 중인 provider job을 확인하지 않고 중단·재등록하지 않는다. 운영 배포 SHA 고정을 유지한다.
- API/DB/자산 ID/claim/lineage/승인 계약을 호환되지 않게 바꾸지 않는다. 증거 없이 완료 상태·외부 성공을 만들어 넣거나 보안 검사를 우회하지 않는다.
- 자신의 작업이 끝나면 자신의 `status.md`와 `result.md`에 변경 파일·검증·미확인 범위·후속 운영 적용을 기록하고 같은 채팅에서 사용자에게 완료 보고한다. 끝난 뒤 다른 파일을 다시 수정하거나 다른 담당 작업을 이어받지 않는다.

이미 완료된 준비: `237c0b2`의 이미지 hook/표시 분리, 소유권 manifest, 자원 잠금, 관련 81개 검사·타입·Next build 통과. 재분리/재통합/복제 폴더 정리부터 다시 시작하지 않는다. 해당 준비 이후 각 기능 결함은 아직 해결되지 않았다.

다른 작업 산출물인 `output/pdf/`와 그 밖의 비소유 변경은 보존한다. 새 실행 지시문은 각 lane의 `prompt.md`이며 옛 status 문서의 인용 지시문은 이번 실행에 사용하지 않는다.
