# 통합 검증결과 — 2026-09-13

## 2026-09-13 현재 통합·운영 상태

개발 원본은 `/Users/kimchangheemac/dev/sellerpilot-app` 하나다. 네 작업의 관련 검사 286/286, production build, 도메인 분리 42/42를 통과해 양쪽 integration-aside에 통합했다. 별도 개발 폴더·새 채팅은 만들지 않았다.

- **운영 웹·게이트웨이:** `e9db6ab3de69c7a3e6875da03fb1763af97ffe35`, Vercel `dpl_A8vQWxEZpBt9HPSzxzFVB8CxaYsH` Ready. 원격 build와 무작업 canary 6개 후 승격했으며 실제 운영 도메인, Aside 서버 SHA, Supabase 활성 SHA와 일정 6개를 재조회했다. 실행 중 작업을 정상 배출한 뒤 로컬 gateway도 같은 SHA로 교체했다. CS 완료 오류 한 건이 worker 전체를 종료시키던 문제의 수정이 포함된다.
- **Mac 이미지·CS:** e783fc0 통합 runtime 설치 및 581개 소스 해시 일치 확인. 그 이후 e9db6ab의 실행 코드 변경은 별도 gateway 프로세스에 적용되는 오류 처리다. 이미지/CS 코드 자체는 같다. 실제 OxiPNG/Zopfli 경로에서 합성 RGBA의 픽셀·알파 동일성을 확인했다.
- **운영 DB:** Aside 공식 계정과 SellerPilot `sqaoqucxakebqkiygdxb`를 확인하고 누락 CS migration 총 12건을 원문 해시 대조 후 적용했다. 이력·독립 초안 큐·credential binding·Lazada 수집/원문 재처리·답변 관찰을 포함한다. 기존 Lazada 보호 함수 본문 해시 보존, 서비스 전용 RPC, RLS/직접 조회 제한을 확인했다. 최종 조회 연결 17/60, blocking 0, idle-in-transaction 0, unsafe pending mutation 0.
- **검증 범위:** CS 초안 API HTTP 204를 운영 로그에서 확인했다. gateway 오류 회귀 23/23, 이번 CS DB 회귀 26/26 통과. 전체 저장소의 기존 실패 83건 전체를 해결한 상태는 아니다.
- **남은 문제:** eBay 게시 복구 RPC 1개와 11번가 복구 RPC 2개가 없다. Shopee 문의 정규화·일부 완료 저장/동시 실행 제한(SPC02)·경쟁 가격 갱신 503을 추가 점검해야 한다. 최종 조회 eBay/11번가/Lazada/Qoo10/Shopee 읽기 작업 각 1건은 lease 미만료이며 Shopee·Lazada 연결 진단 각 1건은 queued다. 실제 수집/답변/상품 등록 완료로 바꾸지 않는다. gateway는 새 버전이지만 degraded/503도 관측되어 전체 안정화 완료가 아니다.

계정, migration별 SHA, 배포·설치 증거 및 다음 작업은 [통합·DB 안정화 기록](reviews/20260913-integration-db-stabilization.md)에 기록했다. 아래 과거 날짜별 기록은 당시 상태다.

## 2026-09-12 당시 검증 기록

다음 내용은 첫 통합 시점의 이력이다. 현재 운영 배포·DB·실행 상태는 위 9월 13일 기록을 사용한다.

## 저장소 및 빌드

- 개발 기준: `~/dev/sellerpilot-app`. 독립 Git, 로컬 node_modules, 클라우드 경로 검사 통과.
- 운영 기준: Vercel Production `dpl_Fkbk9Kyg9whytrri5pUcG5PQkjUm`, Ready, Git SHA `fd426cc588f03c1e187b689141018b6de4a38eca` 확인.
- 통합: 운영 main + integration-aside `55d5a0f`의 최신 반응형 화면 + 검토한 상세/이미지/CS/배송 변경.
- 통합 코드 `2f5aafd`를 origin/couplit 양쪽 integration-aside에 반영. Production main은 유지.
- Preview `https://sellerpilot-global-k5ochwrl5-project-e59d.vercel.app` 생성 요청은 Vercel `BLOCKED / TEAM_ACCESS_REQUIRED`로 차단됨. 사유: 커밋 작성자에게 프로젝트 배포 권한 없음. Ready 배포로 표시하지 않음.
- Vercel용 Next.js 빌드 통과. 타입 검사 통과.
- 통합 기능 집중 검사 124/124, 상품·CS·배송 분리 검사 41/41, 상세 HTML/로컬 경로 검사 10/10 통과.
- 전체 TS 검사: 원본 3,707개 중 161 실패 → 통합 3,767개 중 83 실패. 새 실패명 0. 전체 통과는 아님.
- 브라우저: 로컬 로그인 화면 PC 1280/scrollWidth 1280, 휴대폰 390/scrollWidth 390. 콘텐츠/입력 UI 렌더 및 오류 오버레이 없음 확인. 로그인 이후 전 페이지의 실계정 검증으로 확대 해석하지 않음.

## 로컬 실행

- Mac AI: 통합 소스로 런타임 교체 및 LaunchAgent 실행 확인. 첫 초안 이미지 `SELLERPILOT_FIRST_DRAFT_IMAGES=1` 확인.
- 교체 중 발견된 실제 오류 수정: 상세 HTML 생성기의 app 폴더 의존성을 `lib/product-detail-html.ts`로 이동. Swift/sharp/marketplace-images 설치 사전 검사 후 교체 성공.
- 채널 gateway: 운영 SHA `fd426cc`에 고정. `http://127.0.0.1:8081/readyz`에서 ready, gateway configured, scheduler disabled, 마지막 claim 204 확인. 준비 상태이며 외부 등록/배송 완료 증거는 아님.
- supervisor: `~/dev/sellerpilot-worker-runner.sh` → 통합 저장소 `deploy/channel-gateway-runner.sh`. release pin은 `~/Library/Application Support/SellerPilot/gateway-release`. main 자동 추종, 강제 checkout 및 git clean 제거.
- 임시 `/tmp/sp-main` 작업자 18개 모두 정상 종료 확인. 처리 중 작업을 강제로 끊지 않고 종료까지 기다림.
- **CS 답변 초안: 운영 API 1회 연결 검사에서 HTTP 503.** Vercel의 SUPABASE_SECRET_KEY 설정 존재는 확인했지만 Supabase 커넥터 읽기 권한이 거부되어 원장 RPC 상태를 확인하지 못함. 반복 실패를 만들지 않도록 CS 전용 자동실행은 설치하지 않음. 실행 소스는 최신 통합 런타임에 포함되어 있음.

## 보존 및 iCloud 분리

- 미커밋 파일 4,458개를 해시와 함께 보존. Documents의 커밋된 원본도 로컬 소스 스냅샷으로 보존.
- 깨끗한 이전 작업트리 30개를 `clean-worktrees`로 이동.
- 23개 작업트리의 Git 이력을 로컬 `history.git`으로 다시 연결.
- 로컬 Git 이력이 불완전한 9개를 `unresolved-history`로 이동. 원래 포인터는 `.git.legacy-icloud`로 보존하여 자동 실행 저장소에서 제외. iCloud 원본 객체는 삭제하지 않음.
- 폴더 이동으로 바뀐 의존성 링크 16개를 복구. 운영 gateway node_modules도 독립 로컬 디렉터리로 교체.
- 검사한 dev 작업트리에서 Documents로 향하는 활성 `.git` 포인터 0개. 현재 앱과 정식 실행본은 과거 iCloud 자료를 읽지 않고 실행됨.
- 통합 앱/채널 실행본/AI 실행본 총 169,351개 항목 검사: iCloud를 향하는 링크 0개, dataless 파일 0개. 설치된 AI 실행본의 lib/scripts/prompts 내용은 통합 소스와 차이 0개.
- 일부 과거 객체는 iCloud placeholder라 전체 원본 디렉터리 이동을 취소했다. 과거 이력 전체가 로컬 복원됐다는 뜻은 아니다.

보존 위치: `~/dev/sellerpilot-archive/20260912`. 주요 원장: `working-changes-manifest.json`, `git-relink-manifest.json`, `clean-worktree-move-manifest.json`, `unresolved-history-move-manifest.json`, `dependency-symlink-repairs.json`, `manual-workers-drain.json`.

이번 작업에서 운영 main/Production 배포, 운영 SQL 적용, 신규 상품 등록, 고객 답변 전송, 배송 요청을 직접 실행하지 않았다. 기존 큐를 처리하는 정식 작업자의 실행 상태와 작업별 외부 완료는 구분한다.

Codex 앱에 등록된 기존 프로젝트 바로가기는 Documents를 가리킨다. 앱 자체를 제어하는 도구가 접근을 거부하여 바로가기 경로는 변경하지 못했다. 새 작업은 `~/dev/sellerpilot-app`을 열어 시작한다. 이 작업의 실제 수정·검사·실행은 모두 새 폴더에서 진행했다.
