import assert from"node:assert/strict";import test from"node:test";import{csRecoveryReadinessSchema,csRecoveryRequirements}from"../lib/cs/recovery-readiness";
test("recovery matrix names every required failure and keeps two daily observations",()=>{assert.equal(csRecoveryRequirements.length,11);assert.equal(csRecoveryRequirements.find(row=>row.drill==="daily_restart")?.requiredDistinctDays,2);for(const drill of["token_expiry","permission_revoked","backup_restore","delete_and_recollect","admin_revoked","reply_timeout_unknown"])assert.ok(csRecoveryRequirements.some(row=>row.drill===drill));});
test("readiness contract cannot call a missing drill complete",()=>{const requirements=csRecoveryRequirements.map(row=>({...row,observedDistinctDays:row.drill==="backup_restore"?0:row.requiredDistinctDays,lastPassedAt:row.drill==="backup_restore"?null:"2026-09-08T00:00:00.000Z",state:row.drill==="backup_restore"?"missing":"passed"}));const parsed=csRecoveryReadinessSchema.parse({contract:"sellerpilot-cs-recovery-readiness/1",checkedAt:"2026-09-08T00:00:00.000Z",ready:false,requirements});assert.equal(parsed.ready,false);});
